# План улучшений DocRAGenslate

Дата фиксации: **11 июля 2026 года**  
Статус: **исполняется поэтапно**  
Область: код, безопасность, качество перевода и RAG, парсинг документов,
эксплуатация и модельный контур.

## 1. Цель

Повысить надежность, измеримое качество и эксплуатационную готовность
DocRAGenslate без необоснованной замены уже работающего модельного стека.

Основной принцип: любая новая модель или технология принимается только после
A/B-проверки на реальных документах заказчика и не должна ухудшать безопасность,
лицензионную чистоту, p95-задержку и воспроизводимость результата.

## 2. Подтвержденное исходное состояние

На момент аудита локальный `main` и сервер `a100` находились на коммите
`e751006`, рабочая копия сервера была чистой, `/healthz` возвращал `ok`.

Сервер `a100` является **действующим production-контуром**. В системе созданы и
переданы пользователям 10 учетных записей. Поэтому дальнейшие работы выполняются
как изменения работающего сервиса с реальными пользователями и данными, а не как
свободные эксперименты на тестовом стенде.

Канонический production URL: <https://doc-rag-translate.ds-mind-lab.ru/>.
Публичная проверка 11 июля 2026 года: `GET /` возвращает HTTP 200,
`GET /healthz` возвращает `{"status":"ok"}`.

**14.07.2026, закрытый инцидент входа десяти пользователей.** Проверка через
изолированные браузерные контексты выявила незавершенные обязательные действия
Keycloak у части учетных записей и несоответствующие сохраненные учетные данные
у трех записей. В Keycloak для `user01`-`user10` восстановлены постоянные
учетные данные, включенное состояние, заполненные обязательные поля профиля и
пустой список требуемых действий; секреты в репозиторий и журнал не записывались.
После исправления все 10/10 учетных записей прошли интерактивный вход, были
перенаправлены на `/`, получили токен и HTTP 200 от `/api/documents` и
`/api/folders`; профиль пользователя отображается. Дополнительный smoke под
обычной учетной записью подтвердил маршруты `/upload`, `/chat`, `/account`,
HTTP 200 для конфигурации, папок, документов, сессий чата и памяти, без ошибок
консоли. Инцидент закрыт; изменение parser-контура к нему не относится.

Активный модельный контур:

| Задача | Модель | Размещение |
|---|---|---|
| RAG-чат, агент, анализ изображений | Qwen3.5-35B-A3B GPTQ-Int4 | GPU3, `:8006` |
| Перевод EN/RU/ZH | Hy-MT2-7B BF16 | GPU1, `:8005` |
| Текстовые эмбеддинги | Qwen3-Embedding-8B, MRL 1024 | GPU4, `:8002` |
| Текстовый реранкер | Qwen3-Reranker-4B | GPU4, `:8003` |
| Визуальные эмбеддинги | Qwen3-VL-Embedding-8B | GPU2, `:8007` |
| Визуальный реранкер | Qwen3-VL-Reranker-2B | GPU2, `:8009` |
| Основной VLM-парсер | MinerU2.5-Pro | GPU5, `:30010` |
| Альтернативные парсеры | dots.mocr, PaddleOCR-VL | GPU0, `:8120`/`:8118` |

Установлены MinerU `3.3.1`, vLLM `0.22.1`, PostgreSQL 17 и pgvector `0.8.2`.
В базе на момент проверки было 9 документов, 1796 сегментов, 260 RAG-чанков,
37 терминов глоссария и 9 ручных правок перевода.

Текущее состояние инженерных проверок:

- `pytest`: 36 тестов прошло, 2 теста XLSX завершились ошибкой;
- Ruff по `src`, `tests`, `alembic`: 16 ошибок;
- mypy: 47 ошибок в 18 файлах;
- ESLint: 36 ошибок;
- production-сборка React-SPA и TypeScript-проверка расширения проходят;
- отдельной конфигурации постоянной интеграции в репозитории нет.

## 3. Приоритеты

| Приоритет | Результат | Срок-ориентир |
|---|---|---|
| P0 | Зеленые проверки, fail-closed RLS, секреты, лицензии и резервное копирование | 1-2 недели |
| P1 | Эталонный набор качества и автоматический выпускной шлюз | 1-2 недели |
| P1 | MinerU 3.4 и MinerU-Popo A/B | 1-2 недели |
| P1 | Настоящий BM25 и улучшение фильтрованного HNSW | 1 неделя |
| P1 | Иерархический RAG и проверка цитат | 2-3 недели |
| P1 | Память переводов и строгая терминологическая проверка | 2 недели |
| P2 | Разделение очередей, systemd, полная наблюдаемость и SLO | 2 недели |
| P2 | Qwen3-VL/Granite KIE, доменная проверка RU/EN/ZH, Qwen3.6 и Hy-MT2-30B | после P0-P1 |

Сроки являются оценкой трудоемкости, а не календарным обязательством. Этапы
следует принимать по критериям качества, а не только по факту реализации.

### 3.1. Обязательные правила для действующего production

1. Не останавливать production-модели и пользовательский API ради эксперимента
   без согласованного окна обслуживания.
2. Все A/B-кандидаты поднимать на отдельном порту, временном GPU-слоте или
   изолированном пакетном стенде. Пользовательский трафик остается на текущем
   стабильном контуре до формального решения о переключении.
3. Изменения поведения закрывать флагами функций и включать поэтапно: оператор,
   1-2 пилотных пользователя, затем остальные учетные записи.
4. Миграции БД выполнять по схеме expand/contract: сначала добавление совместимых
   объектов, затем перевод чтения/записи и только после наблюдения удаление старого.
5. Перед миграцией или переиндексацией создавать проверяемую резервную копию.
   Переиндексация не должна удалять рабочий индекс до готовности нового.
6. Для каждого изменения заранее готовить команду отката, предельное время
   восстановления и ответственного за решение о возврате.
7. Не использовать пользовательские документы для внешних проверок, SaaS-судей
   или удаленных API. Все оценки выполняются внутри периметра.
8. Уведомлять пользователей об окне обслуживания и заметных изменениях поведения.
   Сохранять обратную совместимость API и расширения в течение переходного периода.

### 3.2. Обязательный цикл разработки и доставки

Канонический порядок работы над DocRAGenslate:

1. **Разработка в WSL.** Весь исходный код изменяется в локальном репозитории
   `/home/ruslan_safaev/rag_app`, где доступны fff, CodeGraph и Serena.
2. **Локальная проверка.** Выполняются тесты и статические проверки затронутых
   компонентов. React-SPA обязательно собирается локально, потому что на A100 нет
   штатного Node-контура сборки; готовится `web/dist`.
3. **Передача на A100.** Код и собранные артефакты переносятся в
   `/root/projects/rag_app/` с сохранением времени и прав файлов. Перед переносом
   проверяется перечень измененных файлов; пользовательские данные не копируются
   с production на внешние контуры.
4. **Проверка на A100.** В серверном окружении выполняются Python-тесты,
   миграционная проверка, доступность Postgres/Redis/MinIO, модельных сервисов,
   воркеров и GPU-зависимых сценариев. Новая версия еще не считается принятой.
5. **Обновление production.** После успешной проверки выполняются совместимые
   миграции, доставка `web/dist`, перезапуск или переключение API/воркеров и
   включение флагов функций по утвержденному плану.
6. **Проверка живого приложения.** Выполняется smoke-набор через
   <https://doc-rag-translate.ds-mind-lab.ru/>, затем пилотная проверка под
   выделенной учетной записью без раскрытия данных остальных пользователей.
7. **Синхронизация.** Локальная и серверная копии, commit SHA, миграции,
   конфигурация запуска и развернутый `web/dist` приводятся к одному состоянию.
   Расхождения фиксируются до завершения работы.
8. **Отчет.** В `docs/roadmap.md` записываются версия, проверки, результат,
   известные ограничения и команда отката.

Production не используется как место первичного написания кода. Допустимы только
операционные исправления для восстановления аварийного сервиса; после такого
исправления изменение немедленно переносится обратно в WSL, проверяется и
фиксируется в общей истории, чтобы сервер не стал отдельной веткой кода.

### 3.3. Трекер исполнения 20 пунктов

Статусы обновляются в этом файле после каждого существенного шага. Значение
`исследование` означает, что production-код и пользовательский трафик еще не
изменены. Пункт считается завершенным только после проверки на A100, требуемого
пилота, production-smoke и синхронизации локального и серверного состояния.

| № | Пункт | Статус | Текущий результат |
|---:|---|---|---|
| 1 | Зеленые тесты, Ruff, mypy, ESLint и self-hosted CI | **завершен** | 312 тестов, Ruff, mypy, ESLint, Alembic, SPA и extension проходят локально/A100; self-hosted run `29208255226` успешен, production smoke и SHA-синхронизация выполнены |
| 2 | Fail-closed RLS и тесты изоляции | **завершен** | Миграция `0025`, строгий принципал, проверка запуска и FORCE RLS на 16 таблицах развернуты; SQL-запрет по умолчанию и A/B/admin HTTP-canary проходят без утечек |
| 3 | Секреты, лицензии, Keycloak, резервное копирование и восстановление | отложен | 2026-07-13 владелец отложил security-работы; read-only аудит и отдельный dump Keycloak выполнены, production-конфигурация и учетные записи не изменены |
| 4 | Эталонный набор документов и вопросов | **завершен** | A100 release: 236/268 полностью автоматически проверенных кейсов и private sidecar 1:1. Строгий read-only component baseline production direct-RAG зафиксирован с полным provenance: Recall@10 0,9586, точность определения наличия ответа 0,8983, p95 3,370 с; production не изменялся, WSL/A100 синхронизированы |
| 5 | Автоматический выпускной шлюз качества моделей | **завершен** | Fail-closed шлюз для замены LLM: подписанные baseline/candidate/qualification, строгая Git- и corpus-привязка, 20 000 парных bootstrap-выборок, лицензия, long-context, нагрузка, judge и проверяемый откат. Новый A100 baseline на 236 сложных кейсах подписан; policy закреплена, кандидат пока не допущен |
| 6 | A/B MinerU 3.4 | **завершен** | A/B на 147 открытых сложных страницах и закрытом Gold выполнен. 3.4.4 улучшил quality/bbox/графики и скорость, source-only shadow retrieval принят без регрессий; production переключен на точный MinerU 3.4.4 с готовым откатом на 3.3.1. Атрибуция, A100/production smoke и 10/10 учетных записей проверены; отдельным долгом остается эталонная внутристраничная метрика порядка блоков |
| 7 | A/B MinerU-Popo и дерево документа | **завершен** | Изолированный A100-прогон на 7 сложных EN/RU/ZH документах (61 страница, 934 блока) выполнен без ошибок. Кандидат не допущен в production: штатный сборщик дерева потерял 3 блока, а текущий Gold не содержит достаточной разметки структуры и downstream-вопросов для доказательства улучшения RAG. Добавлены строгий `DocumentTree v1`, lossless Popo-адаптер, tree-aware shadow-чанкинг и fail-closed A/B-шлюз; режим остается `off` |
| 8 | Оценка качества парсинга и управляемый fallback | **завершен** | Page-router проверен на 147 сложных страницах, а исправленный MinerU→Paddle fallback — на реальной сложной таблице A100. Устранены сравнение кандидата с сырым вместо финального MinerU, мягкое принятие поврежденной структуры и обработка всего PDF вместо выбранных страниц. Кандидат корректно не допущен: Paddle восстановил таблицу, но не дал точных bbox для цитат; production остается `shadow`, fallback выключен |
| 9 | Настоящий BM25 | **завершен: NO-GO** | `pg_textsearch 1.3.1` и RU/EN BM25-индексы изолированно проверены на 236 сложных кейсах. Четыре fail-closed qualification-прогона не смогли получить воспроизводимый общий reranker: финальный protocol v4 с тройным усреднением остановлен, потому что production-shaped final set не совпал с frozen scores (`case_id=ragq-230802d83f01233877ae678e`). Это не доказывает проигрыш BM25, но не дает допустимого доказательства улучшения. Кандидат не принят; production остается на PostgreSQL FTS/GIN, миграция и трафик не переключались. Эксперимент `dc25476` отменен в `575f864`. |
| 10 | Фильтрованный HNSW | не начат | Требуется проверка iterative scan в pgvector 0.8.2 |
| 11 | Иерархический поиск | не начат | Архитектурная гипотеза описана |
| 12 | Точный бюджет контекста и проверка цитат | не начат | Метрики приемки описаны |
| 13 | Память переводов | не начат | В production только 9 ручных правок |
| 14 | Защита чисел, единиц, стандартов и терминов | не начат | Детерминированный контур спроектирован |
| 15 | Дообучение Hy-MT2 после накопления данных | отложен | До 1000-3000 проверенных пар обучение не выполнять |
| 16 | Разделение ARQ-очередей и идемпотентность | **в работе** | Structured-sidecar получил bounded KIE job, отдельную очередь, claim/lease/retry/conditional publish и `max_jobs=1`; флаг выключен, producer заявок еще не подключен, основная очередь не изменена |
| 17 | API и воркеры под systemd | не начат | Требования к процессам описаны |
| 18 | Наблюдаемость, SLO и оповещения | не начат | Набор обязательных метрик описан |
| 19 | Frontend: lint, lazy loading, Playwright и доступность | **в работе** | Обновлены Vite 8.1.4/React 19.2.7/pnpm 11.12, устранены 36 ошибок ESLint; lint и обе production-сборки проходят. Lazy loading, E2E и a11y еще не выполнены |
| 20 | RAG red-team и финальные модельные эксперименты | исследование | Парный A/B Qwen3-VL/Granite завершен: общего статистически доказанного победителя нет; в этом прогоне Granite быстрее и завершил 150/150 ответов, Qwen лучше по нормализованному EM, вложенным формам и идеальным документам. Qianfan исключен из активных кандидатов. Остались доменная проверка RU/EN/ZH, RAG red-team и генеративные модели |

#### Журнал пункта 4: закрытый эталон RAG

**13.07.2026, автоматический контур и A100 trial.** В WSL добавлены строгий
`GoldRecord`/JSON Schema, приватный sidecar с привязкой 1:1, валидаторы,
двухпроходный локальный Qwen-review с третьим adjudicator при расхождении и
отдельный production baseline-evaluator. Человеческая проверка не требуется:
по решению владельца все кейсы проверяются автоматически внутри периметра.
Локально прошли 425 тестов (1 пропущен), mypy новых модулей/скриптов,
Ruff затронутых файлов и `git diff --check`.

На A100 успешный trial сформировал 16/16 строгих кейсов: по 4 single/multi/
cross-document/no-answer, языки RU/EN/ZH, все обязательные типы text/table/
formula/figure/scan и признаки numbers/units/standards/prompt-injection/leakage.
Независимо сверены реальные SHA-256 исходников MinIO, канонический снимок 262
чанков, две владельческие области, 20 дословных evidence excerpts и retrieval
probes; все артефакты имеют режим `0600`. Корпус production не изменялся.

Автоматический isolation smoke прошёл для 3/3 доступных OIDC bearer-сессий и
10/10 реальных Keycloak subject через точную production-роль `rag_api`: чужие
документы не видны, чужой document/chat scope возвращает 404, пересечений
чанков нет. Восемь остальных учетных записей требуют интерактивный OIDC-шаг;
ради теста не включались устаревшие механизмы impersonation и не менялись
пароли или настройки realm.

Полный checkpointed-прогон сформировал 240 уникальных кандидатов; после первых
двухпроходных review release был корректно заблокирован из-за недостатка
`standards`, `prompt_injection` и `leakage`. Порог не снижался и плохие кейсы не
исправлялись вручную. Хеш-связанный supplement добавил 28 новых сложных случаев,
после чего финальный review v5 принял 236/268 и атомарно выпустил release с
парным sidecar. Release содержит RU/EN/ZH = 82/73/81; 55 явных no-answer и
12 leakage-задач, поэтому baseline ожидает отказ в 67/236 случаях;
`standards` = 9, `prompt_injection` = 13, `leakage` = 12; минимум каждого
content type = 7. SHA-256: review
`31f028d0c2786270f9f7a0232bd1a5634db3768abe1422aebd34263acd287f48`, release
`c5ac4752dba3f7303832847c6caa5e4aa21f262ab5be1fe2768ac17be8ef1578`, sidecar
`1635d3a59465fd6b4f9b9ca56c97bbc091a1f1c7c4e7c46c707a1272924ec717`.

Строгий read-only component baseline выполнен одним непрерывным запуском на
production Retriever + direct `ChatEngine` из чистого Git SHA
`9ef2f7cc2ac395af61b14969bbb6aa71f7f9a546`. Это не проверка полного
`AgentLoop`: она фиксирует отдельно retrieval, прямой ответ, цитаты и
числа/единицы. Результат: answerability accuracy 0,8983; Recall@1/5/10 =
0,6420/0,9320/0,9586; MRR@1/5/10 = 0,9231/0,9558/0,9567;
nDCG@1/5/10 = 0,9231/0,9179/0,9292; citation precision/recall =
0,7926/0,8343; quantity+unit accuracy/recall = 0,2297/0,7905;
unsupported-number rate = 0,3973; total latency mean/p50/p95 =
1,776/1,613/3,370 с. Ожидаемый отказ корректен в 65/67 случаях; для ответных
вопросов модель не отказалась в 147/169. Слабые сегменты базовой линии:
ZH answerability 0,8148 и cross-document 0,8362, несмотря на Recall@10
0,9917 и 0,8673 соответственно. Это исходные показатели для пунктов 5, 9--12
и 14, а не заявление об улучшении качества.

Отчёт `baseline_component.json` имеет SHA-256
`ef79566abdb340d4d7a1504cfea6f7839f08c68d8046320a78ecc0ce374bf336` и режим
`0600`; сериализованные ответы и найденные фрагменты в него не входят. Provenance
содержит чистый Git SHA, хеши release/sidecar, снимок двух областей и десяти
документов, точную конфигурацию/промпт/seed, runtime-версии/командные строки и
полные манифесты весов: LLM
`bb25801552a81d9015823e17c08f88d3b9aa04eedf7ae9b061ecaac716c14f9b`,
embedding `9ef582abafbbe95f0e6cb976bb52524b9b95aff97e9d71919d11c4b47713a71a`,
reranker `23113c952832597ed1c026248624fca63202d1cc3a63110f596538dde5e598e9`.
До и после запуска снимки корпуса и моделей совпали; production-трафик и данные
не менялись. Публичные `/healthz` и `/` вернули 200, auth остался включён.

#### Журнал пункта 5: автоматический выпускной шлюз RAG-моделей

**14.07.2026, завершение.** Реализован закрытый fail-closed шлюз замены LLM.
Baseline, candidate и сырые qualification-измерения читаются один раз из
owner-only файлов, проверяются по точным SHA-256 и HMAC, а отчёты обязаны быть
созданы одной чистой Git-ревизией оценщика. Подпись охватывает прямые зависимости
RAG-оценщика, `pyproject.toml` и `uv.lock`. Произвольная policy запрещена:
comparator принимает только версионированный JSON с вкомпилированным SHA.

Приёмка пересчитывает per-case агрегаты, выполняет 20 000 стратифицированных
парных bootstrap-выборок с поправкой на множественные проверки, контролирует
редкие RU/EN/ZH и multi/cross-document срезы, числа/единицы, цитаты и задержку.
Сырые qualification-данные включают 30 длинных контекстов, 200 парных запросов
при 10 одновременных клиентах, локального неизменяемого judge для всех 236 Gold
кейсов, локальную Apache-2.0 лицензию и полный откат: 6 событий, повторное
измерение Git/config/corpus/весов, пробы всех активных моделей и 10 smoke.

На A100 из чистого SHA `52747e11ea39267d0b8094ef4b9ea1fa4a3c85bf`
повторно выполнен read-only baseline на 236 сложных кейсах. Report SHA
`d9e2c72e1e325637a61d93aad8ddc9fcab30889443d5492ddc165d8cdedce23b`,
attestation SHA
`a87d6154936dfd0fecbb798912806e5cd674fe9757e34204f17e3efce625bc2b`;
оба файла `0600`, подпись успешно проверена обратным чтением. Результат остаётся
в пределах исходного шума: answerability 0,8983; Recall@1/5/10 =
0,6391/0,9349/0,9556; nDCG@10 = 0,9275; quantity+unit accuracy = 0,2297;
unsupported-number rate = 0,3965; p95 = 3,430 с.

Локально прошли 491 тест (1 пропущен), финальный целевой набор 83/83, Ruff,
mypy, CI-policy и patch hygiene; на A100 целевой набор также 83/83. Policy SHA
`9a12be4de15635959bd502cf1d7adcc1d8cedcd917b79eff67a040fc997e33ca`.
Список одобренных candidate weight manifest намеренно пуст: инфраструктура
готова, но ни одна новая модель этим пунктом не принята и пользовательский
трафик не переключался.

#### Журнал пункта 8: парсинг и fallback

**11.07.2026, исследование.** Большой запрос Tavily Research был отклонен
лимитом текущего тарифного плана, поэтому актуализация продолжена через Tavily
Advanced Search и извлечение первичных источников. Это ограничение не является
результатом проверки моделей.

Предварительно подтверждено:

- Qianfan-OCR 4B сообщает 93,12 на OmniDocBench v1.5 и позиционируется как
  лучший именно среди end-to-end моделей, а также поддерживает диаграммы,
  document QA и извлечение полей. Это не доказывает превосходство над
  специализированными парсерами. Источники:
  <https://arxiv.org/html/2603.13398v1>,
  <https://huggingface.co/baidu/Qianfan-OCR>.
- PaddleOCR-VL-1.6 0,9B сообщает 96,33 на более новом OmniDocBench v1.6;
  официальный leaderboard дает MinerU2.5-Pro 95,75 (в статье авторов 95,69).
  По общему парсингу эти
  сопоставимые результаты сильнее публичного результата Qianfan, но должны
  быть воспроизведены на доменном наборе. Источники:
  <https://arxiv.org/html/2606.03264v1>,
  <https://arxiv.org/html/2604.04771v1>,
  <https://github.com/opendatalab/OmniDocBench>.
- Production уже содержит PaddleOCR-VL 1.6 как выбираемый альтернативный
  парсер. Поэтому добавление Qianfan не является очевидным следующим шагом:
  сначала нужно проверить специализированный выигрыш на формах, KIE,
  диаграммах и chart QA, а также downstream RAG.
- Текущий код выбирает `mineru`, `dots_mocr` или `paddle_vl` вручную на документ;
  автоматической оценки качества, page-level маршрутизации и fallback после
  низкой уверенности нет. Изменение поведения пока не выполнялось.

Текущее решение: не загружать Qianfan на production и не занимать GPU до
завершения сравнительного исследования, определения доменного набора и
реализации отключенного по умолчанию контура оценки качества. Итог исследования
должен содержать финальную таблицу кандидатов, пороги A/B и решение о допуске к
теневому прогону на A100.

Эта запись сохраняется как исходная гипотеза и заменена результатами
разделов 6.3, 6.20 и 6.21: Qianfan проверен и исключен из активного списка,
а прямой парный A/B выполнен для более сильной пары Qwen3-VL/Granite.

**11.07.2026, аудит кода и локальный нулевой этап.** Подтверждено, что поле
`parser_backend` задает ручной backend на уровне всего документа, а пункт UI
«Автоматически» означает только глобальный `mineru`. Для PDF ветки `dots_mocr`
и `paddle_vl` выполняются лишь при наличии текстового слоя и без `force_ocr`;
image-only сканы фактически уходят в MinerU. Общего quality fallback нет, а
адаптеры dots/Paddle не проверяют полноту числа возвращенных страниц.

Перед автоматическим переразбором выявлены два блокирующих риска:

1. Повторные parse jobs имеют случайные идентификаторы и могут исполняться
   параллельно без проверки ожидаемой ревизии/статуса.
2. Старые RAG-чанки сохраняются до последующей индексации, а retrieval не
   фильтрует статус документа. Во время неудачного переразбора пользователь
   потенциально продолжит получать устаревший RAG-контент.

Добавлен чистый модуль `src/rag_app/pipeline/parse_quality.py`, который без
моделей, БД и сети формирует `ParseQualityReport` по полноте страниц, наполнению,
повторам, структурному содержимому, таблицам, bbox и прокси-покрытию нативного
текста. Добавлено консервативное правило: fallback допустим только при слабом
или упавшем основном результате, приемлемом кандидате и минимальном отрыве.
Модуль не участвует в выборе результата, поэтому поведение production не изменилось.

После локальной проверки модуль подключен к PDF-ветке только как теневой
наблюдатель за флагом `RAG_PARSER_QUALITY_SHADOW_ENABLED=false`. При значении
по умолчанию код оценки не выполняется. При явном включении логируются только
числовые сигналы и причины уже выбранного результата без текста документа,
запуска второго парсера и изменения сохраненных сегментов. Для последующей
агрегации такой же обезличенный summary сохраняется в nullable JSONB
`documents.parse_quality`; миграция `0021` не меняет RLS и публичный API. При
переразборе с выключенным shadow старое значение очищается, чтобы не показывать
метрики от предыдущей версии результата.

Локальная проверка:

- `uv run pytest -q tests/test_parse_quality.py tests/test_segments.py` —
  **17 passed**;
- `uv run ruff check src/rag_app/pipeline/parse_quality.py tests/test_parse_quality.py`
  — без ошибок;
- `uv run mypy src/rag_app/pipeline/parse_quality.py` — без ошибок;
- `git diff --check` — без ошибок.
- полный `uv run pytest -q` — **44 passed, 2 failed**; оба сбоя относятся к
  ранее зафиксированным XLSX-тестам `test_xlsx_roundtrip` и
  `test_xlsx_skips_data_dump`, новые parser-quality тесты регрессий не внесли.

На A100 файлы перенесены в чистую серверную копию с тем же исходным SHA
`e7510067`; профильные тесты дали **17 passed**, Ruff и `git diff --check`
прошли, `AGENTS.md` и `CLAUDE.md` идентичны. Alembic видит единственную head
`0021`, текущая production-схема остается `0020`. На этом шаге миграция не
применялась, API/worker не перезапускались, `/healthz` остался `ok`.

**11.07.2026, production shadow.** Перед DDL проверена пустая ARQ-очередь и
создан schema-only dump Postgres размером 34505 байт в
`/root/parser_trials/rag_schema_before_0021_2026-07-11.sql`. Миграция `0021`
применена транзакционно: `documents.parse_quality` имеет тип JSONB и допускает
NULL. WSL, `origin/main` и A100 синхронизированы на SHA `2d18e51`; API и worker
перезапущены, внешний и внутренний `/healthz` вернули `ok`. Shadow включен в
worker через runtime environment, fallback и второй parser не включались.

Canary выполнен не на пользовательских данных, а на той же публичной первой
странице EPC/Turnkey benchmark. MinerU завершил parse за 15,85 с; отчет дал
`score=1.0`, `acceptable=true`, `page_coverage=1.0`,
`duplicate_ratio=0.0`, `integrity_ratio=1.0`, без причин отказа. JSONB записал
только агрегаты, после чего штатно прошли перевод, экспорт и индексирование.
Синтетический документ удален через штатный DELETE API, проверка БД дала ноль
оставшихся строк. Пользовательские документы и десять пилотных аккаунтов не
затрагивались.

**11.07.2026, защита переразбора и RAG.** Добавлена монотонная
`documents.parse_revision` и детерминированный идентификатор ARQ
`parse:{document_id}:{revision}`. API создает новую ревизию одним условным
`UPDATE ... WHERE status IN ('error','done') RETURNING`; worker занимает только
совпадающую `uploaded`-ревизию. Дубли и запоздавшие задачи завершаются без
парсинга. Финальная запись сегментов также защищена ревизией и статусом.
Dense, sparse и visual retrieval теперь возвращают чанки только документов со
статусом `done`, поэтому старый индекс не виден во время переразбора или ошибки.

Локально и на A100 защитный набор дал **23 passed**, Ruff прошел; полный набор
дал **50 passed, 2 прежних XLSX-сбоя**. Перед миграцией `0022` создан schema-only
backup 34530 байт. Production синхронизирован на SHA `63919ed`, миграция
применена, API/worker перезапущены в совместимом порядке.

Гонка проверена двумя одновременными `reparse` одного синтетического документа:
ответы составили ровно один `200` и один `409`; в БД создана только ревизия `1`,
в Redis находилась одна задача. При остановленном worker старый чанк физически
оставался (`1`), но число доступных через условие `status='done'` было `0`.
После запуска worker ревизия `1` прошла parse/translate/export/index и вернулась
в `done`; canary удален, очередь пуста, внешний `/healthz` — `ok`.

Следующий безопасный шаг: накопить shadow-распределение на новых/явно
переразобранных документах, собрать доменный набор и реализовать document-level
A/B в отдельном временном пространстве. Fallback допускается только под
выключенным по умолчанию флагом и allowlist 1-2 пилотных аккаунтов. Page-level
fallback отложен: текущие parser
API обрабатывают документ целиком, а надежное слияние страниц требует отдельной
работы с исходными `page_idx`, bbox и путями изображений.

## 4. Этап P0: качество кода и безопасность

### 4.1. Восстановить зеленый инженерный контур

Работы:

1. Исправить два падающих XLSX round-trip теста и определить ожидаемое поведение
   для названий листов, коротких кодов и числовых дампов.
2. Устранить ошибки Ruff в основном коде и миграциях.
3. Разделить ошибки mypy на отсутствующие stubs и реальные дефекты; сначала
   исправить несовместимые кортежи экспорта, `None`-ветки парсеров, типы OpenAI
   streaming API и актуальный Langfuse API.
4. Исправить React Hooks/immutability ошибки ESLint, затем предупреждения
   `react-refresh` либо корректно настроить правило для файлов маршрутов.
5. Добавить self-hosted CI: `pytest`, Ruff, mypy, ESLint, обе сборки, проверка
   Alembic `upgrade head`, Playwright smoke и `git diff --check`.
6. Ввести запрет слияния изменений при красном обязательном этапе CI.
7. До первого production-релиза прогнать полный набор на копии схемы и
   обезличенной/синтетической приемочной библиотеке, не на живой базе.

Критерий приемки:

- все тесты и статические проверки проходят;
- сборки воспроизводимы из lock-файлов;
- CI выполняется внутри периметра и не отправляет документы наружу.

**11.07.2026.** Исправлены две исходные XLSX-регрессии. Стандартные названия
листов `Sheet/SheetN` больше не создают ложные сегменты; короткие коды
`DMFA/BTC/pH/Eo` не считаются прозой, при этом естественные однословные подписи
`Valve/Status/Pressure` и длинные заголовки сохраняются. Добавлен тест значимого
названия листа. Профильный набор: **6 passed**, полный pytest: **53 passed**.

**12.07.2026, обновление зависимостей.** Выполнен полный совместимый upgrade
Python, React-SPA и WXT-расширения. Минимальные версии Python подняты до реально
проверенного lock-набора: FastAPI 0.136.3, Uvicorn 0.51.0, OpenAI 2.45.0,
Langfuse 4.14.0, SQLAlchemy 2.0.51, pgvector 0.5.0, Pillow 12.3.0,
pypdfium2 5.10.1, pytest 9.1.1, mypy 2.2.0, Ruff 0.15.21 и MinerU 3.4.4.
Добавлены актуальные `types-openpyxl`; код переведен на строгие типы OpenAI и
низкоуровневый API наблюдений Langfuse 4.14.

Frontend обновлен до React 19.2.7, Vite 8.1.4, Tailwind 4.3.2,
TanStack Router 1.170.17, Query 5.101.2, pdf.js 6.1.200, WXT 0.20.27 и
Playwright 1.61.1. Проекты закреплены на pnpm 11.12.0. Включены суточный
карантин новых публикаций, явный allowlist install-скриптов и воспроизводимый
локальный virtual store. TypeScript оставлен на 6.0.3: `typescript-eslint`
8.63 официально ограничен `<6.1`; типы Node закреплены на фактической ветке
Node 22, а не искусственно подняты до 26.

Осознанные ограничения: `pdftext 0.7.1` внутри MinerU точно требует
`pypdfium2==5.10.1`; FastAPI 0.137+ ввел `_IncludedRouter`, который вызывает
500 в instrumentator 7.1, а instrumentator 8.0.2 требует Starlette >=1.0 и
пока несовместим с FastAPI, поэтому закреплен диапазон `>=0.136.3,<0.137`;
MinerU 3.4.4 требует vLLM `<0.22`, поэтому
production parse-extra использует 0.21, а vLLM 0.25 тестируется отдельным
изолированным окружением. Форсировать эти версии с нарушением resolver-контракта
запрещено.

Локальная проверка: **303 passed**, Ruff без ошибок по `src/tests/alembic`,
mypy без ошибок по 74 модулям, ESLint без ошибок, сборки SPA и WXT проходят.
Self-hosted CI и серверная проверка на A100 остаются обязательными до закрытия
пункта 1.

**12.07.2026, A100 и production.** Коммиты `654346c` и hotfix `2564ebd`
доставлены на A100. Полное окружение синхронизировано с `--extra parse`:
FastAPI 0.136.3, OpenAI 2.45.0, Langfuse 4.14.0, SQLAlchemy 2.0.51,
pypdfium2 5.10.1, MinerU 3.4.4 и vLLM 0.21.0. Серверный полный набор дал
**304 passed**. Миграция 0024 применена с 0023 на пустой таблице structured
artifacts после schema-only backup
`/root/parser_trials/rag_schema_before_0024_2026-07-12.sql` размером 45547 байт.

Первый smoke обнаружил 500 на защищённых маршрутах с FastAPI 0.139.0:
instrumentator 7.1 обращался к отсутствующему `_IncludedRouter.path`. API был
немедленно возвращен на 0.136.3; добавлен regression-тест связки included router
и Prometheus middleware. После hotfix внутренний `/healthz` возвращает `ok`,
защищённый `/api/documents` без токена — 401, публичные `/healthz` и `/` — 200,
worker запущен с прежними `quality shadow` и `page router shadow`; KIE-флаг и
пользовательский parser fallback остаются выключены.

Добавлен self-hosted GitHub Actions workflow для Python 3.13/uv, pytest, Ruff,
mypy, Alembic single-head и полного offline upgrade, frozen pnpm install,
ESLint/SPA build, WXT compile/build и `git diff --check`. Fork-PR не исполняет
код на частном runner; checkout read-only без credentials, выгрузки артефактов,
SSH/A100/production-доступа и внешнего model cache нет. Policy checker закрепляет
pnpm 11.12, суточный minimum release age и allowlist install-скриптов. Полное
объединенное локальное дерево после добавления KIE job: **312 passed**, Ruff,
mypy по 75 модулям, Alembic, web и extension прошли. Пункт 1 закрывается только
после первого успешного запуска на фактическом self-hosted runner.

Первый фактический run выявил отсутствие генерируемого TanStack
`routeTree.gen.ts` в чистом checkout; production build переставлен в порядок
Vite route generation -> TypeScript check. Повторный self-hosted run
`29208255226` завершился успешно за 2 мин 5 с. На A100 SHA
`27431f5ae9fcad526af60c58f9d13ba20e6a9675` дал **312 passed**, Ruff и mypy
без ошибок; Alembic показывает `0024 (head)`, внутренний health — `ok`, запрос
без токена — 401, публичные `/healthz` и `/` — HTTP 200. Пункт 1 завершен.

### 4.2. Перевести RLS в fail-closed — завершено

**Plan.** Исключить неявный сервисный принципал, сделать ошибку установки GUC
ошибкой транзакции, отделить API от системного воркера и распространить второй
контур на все пользовательские таблицы. До миграции требовались полная резервная копия,
инвентаризация `NULL`-владельцев и проверка связей document-folder,
chat-document/chat-folder и segment-version.

**Act.** На текущем кодовом SHA `77df129` реализованы следующие изменения:

1. Значение принципала по умолчанию стало анонимным и недоверенным;
   `app.user_id`/`app.is_admin` устанавливаются транзакционно, ошибка не
   проглатывается, а `ContextVar` восстанавливается после ответа по возвращённому token.
2. API запускается только под `rag_api` (`NOSUPERUSER`, `NOBYPASSRLS`, не
   владелец таблиц); ARQ и structured-sidecar требуют отдельную роль
   `rag_worker` (`NOSUPERUSER`, `BYPASSRLS`). Проверка запуска останавливает процесс
   при опасной роли, владении таблицей, отсутствии политики или неактивном RLS.
3. Миграция `0025_rls_fail_closed` включила `ENABLE` + `FORCE ROW LEVEL
   SECURITY`, строгие `USING` + `WITH CHECK` и явные права на полном наборе из
   16 таблиц документов, чата, аудита и памяти. Владелец
   `document_structured_artifacts` исправлен с `rag_api` на `rag`; API для
   `audit_log` оставлен только с `SELECT`/`INSERT`.
4. `documents.owner_sub`, `folders.owner_sub` и `chat_sessions.owner_sub` стали
   `NOT NULL`. Политика `memory_audit_log` требует точного tenant и допускает
   только точного пользователя либо аутентифицированного администратора этого
   tenant; анонимный `NULL` больше не расширяет доступ.
5. SQL-фильтры владельца в маршрутах/RAG сохранены и ужесточены. Миграционный
   предварительный контроль останавливает `0025` при любой несовместимой связи владельцев.
6. До изменения создана полная резервная копия
   `/root/parser_trials/rag_before_rls_0025_20260713_082156.dump` (4 432 532
   байта, права `0600`, SHA-256
   `db98740fab4403ee99dfaa7308d4903e37966769071ac0b86d52ab8f1d94f0bc`,
   `pg_restore --list` читает 174 записи). Пять конфликтных chat-сессий и десять
   сообщений отдельно экспортированы в
   `/root/parser_trials/cross_owner_chats_before_0025_20260713_082156.jsonl`
   (22 947 байт, права `0600`, SHA-256
   `f7742be9b4ee29daa23c48f88cf3c837528fe22500b58dc4385fc35e956421e6`).
7. Все пять сессий принадлежали не администраторам; четыре относились к уже
   отсутствующему пользователю Keycloak, одна — к действующему. Владельцы не
   переназначались: оболочки сессий сохранены, чужой `document_id` отвязан,
   десять потенциально раскрывающих чужой документ сообщений удалены, summary
   очищен. После исправления осталось 5/5 сессий, 5/5 отвязано, 0 сообщений и
   0 summary.

**Verify.** Полный набор дал **346 passed, 1 skipped**; отдельная проверка на
реальном PostgreSQL — **1 passed**. В production применена единственная head
`0025`. SQL-аудит после развертывания подтвердил:

- 16/16 таблиц существуют, принадлежат `rag`, имеют RLS ENABLE+FORCE и политику с
  обоими предикатами; под `rag_api` `row_security_active=true` для 16/16;
- без принципала `rag_api` видит **0** строк во всем защищенном наборе;
- `owner_sub IS NULL`: документы 0/12, папки 0/2, chat-сессии 0/35;
- несовпадающие владельцы: document-folder 0/3, chat-document 0/11,
  chat-folder 0/0;
- права: `rag_api` SELECT/INSERT 16/16 и UPDATE/DELETE 15/16, `rag_worker` все
  четыре DML-права 16/16; исключение API — append-only `audit_log`;
- неполных политик без `USING` или `WITH CHECK`: 0.

Аутентифицированный production HTTP-canary на временных учетных записях также
прошел: без токена и с валидно подписанным токеном чужого `azp` получен 401;
владельцы A и B видят в списке только свой документ, получают 200 на него и 404
на чужой; администратор видит оба документа и получает 200 для обоих. После
теста осталось 0 временных пользователей и клиентов Keycloak, 0 строк БД и 0
объектов MinIO. Внешние `/healthz` и `/` отвечают 200, API без токена — 401;
очереди ARQ пусты, `j_failed=0`, точный GitHub Actions run `29235114750` для
SHA `77df129` завершился успешно. Старый тестовый `rag_api_test` на `:8123`
остановлен; production `rag_api` и `rag_worker` остались рабочими.

**Report.** Пункт 2 закрыт на кодовом SHA `77df129`; окончательный
документационный коммит будет следующим SHA. Подробный эксплуатационный контракт
и безопасные SQL-команды находятся в `deploy/RLS.md`.

### 4.3. Закрыть production-инфраструктуру

Работы:

1. Убрать рабочие значения учетных данных по умолчанию из Compose-конфигурации.
2. Перевести Keycloak с `start-dev` на `start --optimized`, включить TLS и
   федерацию с AD/LDAP по готовому production-scaffold.
3. Хранить секреты в закрытых env-файлах или локальном хранилище секретов с
   ротацией и журналом доступа.
4. Добавить `uv audit` как информационную проверку до выхода из preview; критичные
   уязвимости дополнительно проверять локальным OSV-совместимым сканером.
5. Настроить резервное копирование PostgreSQL с WAL/PITR, версионирование MinIO,
   резервирование конфигураций Keycloak и ежемесячную проверку восстановления.
6. Составить реестр лицензий кода и весов. Для MinerU 3.3.1/3.4 проверить
   собственную MinerU Open Source License, коммерческие пороги и обязательную
   заметную атрибуцию онлайн-сервиса; согласовать выполнение с юристом и добавить
   требуемое упоминание в интерфейс или публичную документацию до обновления.

Критерий приемки: документально подтвержденное восстановление приложения и
одного документа на отдельном тестовом контуре в пределах целевых RPO/RTO.

## 5. Этап P1: измеримое качество

### 5.1. Создать эталонный набор

Набор должен содержать реальные или согласованные с заказчиком аналоги:

- 200-500 вопросов RAG;
- не менее 20% вопросов без ответа в выбранной области;
- single-hop, multi-hop, сравнение нескольких документов и разделов;
- таблицы, формулы, рисунки, схемы, P&ID, печати и плохие сканы;
- номера ГОСТ/ISO/API/ASTM, единицы измерения и артикулы;
- RU/EN/ZH, опечатки, сокращения и смешанные языки;
- сложные отрицательные примеры и похожие, но неверные разделы;
- косвенные prompt-injection и попытки межпользовательского доступа.

Метрики поиска:

- `Recall@1`, `Recall@5`, `Recall@10`;
- `nDCG@10`, `MRR@10`;
- полнота отдельно по типам документов, языкам и видам контента.

Метрики ответа:

- точность и полнота цитат;
- доля утверждений, подтвержденных указанными источниками;
- правильность чисел, единиц, стандартов и таблиц;
- точность маршрута `doc_only`/`multi-hop`/`memory`/`out_of_scope`;
- p50/p95 задержки, число шагов агента и доля остановок по лимиту.

### 5.2. Ввести выпускной шлюз моделей

Кандидат принимается только если:

- не ухудшает критические метрики и безопасность;
- дает статистически и практически значимое улучшение целевой метрики;
- проходит проверку на длинных контекстах и параллельной нагрузке;
- имеет разрешительную лицензию для коммерческого on-prem использования;
- поддерживает быстрый откат на текущую модель;
- результаты, конфигурация и версия весов записаны в `docs/roadmap.md`.

## 6. Этап P1: парсинг и структура документа

### 6.1. MinerU 3.4

Официальный MinerU 3.4 выпущен 18 июня 2026 года. Заявлены PP-OCRv6,
повышение точности OCR pipeline-бэкенда примерно на 11% и ускорение обработки
примерно на 100%. Источник: <https://github.com/opendatalab/mineru>.

Лицензионный шлюз обязателен до A/B и выпуска: код MinerU теперь распространяется
не под чистой Apache-2.0, а под MinerU Open Source License на основе Apache-2.0.
Она разрешает коммерческое использование ниже порогов 100 млн MAU и 20 млн USD
месячной выручки, но требует явной атрибуции при предоставлении онлайн-сервиса.
Веса MinerU2.5-Pro отдельно помечены Apache-2.0. Источник:
<https://github.com/opendatalab/MinerU/blob/master/LICENSE.md>.

План A/B:

1. Зафиксировать текущий образ MinerU 3.3.1 и набор 100-200 страниц.
2. Поднять MinerU 3.4 изолированно без изменения production-сервиса.
3. Проверить совместимость с используемым vLLM, патчами и форматом content-list.
4. Сравнить порядок чтения, таблицы, формулы, bbox, скорость и downstream RAG.
5. Переключать production только после победы по совокупному критерию.
6. Не переразбирать пользовательские документы автоматически; сначала проверить
   копии артефактов, затем предложить управляемый переразбор с сохранением старой версии.

#### 14.07.2026: выполнение пункта 6/20 — ЗАВЕРШЕН

**Plan.** Сравнить загруженные в память production-процесса MinerU `3.3.1` и
изолированный MinerU `3.4.4` на одном checkpoint модели, одном vLLM и одном
проверенном сложном корпусе. Единственной переменной должна оставаться версия
MinerU. Выпускное решение должно учитывать сырой и итоговый результат, порядок
чтения, bbox, таблицы, графики, повторяющиеся символы, задержку и downstream RAG.
До решения пользовательские документы не переразбираются, порт `:30010` и
пользовательский трафик не переключаются.

**Act.** Зафиксирован официальный выпуск `mineru-3.4.4-released` от
10.07.2026, commit тега
`0dfc9460cd9ab693b9af60ae3fbffd7bc111b062`. В нем отдельно исправлены анализ
использования Latin/CJK-шрифтов и обнаружение дублирующихся символов со
смещением (`#5238`). На A100 подтверждено важное расхождение runtime: в
дисковом окружении уже установлен MinerU `3.4.4`, но production-процесс
`:30010`, запущенный 17.06.2026, продолжает выполнять загруженный код `3.3.1`.
Он не перезапускался и остается baseline. Кандидат `3.4.4` поднят отдельно на
`127.0.0.1:30011` с vLLM `0.21.0` и тем же
`opendatalab/MinerU2.5-Pro-2605-1.2B`; snapshot модели закреплен SHA
`bff20d4ae2bf202df9f45284b4d43681555a97ed`, manifest весов — SHA-256
`c42b02eb2ec40d446e391e7b8050e634357b9ee2b7b888ace36b3f8bf0e57738`.
Визуальные embedding- и reranker-сервисы останавливались только на время A/B на
GPU2 и после прогона восстановлены.

Скрипт `scripts/build_mineru_ab_corpus.py` сформировал 147 одностраничных PDF с
проверкой исходных SHA-256 и атомарной content-addressed записью: 66 layout,
59 table, 14 chart и 8 text-formatting страниц. В набор входят 138 лицензированных
страниц открытых датасетов и 9 предельно сложных страниц ParseBench. Ревизия
источников — `32b7cf008476684456bd76eb4ae07b11a4c2de70085256bb71ce87d9c8e05f0f`,
SHA-256 manifest —
`b35527eef2a6b2f7f68608ae14ca94c6240b2bd06394905581f3d2b8ebd4b830`.

Benchmark переведен на схему 2: отдельно фиксируются сырой и итоговый текст,
их хеши, bbox, порядок чтения, доля соседних повторов, backfill, таблицы,
графики, p95 и полная runtime-provenance. Парный компаратор работает
fail-closed: требует разные версии MinerU, одинаковые vLLM/model snapshot,
разные идентификаторы серверов и проверяемые evidence-файлы; несовместимые или
неполные отчеты не могут дать решение `accepted`.

Закрытый Gold-набор материализован из release/sidecar и production-оригиналов
без публикации UUID, владельцев, ключей MinIO и имен файлов. По magic bytes и
декодированию подтверждены 7 PDF/61 страница; три OOXML не передаются PDF-парсеру,
а входят в оба downstream-индекса как один и тот же контроль: 83 страницы,
117 production-chunks. Исходники сверены по SHA-256, chunks — по
`parsed_content_sha256`; у одного OOXML отсутствовал `page_count` в БД, поэтому
итоговые 83 страницы берутся из проверенного Gold (ненулевое расхождение БД с
Gold по-прежнему блокируется). Каталог имеет режим `0700`, файлы — `0600`.
`source_revision` —
`dd2dd55dac218c1e4fea219d6a898f1faa1b7b892abbef0f840d87567d57882d`,
SHA-256 manifest —
`9ea916a1097ba9793c204087f72aeb5048d327e8fab4e81a0d714b1387aee49a`,
SHA-256 OOXML controls —
`24fe58bc5d1cd25d5d539ab4820a1aab3f2548228be854ff8795a7a6c941d374`.

**Verify.** Полный открытый прогон завершен `147/147` для каждой версии без
ошибок. После пересчета доказательств итоговые отчеты имеют SHA-256
`85b3ea7d1ce4db18460ce49ec5e8f8ef03617fb4a1b1d69b26b43868600d9ea8`
для 3.3.1 и
`ab5b609ae388c99ffb79cd4e7d8030b414c83213f31c6f13af2775ad6b392eb7`
для 3.4.4; решение компаратора сохранено в
`/root/parser_trials/mineru_ab/comparison-requalified.json`. Регрессий не
найдено: итоговый quality proxy вырос `0,868099 -> 0,977030`, bbox
`0,874003 -> 0,994398`, обнаружение таблиц на `0,033898`, сохранение графиков
`0,571429 -> 0,857143`; среднее время снизилось `18,242 -> 16,570` с, p95
`29,422 -> 20,103` с. При этом парный sign-test задержки статистически незначим
(`p=0,7416`), а строгий компаратор вернул `inconclusive`, потому что
одностраничный открытый корпус не дает достаточного независимого доказательства
порядка чтения. Fail-closed правило не позволяет считать это выпускной победой.

Закрытый Gold-прогон также завершен без ошибок: `7/7` PDF для каждой версии,
61 PDF-страница плюс одинаковый OOXML-контроль из 83 страниц/117 chunks.
Пересчитанные SHA-256 отчетов: 3.3.1
`3f6baef5c8321bddcd7f6583a9307c27da02f525fad8fb02a01c25a7ec4bda3f`,
3.4.4
`7d7a3413f10da7b23a5bbb9c10a1abfea199635c13a71a397431bf348d52d5f3`.
Из 236 Gold-кейсов честно проверяется 231: 169 answerable и 62 no-answer с
retrieval-probe; пять no-answer без поискового эталона не подменялись
синтетическими ожиданиями. Source-only shadow retrieval принят, artifact
SHA-256
`365bb3989e2cb70e9a0d590c1bc481b993ab9cffc2cc5dc70c1c9f7f2f267096`:
для answerable Recall@5/10 остался `0,828402/0,855030`, nDCG@10 изменился
`0,841193 -> 0,841310`; для no-answer probes Recall@5 вырос
`0,512097 -> 0,520161`, Recall@10 `0,689516 -> 0,691532`, nDCG@10
`0,714908 -> 0,718203`. Это изолированный тест влияния parser-output на поиск
по исходным фрагментам, а не полный production RAG: он не проверяет генерацию
ответа, сохранность чисел и качество конечных цитат.

Полный локальный набор дал **601 passed, 1 skipped**; профильные Ruff, mypy,
web lint/build, Bash syntax и `git diff --check` прошли. На A100 проверены
приватные режимы `0700/0600`,
связность Gold, runtime provenance и функциональный визуальный эмбеддинг:
4096 конечных компонент, L2-норма `1,0`. Диагностический smoke на прежних девяти
сложных страницах не являлся выпускным шлюзом, но подтвердил необходимость
полного прогона:

| Режим `table_enable=false` | MinerU 3.3.1 | MinerU 3.4.4 |
|---|---:|---:|
| Успешные страницы | 9/9 | 9/9 |
| Среднее время, с | 26,007 | 22,501 |
| Медиана, с | 22,912 | 20,878 |
| Итоговый quality proxy | 0,9852 | 0,9840 |
| Таблицы / изображения | 3 / 7 | 0 / 8 |

Кандидат в этом smoke быстрее примерно на 13,5%, но потерял table-объекты и
визуальный регион графика. Повтор с `table_enable=true` также не восстановил
таблицы; поэтому скорость не может служить основанием для переключения.

**Report.** Пункт **завершен**, MinerU 3.4.4 **выпущен в production**. Решение
основано на существенном улучшении quality/bbox/графиков и задержки, отсутствии
регрессий в source-only shadow retrieval и дополнительной проверке 61 страницы:
у обеих версий нет переходов назад между номерами страниц. Последняя проверка
не является эталоном естественного порядка блоков внутри страницы; строгий
компаратор поэтому остается `inconclusive`, а отдельная Kendall-tau/order-edit
проверка по структурному эталону OOXML переносится в следующий этап оценки
парсинга. Порог компаратора не ослаблялся и синтетические строки не добавлялись.

Production атомарно переключен на `/root/services/mineru/runtimes/3.4.4` через
симлинк `current` и systemd-unit с health-check и автоматическим откатом на
3.3.1. После перезапуска MinerU прошел сложный PDF smoke через `:30010`, API и
ARQ worker доступны, очередь была пустой, внешний `/healthz` отвечает.
Пользовательские документы не переразбирались.

Лицензия кода MinerU — MinerU Open Source License на основе Apache-2.0 с
порогами и обязательной явной атрибуцией онлайн-сервиса; веса checkpoint
обозначены Apache-2.0. Постоянная ссылка-атрибуция MinerU развернута в footer и
проверена Playwright на desktop/mobile без горизонтального переполнения.
Визуальные сервисы восстановлены, 10/10 реальных учетных записей проходят вход,
redirect на `/`, HTTP-smoke и проверку RLS-изоляции. Локальная и серверная копии
синхронизируются этим коммитом; runtime 3.3.1 сохранен как проверенный rollback.

### 6.2. MinerU-Popo

MinerU-Popo имеет MIT-лицензию и восстанавливает сквозную структуру документа
после постраничного OCR: разорванные абзацы и таблицы, уровни заголовков и связи
рисунок-текст. Источник: <https://github.com/opendatalab/MinerU-Popo>.

Интеграция:

1. Нормализовать выходы MinerU, PaddleOCR-VL и dots.mocr во входную схему Popo.
2. Хранить дерево документа и устойчивые идентификаторы узлов.
3. Строить чанки по дереву, а не только по линейной последовательности сегментов.
4. Сохранять исходные bbox и страницы для цитат и навигации во вьювере.
5. Развернуть 4B-модель пакетно на GPU5 только после замера совместного VRAM.

Критерий приемки: улучшение структурной метрики и downstream `Recall@5`/точности
ответов без ухудшения числа ошибок парсинга и времени обработки выше SLA.

#### 14.07.2026: выполнение пункта 7/20

**Plan.** MinerU-Popo проверялся только как локальный постпроцессор результата
MinerU, а не как новый OCR. Были зафиксированы точные ревизии кода
`75c36a8c0f38adee03c78850366645a58cd5d4af` и модели
`af75342abdc5cbd461264521c9ef46719ff74190`. Код проекта опубликован под MIT,
базовая модель Qwen3-VL-4B — под Apache-2.0. Испытание выполнялось без внешних
API и без передачи документов за периметр. Основания и ограничения сверялись с
репозиторием, карточкой модели и статьей:
<https://github.com/opendatalab/MinerU-Popo>,
<https://huggingface.co/DreamEternal/MinerU-Popo>,
<https://arxiv.org/abs/2605.24973>.

**Act.** В WSL добавлен независимый от runtime-модели `DocumentTreeArtifact v1`:
стабильные серверные идентификаторы, точная SHA-256-привязка к ревизии
сегментов, bbox/страницы, строгая иерархия, продолжения текста и таблиц, связи
подписей и изображений. Popo-адаптер использует канонические `Segment` как
единственный источник текста и геометрии и требует покрытия 1:1. Штатный
`get_json_tree.py` не используется. Tree-aware чанкер сохраняет действующий
контракт `ChunkDraft`, объединяет проверенные цепочки многостраничных таблиц и
в режимах `shadow`/`active` всегда откатывается к плоскому чанкингу при
невалидном sidecar. Production-флаг `document_tree_mode` по умолчанию равен
`off`; существующий RAG и пользовательские документы не менялись.

Отдельный content-free шлюз `popo_gate` проверяет полноту блоков, граф
заголовков, порядок, межстраничные связи, `Recall@5`, `nDCG@10`, цитаты,
RU/EN/ZH-срезы, p95 и VRAM. Артефакты и отчеты создаются атомарно с правами
`0600`. Недостаточная разметка не превращается в нулевую метрику: шлюз выдает
`ineligible` и запрещает выпуск.

**Verify.** Изолированный vLLM 0.22.1/BF16 работал только на loopback GPU5
вместе с production MinerU 3.4.4. На 7 сложных документах трех языков,
61 странице и 934 блоках получены 37/37 успешных вызовов за 134 с: p50
1,671 с, p95 11,707 с, максимум 16,391 с. Popo сохранил source mapping
934/934, корректно сослался на 25 продолжений текста, 56 связей изображений и
3 связи частей таблиц; уровни определены для 153 из 167 заголовков (91,6%).
Пиковое потребление GPU5 вместе с production составило 26 859 MiB, минимум
свободной памяти — 13 583 MiB; health-check не падал.

Испытание выявило два блокирующих upstream-дефекта. `adaptive_chunk` пропускал
короткие наборы заголовков, пока исправление не было применено только во
временном trial-каталоге. Затем штатный `get_json_tree.py` сохранил лишь
931/934 блока: потерял часть цепочки таблицы и документ из таблицы с подписью
без заголовка. Безопасный адаптер проекта сохраняет 27/27 блоков на
регрессионном примере и не повторяет этот дефект. Полный локальный прогон:
`649 passed, 1 skipped`; `mypy` — 89 файлов без ошибок; измененные файлы чисты
по Ruff и `git diff --check`. Приватный агрегированный отчет сохранен на A100 в
`/root/parser_trials/mineru_popo_p7_20260714/metrics.json` с SHA-256
`0eea8c74f2e35b388a39aa528cc35395f62edf05a2d5670590f6ee5e5a898dcd`.

**Report.** Пункт завершен решением **не выпускать MinerU-Popo в production**.
Техническая совместимость с одной A100 доказана, но превосходство над MinerU
3.4.4 для RAG не доказано: текущий Gold не содержит достаточного числа
эталонных parent/order/relation-аннотаций и downstream-кейсов. Публичные
результаты статьи не заменяют локальную проверку RU/EN/ZH. Поэтому Popo,
`DocumentTree` и tree-aware чанкинг остаются выключенными; действующий parser и
RAG не изменены. Повторный допуск возможен только после разметки structural
Gold, прохождения content-free шлюза без потери блоков и доказанного улучшения
downstream-метрик.

### 6.3. Qianfan-OCR только как KIE-компаратор

Qianfan-OCR 4B имеет Apache-2.0, поддерживает прямой image-to-Markdown,
layout, таблицы, диаграммы, извлечение полей и запуск на одной A100.
Источник: <https://huggingface.co/baidu/Qianfan-OCR>.

На актуальном официальном leaderboard OmniDocBench v1.7 модель получает 93,90
на наборе/метрике v1.6, тогда MinerU2.5-Pro получает 95,75. Собственная карточка
Qianfan приводит 93,12 на старом v1.5; это число нельзя напрямую ранжировать
вместе с v1.6. Источник: <https://github.com/opendatalab/OmniDocBench>.

Поэтому Qianfan не следует добавлять как четвертый универсальный parser fallback.
ParseBench дополнительно показывает только 0,90 по извлечению данных графиков и
20,60 по визуальной привязке. Его допустимая роль сужена до KIE-компаратора для
форм, счетов и извлечения полей в JSON. Результат sidecar хранится отдельно от
канонических сегментов и не заменяет MinerU/Paddle. Для chart-to-data Qianfan не
допускается без нового локального результата, который опровергнет публичную
регрессию.

Первым кандидатом для рабочего KIE-контура остается Apache-2.0 модель
`ibm-granite/granite-vision-4.1-4b`: в прямом A/B она была быстрее, дала 150/150
валидных ответов и обработала конкретную патологически длинную таблицу.
`Qwen3-VL-8B-Instruct` принят как
претендент для вложенных форм: у него выше нормализованный EM и больше полностью верных
документов, но один ответ оборвался по лимиту и общего статистически доказанного
превосходства нет. Qianfan остается только историческим компаратором.
Источники: <https://huggingface.co/ibm-granite/granite-vision-4.1-4b>,
<https://github.com/udibarzi/varex-bench>.

### 6.4. Актуальный список парсеров на 13.07.2026

| Кандидат | Лицензия | Сопоставимый публичный результат | Решение для DocRAGenslate |
|---|---|---|---|
| PaddleOCR-VL-1.6, 0,9B | Apache-2.0 | 96,33, OmniDocBench v1.6; 93,19, Real5 | Уже развернут на A100; главный альтернативный parser |
| MinerU2.5-Pro, 1,2B | веса Apache-2.0; код MinerU под собственной лицензией | 95,75 в официальном leaderboard v1.7 | Оставить основным; проверить атрибуцию и условия кода |
| Infinity-Parser2-Flash, 2,2B | metadata Apache-2.0 | 73,25 ParseBench; 91,98 OmniDocBench v1.6 от авторов | Локально не прошел SLA; не допускать без нового runtime/кванта |
| Infinity-Parser2-Pro, 35B/3B active | metadata Apache-2.0 | 74,28 ParseBench; 93,95 OmniDocBench v1.6 от авторов | Не планировать на текущем боксе: BF16 требует двух A100 и TP без NVLink |
| NuExtract3, 4,5B | Apache-2.0 | локально 73,4%, 127/150 valid, p95 71,228 с | Отклонен: слабее, только 127/150 valid и неприемлемый p95 |
| FireRed-OCR-2B | Apache-2.0 | 94,14 OmniDocBench в актуальной таблице; 92,94 в отчете v1.5 | Не приоритетен: публично слабее действующего parser-контура |
| LightOnOCR-2-1B | Apache-2.0 | 83,2 +/- 0,9 olmOCR | A100: быстрый на vLLM 0.25, но лишь 2/5 самых сложных страниц завершились даже при 8192 токенах; не допускать в fallback |
| GLM-OCR, 0,9B | MIT | 95,22 OmniDocBench; 29,60 ParseBench | Исключить по строгому Apache-only; не приоритетен для agent/RAG |
| Unlimited-OCR, 3B/0,5B active | MIT | 93,92, OmniDocBench v1.6 | Исключить из production при Apache-only; research multi-page |
| Granite Vision 4.1, 4B | Apache-2.0 | локально 93,97% нормализованного EM, 150/150 валидных ответов, медиана 6,761 с | Первый кандидат отдельного KIE-контура; не канонический парсер |
| Qwen3-VL-8B-Instruct | Apache-2.0 | локально 94,79% нормализованного EM, 149/150 валидных ответов, медиана 8,485 с | Претендент для вложенных форм; только после доменной проверки RU/EN/ZH |
| Qianfan-OCR, 4B | Apache-2.0 | 93,90 Omni v1.6; 46,20 ParseBench; 0,90 charts | Исторический компаратор; исключить из parser/chart fallback и новых A/B |
| dots.ocr/dots.mocr, 3B | MIT | 90,77, OmniDocBench v1.6 | Действующее исключение для diagram/SVG требует фиксации в реестре лицензий |
| DeepSeek-OCR-2, 3B | требует отдельной юридической проверки | 90,25, OmniDocBench v1.6 | Не допускать: слабее действующих моделей и лицензия не принята |
| HunyuanOCR-1.5 | Hunyuan Community License | ниже действующих лидеров | Исключить по лицензионному правилу проекта |

PaddleOCR-VL-1.6 пока отсутствует в независимой таблице официального leaderboard:
96,33 опубликованы авторами модели на том же протоколе v1.6. Это сильный сигнал,
но не замена локальному воспроизведению. Main-ветка OmniDocBench с 30 апреля
2026 года называется v1.7; обновление добавило Qianfan в таблицу, а базовый набор
и метрика для приведенных результатов остаются v1.6.

На A100 подтвержден действующий `paddle-genai.service`: модель
`PaddleOCR-VL-1.6-0.9B`, отдельный vLLM 0.10.2, `:8118`, Apache-2.0,
`max_model_len=16384`. Актуальная документация PaddleOCR, полученная через
Context7, предписывает тот же локальный запуск с backend vLLM и поддерживает
структурированный JSON/Markdown, восстановление уровней заголовков и слияние
межстраничных таблиц. Поэтому сначала следует измерить и исправить использование
уже работающего Paddle 1.6 на сканах, а не занимать GPU еще одной общей моделью.

Unlimited-OCR выпущен 22 июня 2026 года под MIT и использует Reference Sliding
Window Attention для one-shot обработки десятков страниц при постоянном KV-cache.
Его 93,92 на v1.6 не делает его лучшим постраничным парсером; ценность необходимо
проверять отдельно на продолжениях таблиц, заголовках и связности длинного
документа. Источники: <https://github.com/baidu/Unlimited-OCR>,
<https://arxiv.org/html/2606.23050>.

Обязательное ограничение публичных результатов: OmniDocBench оценивает главным
образом английский, упрощенный китайский и смешанные EN/ZH страницы; сам проект
указывает, что текстовая оценка пока включает только китайский и английский.
Русское качество этими числами не подтверждено.

### 6.5. Проверка ParseBench

ParseBench оценивает не сходство Markdown с эталоном, а пять свойств, более
близких к agent/RAG: таблицы, извлечение точек графика, смысловую точность текста,
семантическое форматирование и визуальную привязку. Набор содержит 2078 уникальных
страниц и 169011 проверочных правил из страхования, финансов, государственных и
других корпоративных документов. `Overall` — простое невзвешенное среднее пяти
измерений. Источники: <https://www.parsebench.ai/#leaderboard>,
<https://github.com/run-llama/ParseBench>,
<https://arxiv.org/html/2604.08538>,
<https://huggingface.co/datasets/llamaindex/ParseBench>.

Актуальный полный CSV на 11.07.2026 дает следующую картину open-weight моделей:

| Модель | Overall | Tables | Charts | Content | Semantic | Visual | Вывод |
|---|---:|---:|---:|---:|---:|---:|---|
| KDL-Frontier-Parser-nano | 76,36 | 85,56 | 63,41 | 87,19 | 66,81 | 78,84 | Лучший компактный, но AGPL-3.0 — исключить |
| Infinity-Parser2-Pro | 74,28 | 86,40 | 61,30 | 89,70 | 59,10 | 74,90 | Сильный, но 35B/3B active и рекомендуемый TP=2 |
| Infinity-Parser2-Flash | 73,25 | 82,88 | 55,56 | 89,52 | 57,70 | 80,61 | Лучший практически размещаемый новый кандидат |
| MinerU2.5-Pro-2605 | 72,78 | 77,59 | 61,64 | 87,88 | 57,49 | 79,30 | Текущий production baseline |
| PaddleOCR-VL-1.6 | 67,43 | 67,77 | 54,24 | 82,71 | 54,64 | 77,80 | Сильный OmniDoc OCR, но не лидер agent-oriented parsing |
| Qianfan-OCR | 46,20 | 72,30 | 0,90 | 83,50 | 53,80 | 20,60 | Не использовать как общий или chart-extraction parser |
| Unlimited-OCR | 46,17 | 70,21 | 1,34 | 86,81 | 0,97 | 71,52 | Только отдельная проверка multi-page continuity |

ParseBench меняет первоначальный приоритет: первая новая модель для A/B —
Infinity-Parser2-Flash, а не Qianfan и не GLM-OCR. Ее веса содержат 2,213 млрд
BF16-параметров и помещаются на одну A100-40GB. Карточка и Hugging Face metadata
указывают Apache-2.0, vLLM/Transformers и EN/ZH/multilingual. Однако в репозитории
весов не найден отдельный файл `LICENSE`; до переноса весов на A100 необходимо
получить текст лицензии или письменное подтверждение, а не полагаться только на
metadata. Источник: <https://huggingface.co/infly/Infinity-Parser2-Flash>.

Infinity-Parser2-Pro имеет 35,1 млрд BF16-параметров, около 70 ГБ только весов;
официальный рецепт использует две GPU, vLLM и контекст 65536. В сервере без
NVLink и со всеми занятыми A100 он не является первым практическим кандидатом.
KDL-Frontier-Parser-nano компактен (1,16B), но его AGPL-3.0 не соответствует
принятой политике production. Источник:
<https://huggingface.co/KDLAI/KDL-Frontier-Parser-nano>.

Ограничения рейтинга:

- результаты не являются downstream RAG QA: нет `Recall@k`, точности ответа и
  цитат, поэтому итог все равно проверяется фиксированным RAG-контуром;
- простой `Overall` скрывает разные профили: Flash выигрывает у MinerU всего
  0,47 пункта, лучше по таблицам, но хуже на 6,08 пункта по графикам;
- публичный набор не подтверждает русский язык, нефтегазовые документы, P&ID,
  договорные исправления и строительные штампы;
- KDL сообщает end-to-end pipeline с детерминированным постпроцессингом, то есть
  сравнивается весь результат, а не только сырые веса;
- Qianfan может остаться только KIE-компаратором с отдельным промптом; его 0,90
  на извлечении данных графика запрещает называть его сильным chart parser.

#### Фактический smoke KDL на A100

По прямому решению владельца проекта KDL допущен к закрытому тесту несмотря на
AGPL-3.0; это разрешение относится к скачиванию и оценке, а не к production.
Production-включение потребует отдельного выполнения сетевых условий AGPL и
предоставления соответствующего исходного кода.

Проверка выполнена 11.07.2026 без подключения к пользовательскому API:

1. Скачаны публичные веса `KDLAI/KDL-Frontier-Parser-nano`, фактический размер
   checkpoint 2,15 ГиБ. Модель поднята на GPU5, `127.0.0.1:8130`, с
   `gpu_memory_utilization=0.25`, `max_num_seqs=4`; production MinerU не
   останавливался.
2. Официальный ParseBench перенесен из WSL на A100 и закреплен на commit
   `cfbb8a3aaa05f0ffd8ca88c76196281be7264c03`. Использован официальный
   двухступенчатый provider: layout страницы, crop регионов, распознавание по
   типам и детерминированный постпроцессинг.
3. Smoke-страница — первая английская страница 20-страничного EPC/Turnkey
   procurement document. Это исключает объяснение ошибки неподдерживаемым RU.
4. На vLLM 0.21.0 checkpoint загрузился за 2,16 ГиБ и endpoint стал healthy,
   но layout output был поврежден и не содержал валидной пары служебных токенов.
   После обязательного по model card `skip_special_tokens=false` ответ начинался
   с `<|box_start|>`, но оставался бессвязным и не разбирался.
5. На изолированном OCR-runtime vLLM 0.10.2 после явной передачи официальных
   `min_pixels/max_pixels` модель также не дала layout: ответ состоял из
   повторяющихся `!`. Latency страницы составила 31,645 с.
6. ParseBench CLI в обоих случаях сообщил `Successful: 1, Failed: 0`, хотя
   normalized output содержал ноль страниц и пустой Markdown. Это важный дефект
   приемочного контура: технический HTTP 200 нельзя считать успешным parse.

Решение: KDL **не допускать** к полному A/B и production на текущем runtime.
Повторять тест только после получения от разработчика точного container digest,
версий vLLM/Transformers и контрольного входа с ожидаемым hash/output. Журналы
и конфигурация запуска оставлены на A100 для воспроизводимости; временный сервис
остановлен, а checkpoint позднее удален при контролируемой очистке диска. GPU5
вернулся к исходным 12927 МиБ, публичный `/healthz` после теста — `ok`.
Добавленный `ParseQualityReport` правильно квалифицировал бы такой результат как
`empty_output`; это подтверждает необходимость собственного quality gate поверх
сторонних harness.

#### Фактический smoke Infinity-Parser2-Flash на A100

Проверка выполнена 11.07.2026 на той же первой странице EPC/Turnkey document,
без подключения к пользовательскому API и без остановки production MinerU.
Временный endpoint занимал GPU5 совместно с MinerU; максимальное наблюдаемое
потребление составило 24489 из 40960 МиБ.

1. Скачан публичный checkpoint `infly/Infinity-Parser2-Flash`: 2,213 млрд BF16-
   параметров, около 4,25 ГиБ VRAM после загрузки. Hugging Face metadata указывает
   Apache-2.0, но отдельного файла `LICENSE` в репозитории модели нет; до
   production нужен самостоятельный текст лицензии или подтверждение владельца.
2. Актуальная карточка модели повторно извлечена через Tavily. Официальный рецепт
   требует `vLLM==0.17.1`, `reasoning-parser=qwen3`, prefix cache, PyTorch 2.10 и
   контекст 65536 для Pro. Flash запущен с теми же runtime-флагами, но с безопасным
   окном 16384, `max_num_seqs=2` и `gpu_memory_utilization=0.30`.
3. Текущий checkpoint требует `tokenizer_class=TokenizersBackend` и
   `processor_class=Qwen3VLProcessor`, которых нет в разрешенном самим
   `vLLM 0.17.1` диапазоне `Transformers<5`. С принудительным
   `Transformers 5.3.0` endpoint поднялся, но предупреждал о нераспознанных
   `mrope_section/mrope_interleaved`.
4. Официальный SDK `infinity-parser2==0.3.0` имеет три проблемы упаковки:
   безусловно импортирует локальный `vllm` даже для HTTP-клиента, не объявляет
   обязательный `torchvision`, а direct backend требует не объявленный
   `accelerate`. Его стартовый текстовый `ping` к мультимодальной модели имеет
   жесткий таймаут 5 с и ложно объявляет healthy endpoint недоступным.
5. Через рекомендуемый vLLM-контур страница генерировалась 118,127 с и вернула
   только повторяющийся символ `!`. ParseBench снова сообщил
   `Successful: 1, Failed: 0`, хотя normalized Markdown был пуст, layout не
   содержал элементов. Следовательно, публичный harness нельзя использовать как
   единственный выпускной шлюз.
6. Контрольный запуск того же checkpoint через официальный
   `transformers`-backend был успешен: загрузка 3,541 с, генерация 10,80 с,
   полное время 14,357 с. Получен валидный JSON из 7 элементов с bbox и точным
   текстом: `STANDARD PROCUREMENT DOCUMENT`, заголовок RFP, EPC/Turnkey,
   World Bank и дата `July 2020`. GPU5 после завершения вернулся к исходным
   12927 МиБ.

Решение: checkpoint оставить **кандидатом на доменный A/B**, но не допускать в
production через текущий vLLM-контур. Следующий повтор возможен после согласования
совместимого набора `vLLM/Transformers`, устранения `mrope`-предупреждений и
проверки лицензии. Для воспроизводимого A/B допустим отдельный transformers-
worker, но сначала необходимо сравнить 150-250 страниц и downstream RAG с уже
развернутыми MinerU2.5-Pro и PaddleOCR-VL-1.6. ParseBench-окружение, сырой
результат и журналы оставлены в `/root/parser_trials/`. Checkpoint,
дублирующий SDK-cache и тяжелое direct-окружение удалены после сохранения точных
версий; `requirements.freeze.txt` позволяет их восстановить. После удаления
отклоненных KDL/Infinity и завершенных Qianfan/Granite trial-checkpoints на
корневом разделе свободно 33 ГиБ (94% занято).

### 6.6. Допуск parser fallback к пилоту

Обязательное правило проекта: простые синтетические страницы разрешены только
для unit/regression-проверок кода и не являются доказательством качества модели.
Любое решение об A/B, смене parser, fallback или production-допуске принимается
только на очень сложных многостраничных документах с неоднородной структурой.

1. Собрать 150-250 доменных страниц: EN/RU/ZH, формы, счета, таблицы с
   объединениями и продолжениями между страницами, формулы, схемы, P&ID, печати,
   рукописные пометки, плохие и повернутые сканы, смешанные текстовые слои и OCR.
2. Отдельно оценивать общий Markdown/layout, таблицы, числа и единицы, KIE,
   chart/diagram QA, межстраничную структуру и полноту страниц.
3. Для downstream RAG измерять `Recall@5`, `nDCG@10`, точность чисел и цитат на
   одном и том же индексе/генераторе; parser является единственной переменной.
4. Практический выигрыш: не менее 2 процентных пунктов в целевой подгруппе,
   положительная нижняя граница 95% bootstrap-интервала и отсутствие критической
   регрессии общей метрики, RU-страниц, p95, VRAM и сохранности bbox.
5. Сначала включить shadow-оценку текущего результата. Затем document-level A/B
   `MinerU2.5-Pro ↔ PaddleOCR-VL-1.6 ↔ Infinity-Parser2-Flash`; fallback
   только для `parser_backend=NULL`, за выключенным по умолчанию флагом и
   allowlist 1-2 аккаунтов. Явно выбранный пользователем backend не заменять.
6. Page-level routing вводить отдельным этапом после стабильного document-level:
   требуется безопасное разбиение PDF, восстановление исходных `page_idx`, bbox,
   путей изображений и атомарное слияние результатов.

### 6.7. Первый сложный A/B MinerU и PaddleOCR-VL

**11.07.2026.** Для раннего выявления системных дефектов собран воспроизводимый
открытый диагностический набор из ParseBench под Apache-2.0. В него вошли девять
реальных страниц, отобранных по сложности, а не случайным образом:

- три таблицы: 270 ячеек/80 объединений, 1821 ячейка и 1845 ячеек/615 строк;
- две страницы с графиками, включая трехмерный график;
- две страницы со сложной многоколоночной версткой, до 198 правил проверки;
- восьмиколоночная OCR-газета и страница плотных юридических примечаний.

Скрипт `scripts/download_parsebench_complex_subset.py` скачивает точные исходники,
проверяет сигнатуру PDF и записывает URL, размер и SHA-256 в manifest. Скрипт
`scripts/benchmark_complex_parsers.py` последовательно запускает одинаковые PDF
через production-адаптеры `mineru` и `paddle_vl`, сохраняет сырые результаты по
backend и обновляет JSON после каждой страницы. В summary нет текста документов.
Пользовательские документы, API и очередь ARQ в тесте не участвовали.

Результат A100, 9 из 9 страниц успешно обработаны каждым backend:

| Метрика | MinerU2.5-Pro | PaddleOCR-VL-1.6 |
|---|---:|---:|
| Среднее время страницы, с | 27,121 | 14,607 |
| Медиана времени страницы, с | 28,644 | 14,995 |
| Извлечено символов | 117954 | 99909 |
| Сегментов | 111 | 766 |
| Изображений | 0 | 11 |
| Таблиц на трех table-страницах | 0 | 3 |
| Ячеек на трех table-страницах | 0 | 1082 |
| Страниц без текста | 0 | 1 |
| Средний текущий quality score | 0,9980 | 0,9829 |

Интерпретация ограничена структурными прокси: количество ячеек не доказывает их
правильность, а сохраненное изображение графика не означает извлечение его точек.
Тем не менее прогон выявил четыре подтвержденные проблемы:

1. Production MinerU работает с выключенным табличным режимом и не создал ни
   одной таблицы на трех специально отобранных table-страницах. Он сохранил текст,
   но потерял структуру, необходимую табличному RAG.
2. Paddle восстановил таблицы на двух страницах, но на самой плотной таблице
   вернул только изображение без текста. Поэтому глобальная замена MinerU на
   Paddle также недопустима.
3. На обеих страницах с графиками Paddle сохранил визуальные регионы, MinerU —
   нет; ни один результат не доказал извлечение числовых рядов графика.
4. Текущий `ParseQualityReport` принял все 18 результатов и дал MinerU более
   высокий средний балл, хотя тот потерял все table-объекты. Значит, полнота
   страниц и отсутствие повторов необходимы, но недостаточны без ожидаемого типа
   страницы и проверки структуры.

Дополнительная проверка сырых артефактов уточнила причину: на восьми из девяти
страниц MinerU сохранил пустой `content_list=[]`; на оставшейся странице вернул
только одну строку заголовка таблицы. Показанные выше 117954 символа появились
почти полностью из штатного `backfill_text_layer`, а не из VLM. Поэтому итоговый
балл 0,998 оценивает сохранность текста после восстановления, но ошибочно выглядит
как высокая оценка самого parser. В отдельном повторе трех table-страниц с
`RAG_MINERU_TABLE_ENABLE=true` сырые content-list остались пустыми, таблиц снова
получено 0, среднее время составило 28,731 с. Проблема подтверждена для текущей
связки MinerU 3.3.1 + `vlm-http-client`; распространять вывод на все backend и
версии MinerU без отдельного теста нельзя.

Shadow-метаданные расширены до схемы 2: отдельно сохраняются агрегаты сырого
parser, `backfilled_page_count` и `backfilled_page_ratio`. Текст, номера страниц и
содержимое таблиц в JSONB не записываются. Поле `backend` теперь отражает
фактически выполненный MinerU, если выбранный альтернативный parser не применялся
к скану. Это наблюдаемость, а не автоматическое переключение backend.

Решение: не менять production backend и не включать fallback. Следующие проверки
выполнять в таком порядке:

1. На том же наборе сравнить MinerU с включенным и выключенным table mode —
   выполнено, улучшения нет.
2. Добавить benchmark-метрики `table_detected`, отношение числа извлеченных ячеек
   к ожидаемому и `visual_region_preserved`; не называть их точностью без сверки
   содержимого и геометрии — выполнено в локальном benchmark-скрипте.
3. Подготовить локальный классификатор типа страницы: обычный текст, таблица,
   график/схема, OCR-скан и смешанная страница. Проверять его отдельно от parser.
4. После контролируемой очистки диска сравнить на этих же сложных страницах
   MinerU pipeline, текущий VLM HTTP и изолированный MinerU 3.4; до этого не
   интерпретировать пустой HTTP-output как общий предел MinerU2.5-Pro.
5. Расширить набор до 150-250 доменных страниц и обязательно включить очень
   сложные многостраничные документы с продолжениями таблиц и разделов.
6. Только затем оценивать page-level router: MinerU для сплошного текста,
   Paddle для структурных/визуальных страниц и отдельный sidecar для chart data.
7. Итог принимать по downstream `Recall@5`, `nDCG@10`, точности чисел и цитат,
   сохраняя одинаковые embedding, reranker и генератор ответа.

**11.07.2026, production shadow v2.** Изменение развернуто на SHA `f261bc2` без
миграции БД и без остановки API/модельных сервисов; перезапущен только ARQ worker
при пустой очереди. Локально и на A100 полный pytest дал **56 passed**, профильный
Ruff прошел. Fallback и пользовательский выбор parser не изменялись.

Canary выполнен через локальный auth-off API на первой сложной table-странице
описанного ParseBench-набора. Worker за 28,34 с записал итоговый `score=1.0`, но
отдельно зафиксировал сырой `raw_parser.score=0.0`, причины `empty_output` и
`missing_pages`, `backfilled_page_count=1`, `backfilled_page_ratio=1.0`. В JSONB
нет текста страницы. Документ штатно прошел перевод, экспорт и индексирование до
`done`, затем удален через API. После удаления в базе снова 9 документов, очередь
ARQ пуста, внутренний и публичный `/healthz` возвращают `ok`; WSL, `origin/main`
и A100 синхронизированы.

### 6.8. Перепроверка Qianfan и KIE-sidecar на 11.07.2026

Актуализация выполнена через большой Tavily Research с часовым таймаутом,
точечное извлечение первичных страниц и Context7 для текущего vLLM. Ошибка
обзорного отчета о якобы еще не опубликованных весах Qianfan не принята:
официальный Hugging Face репозиторий фактически содержит Apache-2.0 `LICENSE`,
два shard весов общим размером 8,83 ГиБ и инструкции vLLM/SGLang/Transformers.

Публичная проверка изменила роль модели:

- ParseBench: Qianfan получает `46,20` overall, `72,30` tables, `0,90` charts,
  `83,50` content и `20,60` visual grounding. Это исключает его из общего
  parser fallback и из chart-to-data маршрута.
- Вендор сообщает среднее `87,9` по пяти KIE/OCR-наборам, но независимого
  прямого сравнения с актуальными KIE-моделями нет.
- Granite Vision 4.1-4B имеет Apache-2.0, специальный JSON-Schema режим и
  официальный результат `94,2` exact match на полном VAREX. Это более сильный
  первый кандидат, но не доказательство преимущества над Qianfan без общего
  набора.

Для прямого A/B добавлены скрипты
`scripts/download_varex_complex_subset.py` и
`scripts/benchmark_varex_sidecars.py`. Первый воспроизводимо выбирает по три
наиболее сложные схемы из разделов Nested и Table открытого VAREX, сохраняет
исходные PDF/JPEG, JSON Schema, эталон, URL и SHA-256. Второй использует один и
тот же официальный user-prompt, изображение, `temperature=0`, JSON mode и
лимит 8192 токена. Официальный scorer закреплен на commit `3cce9c1`; набор
содержит 113 оцениваемых полей. Простые формы намеренно не включались.

Обе модели запущены последовательно на GPU5 рядом с production MinerU, на
отдельных localhost-портах и без пользовательского трафика. Qianfan использовал
исправление рекомендованного самим Transformers Mistral-regex; без него runtime
явно предупреждал о неверной токенизации. Первый черновой прогон с системным
prompt признан методологически неверным и не использован в решении.

| Модель | VAREX, 6 сложных страниц | Nested | Table | Полностью верно | Среднее время |
|---|---:|---:|---:|---:|---:|
| Granite Vision 4.1-4B | **52,2%** | **49,0%** | 54,8% | **1/6** | **1,502 с** |
| Qianfan-OCR 4B | 43,4% | 29,4% | 54,8% | 0/6 | 3,635 с |

Вывод ограничен малой, специально усложненной диагностической выборкой:
Granite опережает Qianfan на 8,8 п.п. overall и на 19,6 п.п. по вложенным
формам, а по табличным формам модели равны. Ни одна модель не готова к
автоматическому production-включению. Для KIE сначала проверяется Granite,
Qianfan остается компаратором; для chart-to-data проверяются Granite и текущий
Qwen3.5, но не Qianfan. Канонический Markdown/layout остается у MinerU с
PaddleOCR-VL как проверяемым структурным fallback.

Локально добавлен безопасный нулевой этап page routing:

1. `page_routing.py` только предлагает роль страницы и атомарно сливает полную
   страницу при наличии канонических `bbox_pt/page_size_pt`; модель и БД он не
   вызывает.
2. Результаты KIE/chart/diagram являются отдельными sidecar-артефактами, а не
   `SegmentDraft` канонического документа.
3. Конфигурация маршрутизатора имеет режим `off` по умолчанию, будущие
   `shadow/canary`, allowlist, лимит 12 страниц, порог качества и таймаут.
4. Shadow v2 теперь получает реальное покрытие нативного текстового слоя по
   страницам без повторного чтения PDF; sparse-ключи отклоняются.

Проверка WSL и A100: полный `pytest` — **76 passed**, профильные Ruff и mypy
прошли. Изменение развернуто на SHA `379599e`; при пустой очереди перезапущен
только worker. Код маршрутизации не подключен к worker и не меняет
пользовательский результат.

Production canary выполнен через auth-off localhost API на наиболее сложной
Nested-форме VAREX. Документ прошел состояния `uploaded → parsing → translating
→ done`. Shadow записал итоговый `native_text_coverage=0,9958`, сырой
`raw_parser.native_text_coverage=0,0`, причины `empty_output`, `missing_pages`,
`low_native_text_coverage` и `backfilled_page_ratio=1,0`. Это подтверждает, что
новая метрика различает реальный parser и нативный добор. Canary удален штатным
API; очередь снова `0`, в библиотеке `9` исходных документов.

Временные endpoints остановлены, trial-веса Qianfan и Granite удалены; на диске
снова свободно 33 ГиБ, GPU5 вернулся к 12927 МиБ. Внутренний и внешний
`/healthz` после эксперимента — `ok`. Следующее решение принимается только после
150-250 очень сложных страниц, включая многостраничные продолжения таблиц,
P&ID, плохие RU/EN/ZH-сканы и downstream RAG-метрики.

### 6.9. Подключение shadow page routing

После production canary нулевой scaffold доведен до выполняемого теневого
маршрутизатора. Добавлен чистый `page_classifier.py`, который без ML-моделей и
новых зависимостей классифицирует каждую физическую страницу как `text`,
`table`, `form`, `chart`, `diagram`, `scan`, `mixed` или `unknown`. Он использует
только уже извлеченные типы сегментов, структурные метаданные, подписи,
формоподобные строки, нативный текст и `PageParseQuality`.

`page_routing_shadow.py` для каждой страницы отдельно пересчитывает raw/final
качество тем же `evaluate_parse`, учитывает backfill и явный выбор backend,
затем вызывает чистый `propose_page_route`. При лимите страниц специализированные
`structured/chart/diagram` роли имеют приоритет перед общим parser fallback;
внутри роли сохраняется физический порядок страниц.

Режимы теперь имеют исполняемую семантику:

- `off` не классифицирует и не пишет routing-сигналы;
- `shadow` рассчитывает план для PDF и сохраняет только агрегаты;
- `canary` рассчитывает его только для `owner_sub` из allowlist;
- явно выбранный пользователем parser всегда остается `primary`;
- ни один режим пока не вызывает вторую модель и не меняет `Segment`, `Chunk`,
  перевод, экспорт или RAG.

В `documents.parse_quality.page_routing` допускаются только версия схемы, режим,
счетчики типов/ролей/причин, число eligible/selected/truncated страниц и число
классификаций с низкой уверенностью. Текст, значения полей, bbox и номера страниц
в shadow JSONB не записываются.

Для длинного A/B `benchmark_varex_sidecars.py` получил возобновление после
сбоя. Resume разрешен только при совпадении версии протокола, модели, лимита
токенов, температуры и роли prompt; поврежденный JSON или прошлый error
обрабатывается повторно. Локальная проверка: **91 passed**, профильные Ruff,
mypy и `git diff --check` прошли.

Shadow router развернут на SHA `dcbf300`; A100 также дал **91 passed**, Ruff и
mypy без ошибок. Worker перезапущен при пустой очереди с
`RAG_PARSER_PAGE_ROUTER_MODE=shadow`. Production canary на наиболее сложной
Nested-форме VAREX классифицировал страницу как `form`, выбрал
`structured_extraction` по причине `form_requires_schema_extraction` и записал
ровно один selected route. Raw MinerU остался `score=0,0`, итог после нативного
добора `score=0,9995`; основной pipeline дошел до `done`. Canary удален, очередь
`0`, в библиотеке снова `9` документов, внутренний и внешний `/healthz` — `ok`.

Архитектурный аудит следующей фазы принял отдельный контур
`document_structured_artifacts`: metadata в Postgres, неизменяемый JSON в private
MinIO, обязательная `parse_revision`, owner/RLS-проверка через `Document`,
отдельная ARQ-очередь с параллелизмом 1 и lifecycle `off → shadow → canary`.
Structured sidecar не будет храниться в `Segment.meta`, `Chunk` или
`parse_quality` и не сможет задержать или сломать основной pipeline.

### 6.10. Расширенный сложный корпус

На A100 воспроизводимо скачаны 150 наиболее сложных страниц VAREX: 75 Nested и
75 Table. Manifest содержит CDLA-Permissive-2.0, JSON Schema, эталон, исходные
PDF/JPEG и SHA-256. Проверены все 300 файлов; расхождений хешей нет, общий размер
124 МБ. Этот набор используется для устойчивой оценки KIE, а не заменяет другие
типы документов.

На всех 150 страницах выполнен прямой A/B Granite Vision 4.1-4B и Qianfan-OCR
4B. Обе модели получили одинаковые изображения, JSON Schema, официальный
user-prompt, `temperature=0`, JSON mode и лимит 8192 токена. Qianfan запускался
с обязательным `fix_mistral_regex=true`; предупреждение неверной токенизации
отсутствовало. Официальный VAREX scorer оценил 2936 полей:

| Модель | Overall | Nested | Table | Полностью верно | Валидный JSON | Median | p95 |
|---|---:|---:|---:|---:|---:|---:|---:|
| Granite Vision 4.1-4B | **90,5%** | **88,9%** | **91,7%** | **58/150** | 147/150 | **2,643 с** | **4,045 с** |
| Qianfan-OCR 4B | 15,8% | 14,2% | 17,0% | 10/150 | 146/150 | 4,549 с | 9,003 с |

Три Granite и четыре Qianfan ответа дошли до лимита или вернули незавершенный
JSON; их первичные ошибки не скрывались retry и вошли в score. Среднее время с
учетом обрывов: 3,877 с у Granite и 7,008 с у Qianfan, максимум 65,98 и 75,749 с.
Результат шести страниц из §6.8 остается диагностикой экстремального края, но
не используется как финальная оценка.

Решение: Qianfan исключен из общего parser fallback, chart route и первого KIE
sidecar; он остается только историческим компаратором. Первый кандидат
structured extraction — Granite Vision 4.1. Его 90,5% на формах не разрешает
автоматическое включение: остаются JSON failures, нет проверки RU/EN/ZH
нефтегазовых форм, P&ID и downstream RAG. Перед shadow model job обязательны
отдельная очередь, bounded schema splitting и закрытый доменный release gate.

Для полного parser release gate спроектирован набор из 234 входов и 184
уникальных исходных содержаний:

| Корпус | Входы | Назначение | Лицензионный статус |
|---|---:|---|---|
| [PubTables-v2](https://huggingface.co/datasets/kensho/PubTables-v2), rev `aa575e7` | 36 | полные многостраничные таблицы | CDLA-Permissive-2.0 |
| [VAREX](https://huggingface.co/datasets/ibm-research/VAREX), rev `2dfc338` | 60 | Flat/Nested/Table при 200 и 50 DPI | CDLA-Permissive-2.0 |
| [ChartArena](https://github.com/pspdada/ChartArena), rev `472c39b` | 24 | EN/ZH charts, flowchart, mind map | данные research-only; manifest-only |
| [PID2Graph](https://zenodo.org/records/14803338), DOI `14803338` | 24 | полные P&ID, символы и связность | CC BY-SA 4.0, GraphML требует проверки |
| [AI2D-RST](https://github.com/thiippal/AI2D-RST), rev `76cf0f8` | 12 | научные диаграммы и связи | CC BY/CC BY-SA 4.0 |
| [MWS Vision Bench](https://huggingface.co/datasets/MTSAIR/MWS-Vision-Bench), rev `b8d4737` | 30 | RU OCR/Markdown/grounding/KIE/VQA | MIT |
| [MDPBench](https://github.com/Yuliang-Liu/MultimodalOCR/tree/main/MDPBench), rev `8b24ed5` | 24 | RU/EN/ZH digital/photo | лицензия данных неясна; manifest-only |
| [Real5-OmniDocBench](https://huggingface.co/datasets/PaddlePaddle/Real5-OmniDocBench), rev `8a8ce98` | 24 | scan/warp/photo/light/skew | лицензия данных неясна; manifest-only |

Отбор внутри каждого страта выполняется по сортировке
`SHA256("docragenslate-ocr-v1\0" + dataset + "\0" + stratum + "\0" +
canonical_id)`. Manifest фиксирует revision/DOI, canonical/group id, язык,
сценарий, URL, лицензию, относительный путь, SHA-256 и selected hash.
Многостраничные таблицы и P&ID оцениваются целиком, а не отдельными удобными
кропами. Помимо официальных метрик сохраняются parse failure rate, p50/p95,
pages/min и пиковая VRAM.

ChartArena, PID2Graph, MDPBench и Real5 не помещаются в Git до юридической
проверки. Публичный набор дополняет, но не заменяет закрытый release gate из
20-30 реальных нефтегазовых документов RU/EN/ZH с таблицами, P&ID, печатями,
исправлениями и плохими сканами.

Для лицензионно однозначной части добавлен offline-конструктор
`scripts/build_licensed_ocr_manifest.py`. Он не скачивает данные и принимает
только локальный JSONL-каталог, фиксирует точные revision и лицензии, проверяет
source-specific pinned URI, роли, lowercase SHA-256, квоты и число входов.
Детерминированное двудольное сопоставление закрывает квоты без ложного дефицита
при пересечении физических групп; exact URI/SHA-дубли отклоняются. Результат
содержит 86 единиц и 138 входов: PubTables-v2 36, VAREX 60, AI2D-RST 12 и MWS
Vision Bench 30. Остальные 96 входов полного дизайна остаются manifest-only до
завершения лицензионной проверки.

Offline-builder не читает исходные байты, поэтому manifest честно получает
`verification_state=metadata_only_unverified`. Поле `group_policy` обозначает
только заявленную identity, а `materialization_requirements` требует сверить
каждый объект с SHA-256, подтвердить trusted physical identity и отклонить весь
manifest при любом расхождении. VAREX/MWS дополнительно обязаны ссылаться на
pinned source record; AI2D разделяет pinned annotation и content-addressed
source image. Локальный полный набор тестов: **175 passed**; профильные Ruff,
mypy и `git diff --check` прошли.

### 6.11. Revision-safe foundation structured sidecar

Следующий expand-only слой реализован без запуска model jobs и без изменения
канонического RAG:

1. `structured_artifacts.py` задает строгий Pydantic discriminated union для
   `kie`, `chart` и `diagram`. Ограничены строки, поля, серии, точки, узлы и
   ребра; запрещены NaN/Inf, active HTML и встроенные `data:`/`file://` ресурсы.
2. Каждый envelope содержит UUID документа/артефакта, `parse_revision`, физическую
   страницу, SHA-256 источника, backend/model/prompt version и timezone-aware
   время. Bbox валидируется в канонических пунктах внутри `page_size_pt`.
3. JSON сериализуется детерминированно, ограничен 5 МиБ и получает content
   SHA-256. MinIO key строится только из UUID, revision, page index и enum типа:
   `{document_id}/sidecars/r{revision}/p{page}/{type}/{artifact_id}.json`.
4. Миграция `0023` добавляет `document_structured_artifacts` с FK cascade,
   статусами `queued/running/ready/error/superseded`, request/source/content
   hash, summary без extracted values, уникальным idempotency key и lookup index.
5. Таблица получает отдельную RLS policy через `documents.owner_sub` с `USING`
   и `WITH CHECK`; app-level authorization остается обязательной. В текущем
   production API работает под `rag_api` (`NOSUPERUSER`, `NOBYPASSRLS`, не
   владелец), а миграция `0025` дополнительно включила FORCE RLS.
6. Storage умеет удалять все artifacts документа только по точному UUID-префиксу.
   DELETE API вызывает эту очистку best-effort, не затрагивает соседние UUID и не
   блокирует каскадное удаление БД при сбое MinIO.

Foundation пока не регистрирует ARQ-задачу, не создает строки/объекты и не имеет
пользовательского API. Следующий допуск требует отдельной очереди с
параллелизмом 1 и guarded publish `running → ready` только при совпадении текущей
`parse_revision`. Локально: **102 passed**, профильные Ruff/mypy прошли,
`alembic heads` показывает единственную head `0023`.

Первый из этих предохранителей реализован локально: модуль
`workers/structured_sidecar.py` задает отдельную очередь
`arq:structured-sidecar`, отдельный health key, `max_jobs=1` и использует
существующие lifecycle БД/MinIO. ARQ 0.28 требует хотя бы одну функцию, поэтому
зарегистрирована только безвредная `health_probe`; модельных jobs и cron нет,
модельные клиенты не создаются, основной `WorkerSettings` не менялся. Имя
очереди валидируется в отдельном namespace и не может совпасть с `arq:queue`,
а тест создает настоящий экземпляр ARQ Worker. Каркас еще не запускается в
production и сам по себе не создает дополнительный процесс.

Foundation развернут на SHA `2dafbab`. На A100 полный pytest также дал
**102 passed**, Ruff/mypy прошли. В отдельной временной Postgres-базе с теми же
предустановленными `vector/pg_trgm` успешно выполнен цикл
`upgrade 0022 → 0023 → downgrade 0022 → upgrade 0023`; повторная полная сборка
с нуля подтвердила `RLS=true`, одну policy и 14 constraints.

Перед production DDL создан schema-only backup
`/root/parser_trials/rag_schema_before_0023_2026-07-11.sql`, 34577 байт. Роль
приложения не имеет постоянных DDL-прав; для миграции ей временно выданы только
`CREATE ON SCHEMA public` и `REFERENCES ON documents`, после транзакции оба права
отозваны и повторная проверка дала `false/false`. Production находится на
`0023`, таблица пуста, RLS включен. API и worker перезапущены при пустой очереди;
page-router остался в `shadow`, все health-check — `ok`.

Delete canary проверил prefix isolation на настоящем MinIO: под UUID публичного
VAREX-canary создан тестовый sidecar-object, под соседним UUID — контрольный.
Штатный DELETE удалил все объекты только целевого документа, контрольный объект
сохранился и затем был удален отдельно. Canary дошел до `done`, после очистки в
библиотеке снова 9 документов, очередь `0`.

### 6.12. Granite structured extraction protocol v3

До подключения модельной задачи принят воспроизводимый протокол
`granite-varex-split-v1`. Подтвержденные ограничения Granite Vision 4.1-4B:
Apache-2.0, нативный vLLM не ниже 0.21.0, официальный zero-shot VAREX 94,2%; на
раннем прогоне 150 сложных страниц получено 90,5% и три обрыва по лимиту 8192
токена. Отдельный прямой `benchmark protocol v3` на vLLM 0.25.0 дал 93,97%
нормализованного EM и 150/150 валидных ответов, но не использовал описанные ниже
разбиение/объединение и учет первого ответа; это предварительная оценка базовой
модели, а не результат рабочего исполнителя `granite-varex-split-v1`.
VAREX отдельно описывает schema echo при `$defs/$ref` и ухудшение последних
полей длинной схемы, поэтому отправлять произвольную пользовательскую схему
одним запросом нельзя.

Обязательный protocol v3:

1. Принимать только локальные `$ref`, детерминированно раскрывать `$defs`,
   отклонять циклы, внешние ссылки и конфликтующие композиции; все листья делать
   nullable и сохранять исходные JSON Pointer.
2. Flat/Nested chunk ограничивать 24 scalar leaves и 2048 schema tokens;
   таблицу — восемью новыми колонками плюс максимум два повторяемых anchor.
   Максимум 16 logical chunks, 24 model attempts и глубина split 3 на страницу.
3. Использовать один PNG страницы и один user multimodal message, официальный
   VAREX prompt, `temperature=0`, JSON object mode; 4096 output tokens для
   Flat/Nested и 8192 для Table. Модель закреплять точным HF commit SHA.
4. Считать `source_sha256` от точных PNG bytes, `schema_sha256` от канонической
   нормализованной схемы, а parent `request_hash` — от версии протокола/prompt,
   model id/revision, обоих hash, split policy и token limits. Endpoint,
   timestamp, timeout и номер попытки в hash не входят.
5. Принимать только `finish_reason=stop`, единственный JSON object без schema
   echo, со всеми запрошенными путями и явным `null` для отсутствующих значений.
   После merge результат повторно валидируется полной нормализованной схемой.
6. 408/429/5xx допускают один идентичный retry. `length`, invalid JSON, schema
   echo и missing paths вызывают только детерминированный split. Минимальный
   невалидный chunk становится terminal failure, а не подменяется null-ответом.
7. Все model attempts append-only; даже восстановленный artifact сохраняет
   `first_pass_success=false`, исходную ошибку и отдельную recovered-метрику.
   Raw response хранится только в private MinIO, Postgres получает статусы и
   hash без извлеченных пользовательских значений.

Допуск Granite job: не ниже 90,0% overall, 88,5% Nested и 91,0% Table на 150
сложных VAREX, final valid JSON не ниже 149/150 при отдельном учете first-pass;
затем закрытый gate минимум на 60 RU/EN/ZH нефтегазовых формах с полным
сохранением подтвержденных чисел, единиц и идентификаторов. До выполнения этих
условий очередь остается пустой, а model sidecar — выключенным.

Прямой замер подтверждает запас качества базовой модели, но не закрывает
VAREX-проверку рабочего исполнителя. Требуются отдельный сквозной прогон через
`granite-varex-split-v1` и закрытая доменная проверка; постановщик/API не
включаются и sidecar остается выключенным.

Чистый foundation протокола реализован в
`pipeline/structured_extraction_protocol.py`: fail-closed подмножество JSON
Schema, локальные `$ref`, preflight-лимиты размера/глубины/ref/leaves/chunks,
канонический request hash, bounded Nested chunking и schema-echo detector.
Combinators и межполевые ограничения отклоняются. Для корневых table-массивов
добавлен отдельный split/merge: максимум восемь новых колонок, два повторяемых
anchor, 50 строк и 4096 байт на ячейку. Многочастный merge разрешен только по
непустым уникальным anchor без позиционного fallback; расхождение состава строк,
null/отсутствующего значения или повтор anchor завершает страницу ошибкой.
Вложенная таблица внутри `$ref` пока не раскрывается table-планировщиком и должна
явно попасть в release gate, а не быть молча упрощена. Foundation не обращается
к модели, БД или MinIO и не включен в production flow.

Каркас очереди, protocol v3 и offline-manifest зафиксированы коммитом
`2da75f9` и синхронизированы между WSL, `origin/main` и A100. На A100 полный
pytest дал **175 passed**, профильные Ruff/mypy прошли; основная и sidecar
очереди содержат `0` jobs, внутренний и публичный `/healthz` возвращают `ok`.
Running API и основной worker не перезапускались, новый sidecar worker не
запускался, модельные сервисы и production-поведение не изменялись.

### 6.13. Байтово проверенный сложный корпус и downstream-метрики

Первоначальный контракт с отдельным URL каждого изображения оказался неверным
для реальных источников. PubTables хранит изображения и аннотации в двух TAR.GZ,
VAREX — в четырех Parquet shards, AI2D/AI2D-RST — в двух ZIP, а MWS связывает
закрепленный JSONL с отдельными image assets. Контракт заменен на типизированные
ссылки `direct`, `archive_member`, `parquet_field`, `parquet_row` и `jsonl_row`.
Для каждого контейнера закреплены revision и SHA-256; локальные пути, redirect,
archive member, row/field и derived bytes проверяются до присвоения статуса.

Лицензии фиксируются консервативно и на уровне компонентов: PubTables и VAREX —
CDLA-Permissive-2.0; AI2D + AI2D-RST —
`CC-BY-4.0 AND CC-BY-SA-4.0`; MWS — `MIT AND CC-BY-4.0` из-за расхождения
деклараций benchmark code и source assets. Materializer не разрешает менять
`license_components` или `source_components` при переходе в `bytes_verified`.

Добавлены три offline-утилиты:

1. `build_licensed_ocr_candidates.py` читает только локальные закрепленные
   контейнеры, повторно проверяет SHA-256/inode после обработки, ограничивает
   TAR/ZIP/Parquet/JSONL и строит точные source references.
2. `build_licensed_ocr_manifest.py` выполняет детерминированный hash-ranking и
   двудольное сопоставление квот; `NaN/Infinity`, подмена лицензии, повтор одной
   физической записи и превышение закрепленной квоты отклоняются.
3. `materialize_licensed_ocr_manifest.py` заново проверяет каждый container и
   derived object, поддерживает безопасный локальный cache по имени SHA-256,
   пишет во временный каталог и публикует результат только атомарно.

На A100 обработаны реальные 4,7 ГБ исходных контейнеров. Генератор получил
**6708 кандидатов**; manifest выбрал **86 единиц / 138 входов**: PubTables 14/36,
VAREX 30/60, AI2D-RST 12/12 и MWS 30/30. SHA-256 исходного manifest:
`1a7773ff9a1bd3040fdae714a536ea8bf86fb5d90397276c3347865311d8f5f4`.

Материализация в `/root/parser_trials/ocr_materialized_v1` завершилась статусом
`bytes_verified`. Независимый повторный проход подтвердил **224/224 ссылки**,
224 уникальных объекта, **9/9 контейнеров**, 5 045 423 116 проверенных байт,
ноль межкорпусных digest-дублей и ноль незавершенных `.part`. SHA-256 итогового
`manifest.verified.json`:
`5255af59ebbacec8354a136863e133812db827cbbda413acc6c551fc37ed5144`.

Table-протокол отдельно прогнан на 75 сложных VAREX Table schemas: все 75
планируются, а эталон проходит split/merge 75/75 после нормализации
`missing → null`. Без этой явно заданной нормализации exact составляет 71/75,
поскольку четыре официальных ground truth не содержат часть schema fields.
Все 75 реальных таблиц оказались одночастными; перестановка строк и merge трех
частей проверены синтетическими adversarial-тестами, а не выданы за результат
VAREX.

В `rag_app.eval.rag_metrics` добавлены детерминированные Recall@k, MRR@k,
graded nDCG@k, validity/precision/recall/F1 цитат и точность пар «число + единица»
по стабильным внешним evidence references. No-answer оценивается отдельно.
Неоднозначное `1,234` требует явной политики decimal/thousands, `0,125`
нормализуется как `0.125`, а поврежденные смешанные формы вроде `1,234,56`
отклоняются. Локальный полный набор после защитных исправлений: **237 passed**;
профильные Ruff, mypy и `git diff --check` прошли.

Эти метрики пока не являются доказательством улучшения production RAG:
structured sidecars еще не индексируются в `Segment`/`Chunk`. Следующий gate
должен сравнить parser backends в отдельных eval-БД при одинаковых embedding,
reranker и generator, используя стабильные source/page references. Model job,
sidecar worker и пользовательское поведение production на этом шаге не менялись.

### 6.14. Повторная проверка моделей и единый parser-benchmark contract

Актуализация на 11.07.2026 выполнена через Tavily по первичным карточкам моделей,
техническим отчетам и текущему ParseBench. Отдельные агенты независимо проверили
рейтинг и эксплуатационную пригодность для A100-40GB. Универсальной новой модели,
доказанно превосходящей Granite Vision 4.1 на schema-KIE, не найдено: Granite
остается единственной из shortlist с опубликованным VAREX 94,2%, а локально уже
получил 90,5% на 150 сложных страницах.

Актуальный ролевой shortlist:

| Роль | Первый кандидат | Закрепленная ревизия | Причина |
|---|---|---|---|
| Общий parser/table/layout | `infly/Infinity-Parser2-Flash` | `9837b83778196e6107b3767ca62eb5bdfc08f22a` | Apache-2.0, 2,2B, ParseBench 73,3 против 72,8 у MinerU; помещается на одной A100 |
| Действующий fallback | `PaddlePaddle/PaddleOCR-VL-1.6` | `66317acc4c9fc17bd154591ce650735cd2855f3e` | Apache-2.0, 0,959B, уже поднят на `:8118`; OmniDocBench v1.6 96,33 и Table-TEDS 94,76 |
| Schema-KIE challenger | `numind/NuExtract3` | `2e9fca82ee641e6bb6e1f5d905241e994be27a07` | Apache-2.0, 4,5B, schema-driven JSON и одна A100; обязан пройти тот же VAREX, что Granite |
| Quality ceiling | `infly/Infinity-Parser2-Pro` | `2f82e707a3baa094c2f121e72b8455132e4b08c4` | ParseBench 74,3, но около 70 ГБ BF16 и официальный TP=2; не подходит первым production-кандидатом без NVLink |
| Вторичный parser | `FireRedTeam/FireRed-OCR` | `7bf5e9d91d3bb6bf91a3b6bc66d163096b696e50` | Apache-2.0, 2,1B; тестировать только если Flash/Paddle не закрывают доменные ошибки |

KDL-Frontier-Parser-nano остается первым в ParseBench среди этих компактных
моделей: 76,4 overall, 85,6 tables, 63,4 charts, 87,2 content, 66,8 semantic и
78,8 grounding. Но веса опубликованы под AGPL-3.0, а фактический A100-smoke в
двух vLLM-runtime дал поврежденный layout. Поэтому открытая доступность весов не
делает KDL допустимым для Apache-only production. Qianfan-OCR также не возвращен
в общий fallback: его локальные 15,8% на VAREX и ParseBench chart 0,9 сильнее
ограничивают решение, чем заявленные общие OCR-метрики.

Для следующих A/B в `scripts/benchmark_complex_parsers.py` реализован единый
offline-import внешних результатов:

```bash
uv run python scripts/benchmark_complex_parsers.py CORPUS OUT \
  --backends mineru paddle_vl \
  --prediction infinity_flash=/path/to/canonical_predictions
```

Каждый `<source.pdf>.json` обязан содержать версию схемы, точные `model id`,
revision и runtime, SHA-256 исходного PDF, измеренную latency и непрерывный
reading order сегментов. До оценки проверяются SHA corpus manifest, диапазон
страниц, конечные координаты, `bbox_pt` внутри `page_size_pt`, типы сегментов и
лимиты размера. Сводка пишется атомарно; ошибочный prediction остается явным
`status=error` и не маскируется пустым успешным parse. В worker/API этот адаптер
не импортируется.

Локальная проверка: профильный набор **10 passed**, полный backend **245 passed**;
измененные файлы проходят Ruff и mypy. Репозиторный Ruff пока не является
зеленым: осталось 124 ранее существовавших нарушения вне этого изменения.
Следующий порядок неизменен: сначала полный локальный прогон, затем синхронизация
на A100; Infinity Flash проходит 138 байтово проверенных входов и закрытый
доменный набор, NuExtract3 — те же 150 VAREX и сложные RU/EN/ZH-формы против
Granite. Только после parser-only downstream `Recall@5`, nDCG, точности цитат и
чисел допускаются shadow model job и canary. Production и десять действующих
аккаунтов этим изменением не затронуты.

Контракт зафиксирован коммитом `47d40a2` и синхронизирован между WSL,
`origin/main` и A100. На A100 профильный набор дал **10 passed**, полный backend
**245 passed**, измененные файлы прошли Ruff/mypy. API, основной worker,
sidecar worker и GPU-сервисы не перезапускались. Основная и sidecar ARQ-очереди
после проверки `0/0`; внутренний и публичный `/healthz` возвращают `ok`,
`/api/config` подтверждает `auth_enabled=true` и production OIDC issuer.

### 6.15. Изолированные runners Infinity Flash и NuExtract3

Перед новым скачиванием выполнен read-only аудит A100. Наиболее безопасен GPU5:
при работающем MinerU свободно 27 515 МиБ. На корневом разделе осталось около
23,99 ГБ и заполнение достигло 96%, поэтому model cache запрещено размещать в
`/root`. Временные HF-веса и окружения должны жить в
`/tmp/rag_parser_trials`, а `/root/parser_trials/runs` хранит только небольшие
логи, prediction и итоговые манифесты. Infinity и NuExtract запускаются строго
последовательно; production-переводчик на GPU1 не затрагивается.

Для Infinity-Parser2-Flash добавлен
`scripts/generate_infinity_flash_predictions.py`. Runner:

1. Принимает только полный pinned commit
   `9837b83778196e6107b3767ca62eb5bdfc08f22a` и заранее сверяет SHA каждого PDF.
2. Использует direct Transformers, а не vLLM и не SDK: прежний vLLM 0.17.1
   вернул 8192 символа `!`, а SDK безусловно импортирует vLLM даже для другого
   backend.
3. Рендерит одностраничный PDF при фиксированном DPI, отправляет официальный
   `doc2json` prompt и строго разбирает полный JSON без repair/truncation.
4. Проверяет нормализованные bbox `0..1000` и переводит их непосредственно в
   top-left PDF points. Reading order модели становится непрерывным `idx`.
5. Преобразует title/text/formula/figure/table в канонические сегменты. HTML-
   таблица получает реальные ячейки и span; неразбираемая таблица явно остается
   paragraph с `table_parse_error`, поэтому не может дать ложный table score.
6. Пишет revision, точные runtime-версии, source SHA, latency, render metadata,
   сырой ответ и атомарный resume-safe prediction.

Для `benchmark_varex_sidecars.py` добавлен профиль `nuextract3`. Он не вставляет
JSON Schema в обычный текстовый prompt. Официальный NuMind 0.3.0 конвертирует
схему в JSON-template с `omit_unsupported_branches=false`; любое потерянное поле
завершает страницу ошибкой. Template передается JSON-строкой через
`chat_template_kwargs.template`, описания полей — через `instructions`, thinking
выключен. На всех 150 текущих VAREX схемах официальный конвертер агента дал
150/150 без потерянных ветвей и пустых инструкций. Прогон закрепляется на
`2e9fca82ee641e6bb6e1f5d905241e994be27a07`, требует явный runtime, seed 0,
8192 output tokens и принимает только `finish_reason=stop` плюс прямой валидный
JSON без исправлений.

Локально профильный набор дал **24 passed**, полный backend — **254 passed**;
измененные runners проходят Ruff/mypy. Следующий обязательный этап: A100
fail-fast smoke Infinity на сложной table/chart странице и greedy parity-smoke
NuExtract Transformers/vLLM на пяти VAREX. Только после валидного smoke
разрешается полный прогон; production API/worker и пользовательские данные на
этом этапе не меняются.

### 6.16. Фактическая проверка Infinity Flash и NuExtract3 на A100

Оба эксперимента выполнены 11.07.2026 на GPU5 рядом с работающим MinerU, на
отдельных локальных контурах, без остановки production API, ARQ-воркера или
модельных сервисов. Веса, окружения и кэш находились в
`/tmp/rag_parser_trials`; в `/root/parser_trials/runs` сохранены только малые
воспроизводимые результаты. Пользовательские документы и десять действующих
учетных записей не использовались.

**Infinity-Parser2-Flash.** Закреплена ревизия
`9837b83778196e6107b3767ca62eb5bdfc08f22a`; среда прямого запуска:
`Transformers 5.3.0`, `torch 2.10.0+cu128`, `qwen-vl-utils 0.0.14`,
`PyMuPDF 1.28.0`, `Pillow 12.3.0`. vLLM 0.17.1 ранее возвращал повторяющийся
`!`, поэтому в gate использован только прямой официальный Transformers-контур.

На первой сложной таблице ParseBench с 67 эталонными строками, 270 аннотациями
ячеек и 80 объединениями модель вернула валидные bbox и четыре сегмента, включая
таблицу: 65 строк, 940 сырых ячеек, 321 непустая и 91 уникальное значение.
Задержка составила **143,771 с**. Число ячеек является структурным прокси, а не
точностью: объединения и повтор значений не позволяют напрямую сравнивать 940 с
270. На той же странице production MinerU занял 28,483 с и не создал сегмент
таблицы, PaddleOCR-VL занял 16,509 с и создал таблицу.

На второй, еще более плотной таблице с 111 строками и 1821 аннотацией ячеек
генерация не завершилась более чем за 600 с. Процесс остановлен, а скрипт получил
обязательный `--max-generation-seconds=600`, чтобы отдельная страница больше не
могла неограниченно удерживать GPU. Решение: **не запускать полный корпус из 138
входов и не допускать Infinity Flash в fallback на текущем runtime**. Валидная
первая таблица подтверждает способность модели восстанавливать структуру, но
задержка и незавершение плотной страницы нарушают SLA. Повтор допустим только с
подтвержденной быстрой средой исполнения и тем же строгим контрактом результата.

**NuExtract3.** Закреплена ревизия
`2e9fca82ee641e6bb6e1f5d905241e994be27a07`; среда исполнения:
`vLLM 0.24.0`, `Transformers 5.5.4`, `numind 0.3.0`, `torch 2.11.0`, BF16,
контекст 16384, `max_num_seqs=1`, seed 0. Модель получала ту же страницу и ту же
JSON Schema, что Granite/Qianfan, но через официальный шаблонный контракт
NuExtract; оценщик использует ту же ревизию VAREX и те же 2936 полей.

Диагностический прогон шести страниц дал 90,3% overall и выглядел сильнее Granite на
этом экстремальном поднаборе. Полный прогон доказал, что такой вывод не
обобщается:

| Модель | Overall | Nested | Table | Полностью верно | Строго валидный ответ | Медиана | p95 |
|---|---:|---:|---:|---:|---:|---:|---:|
| Granite Vision 4.1-4B | **90,5%** | **88,9%** | **91,7%** | **58/150** | **147/150** | **2,643 с** | **4,045 с** |
| NuExtract3 | 73,4% | 85,4% | 63,8% | 42/150 | 127/150 | 3,398 с | 71,228 с |
| Qianfan-OCR 4B | 15,8% | 14,2% | 17,0% | 10/150 | 146/150 | 4,549 с | 9,003 с |

Все 23 отказа NuExtract3 являются честными
`finish_reason=length`, без исправления JSON или подмены пустым результатом: два
в Nested и 21 в Table. Для 127 валидных ответов медиана составила 3,198 с, p95 —
4,962 с и максимум — 6,578 с; с учетом отказов среднее выросло до 13,681 с,
максимум — до 71,385 с. Следовательно, проблема локализована не в обычной
скорости, а в неограниченно разрастающемся ответе для плотных схем.

Решение: **Granite остается первой моделью структурированного KIE-контура;
NuExtract3 не заменяет его и не допускается в production fallback**. NuExtract3
сохраняется как исследовательский кандидат после применения того же ограниченного
разбиения схем и таблиц и строгого слияния, что приняты протоколом Granite v3.
Новый A/B имеет смысл только при не менее чем 149/150 итоговых валидных JSON и
точности на том же VAREX, близкой к Granite, без ухудшения закрытой проверки
RU/EN/ZH. Qianfan остается исключенным из общего, chart и первого KIE-маршрута.

После эксперимента временный NuExtract endpoint остановлен, GPU5 вернулся к
исходным 12927 МиБ занято / 27515 МиБ свободно, 30 ГБ временного кэша и окружений
удалены. Воспроизводимые результаты сохранены в
`/root/parser_trials/runs/infinity_flash_9837b837_smoke_v1` и
`/root/parser_trials/runs/nuextract3_2e9fca82_full150_v1`. Локальный полный
backend-набор после runner-исправлений: **256 passed**. Production backend и
маршрутизация не переключались.

### 6.17. Исполняемый page fallback MinerU → PaddleOCR-VL

После модельного gate реализован первый фактически исполняемый fallback, не
меняющий роль MinerU как основного parser. Он предназначен только для страниц,
которые теневой router уже направил в `parser_fallback`: таблица без структуры,
низкий сырой score или страница, полностью восстановленная нативным текстовым
слоем. Формы, графики и диаграммы этим путем не заменяются: они остаются
отдельными KIE/chart/diagram sidecar и требуют явной схемы результата.

Предохранители:

1. Model call разрешен только при одновременных условиях
   `RAG_PARSER_PAGE_FALLBACK_ENABLED=true`, router mode `canary`, владелец входит
   в allowlist, основной backend — MinerU и есть хотя бы одна выбранная страница.
   Значения `off` и `shadow` никогда не вызывают второй parser.
2. Paddle обрабатывает отдельную временную копию PDF. Результат оценивается
   постранично; основной документ не меняется при таймауте, исключении, пустой
   странице, неверном page index или недостаточном улучшении.
3. Таблица принимается только если в сыром MinerU-результате table-сегмента нет,
   Paddle вернул валидную table-структуру и прошел минимальный quality score.
   Для `low_raw_score/backfilled_primary` дополнительно требуется заданный
   минимальный отрыв кандидата.
4. Замена выполняется атомарно целой страницей через существующий
   `merge_page_replacements`; глобальный reading order перестраивается. Пока
   Paddle не возвращает точные bbox, каждому принятому блоку задается честно
   помеченная `geometry_precision=full_page`: навигация ведет на страницу, но не
   выдает грубую рамку за точную локализацию.
5. Финальная запись остается защищена `parse_revision` и статусом `parsing`.
   Устаревшая задача не может опубликовать ни primary, ни fallback.
6. В `parse_quality.page_fallback` записываются только status, backend, счетчики
   попыток/принятых страниц и агрегированные причины. Текст, номера страниц,
   значения таблиц и сообщения исключений туда не попадают.
7. Sidecar-таймаут ограничен 180 с. При таймауте или отмене subprocess Paddle
   принудительно завершается и ожидается, поэтому не остается фонового GPU-
   процесса. Основной parse продолжает работу с MinerU-результатом.

Локально и на A100 полный backend-набор дал **265 passed**; профильные Ruff и
mypy прошли. Дополнительно выполнена offline-проверка на первой сложной
ParseBench-таблице: 67 эталонных строк, 270 аннотаций ячеек и 80 объединений.
Production MinerU за 28,704 с вернул ноль сырых сегментов; после нативного добора
router выбрал `parser_fallback` по `low_raw_score`. PaddleOCR-VL за 15,676 с
вернул три сегмента, включая таблицу. Gate принял ровно одну страницу, итог
содержал одну таблицу и `full_page`-геометрию. Временные файлы удалены; документ
в production API/БД не загружался.

Изменение развернуто на SHA `954707b`. Основной worker перезапущен при очередях
`0/0` с прежним `RAG_PARSER_PAGE_ROUTER_MODE=shadow`; новый fallback-флаг не
задан и равен `false`. Следовательно, код загружен в production-процесс, но ни
один из десяти пользовательских аккаунтов не получает второй parser. Внутренний
и публичный `/healthz` после перезапуска возвращают `ok`.

Следующий допуск: закрытый canary на 1-2 выделенных аккаунтах и 20-30 сложных
RU/EN/ZH нефтегазовых документах. Переключение разрешается только после
downstream `Recall@5`, nDCG@10, точности чисел/единиц и цитат; до этого режим
остается `shadow`, а fallback выключен.

#### 14.07.2026: завершение пункта 8/20

**Plan.** Выпускной объект пункта — не отдельный PaddleOCR-VL, а полная система
`MinerU 3.4.4 -> router -> выборочные страницы Paddle -> атомарный merge`.
Диагностический `ParseQualityReport` не считается доказательством точности.
Для следующего полноценного Gold закреплен fail-closed протокол: reading-order
NED вместе с Kendall tau-b и полнотой блоков; TEDS/TEDS-Struct и
GriTS-Top/Con/Loc для таблиц; COCO AP/AR для layout/bbox; downstream
`Recall@5`, `nDCG@10` и точность цитат. Непарные блоки должны штрафоваться, а
пропущенная таблица получать ноль. Источники:
<https://github.com/opendatalab/OmniDocBench>,
<https://docs.scipy.org/doc/scipy/reference/generated/scipy.stats.kendalltau.html>,
<https://github.com/microsoft/table-transformer>,
<https://github.com/cocodataset/cocoapi>.

Статистический допуск требует не менее 200 страниц из 30 документов, разбиения
по документам, RU/EN/ZH и критических типов страниц, 20 000 кластерных
bootstrap-выборок, нижней границы выигрыша не менее 1 п.п. на целевом срезе,
регрессии общих/downstream-метрик не более 0,5/1 п.п. и p95 не выше 2x MinerU.
Пустой срез, невычислимый интервал или несовпадение manifest означает
`ineligible`, а не условное принятие.

**Act.** Аудит выявил, что прежний canary сравнивал Paddle с `primary_raw`, но
заменял `primary_final` после текстового backfill. Теперь кандидат обязан не
ухудшить финальные score, native-text coverage, integrity и duplicate ratio.
`ParseQualityReport.acceptable` стал fail-closed для пустых/пропущенных страниц,
неверных page index, дубликатов, низкого покрытия нативного текста и поврежденной
структуры. Валидатор проверяет фактические `bbox_pt/page_size_pt`, конечность и
границы координат, `table_cells`, `table_rows`, rowspan и colspan.

Добавлен `pypdf 6.14.2`: во второй parser передаются только выбранные физические
страницы, после чего индексы строго возвращаются в исходную нумерацию. Временный
PDF создается с правами `0600`; дубли, несортированные и выходящие за диапазон
страницы отклоняются. Синтетический full-page bbox больше не допускается в
рабочем выборе: он остается только явной диагностической опцией unit-теста.
Следовательно, текущий Markdown-адаптер Paddle без точных bbox не может незаметно
ухудшить подсветку и цитаты.

**Verify.** На закрепленных выходах MinerU 3.4.4 для 147 сложных открытых страниц
router получил 31 роль `parser_fallback`, 67 `structured_extraction`, одну
`diagram_extraction` и 48 `primary`; семь сырых результатов не прошли новые
hard-инварианты. Это анализ полного набора, а не выбор удачных страниц. Отчет
`/root/parser_trials/page_fallback_p8_20260714/router147.json` имеет SHA-256
`844c31b29f41c18af1a6f29eb157529feae2ccb8891e49997dc8fb1dd6ad426e`.

Fail-fast A100 smoke выполнен на реальной сложной table-странице с точной
привязкой источника. У MinerU raw было 0 сегментов и score `0,0`; штатный
text-layer backfill дал 3 сегмента и score `0,9999`. PaddleOCR-VL-1.6 за
15,990 с вернул 3 сегмента, одну таблицу и score `1,0`, но без точной геометрии.
Шлюз отклонил замену с причиной `precise_geometry_missing`; канонические
сегменты не публиковались. Приватный content-free отчет
`/root/parser_trials/page_fallback_p8_20260714/smoke.json` имеет SHA-256
`df562a09211f2733db8639e5fb01d0f372a039ff1c25da5efb968d9ff96ccbaf`.

Локально и на A100 полный набор дал **654 passed, 1 skipped**; профильные Ruff и
mypy прошли. Worker перезапущен при пустой очереди на коммите `dd9ed16` с
`RAG_PARSER_PAGE_ROUTER_MODE=shadow`; fallback-флаг и allowlist не заданы.
MinerU, Paddle, API и внешний health-check остались доступны.

Зафиксирован runtime без подмены условий A/B: MinerU `3.4.4` / vLLM `0.21.0`
с ревизией модели `bff20d4ae2bf202df9f45284b4d43681555a97ed` и
PaddleOCR-VL `1.6` / PaddleOCR `3.7.0` / vLLM `0.10.2` с ревизией модели
`66317acc4c9fc17bd154591ce650735cd2855f3e`. Предупреждения Paddle о
непримененных min/max pixels и slow image processor сохранены в provenance как
ограничения измерения, но конфигурация во время прогона не менялась.

**Report.** Пункт завершен решением **не включать автоматическую замену страниц
в production**. Приложение уже стало безопаснее: поврежденная таблица, ухудшение
финального MinerU или неточный bbox больше не могут пройти canary gate, а
Paddle не обрабатывает ненужные страницы. Прямое улучшение RAG не заявляется:
для него отсутствуют точные bbox и достаточный закрытый structural/downstream
Gold. Production продолжает собирать обезличенный `shadow`; повторный допуск
начинается только после bbox-capable адаптера и полного протокола выше.

### 6.18. Строгий исполнитель Granite KIE и формат артефакта v2

12.07.2026 реализован ограниченный исполнитель Flat/Nested JSON Schema для
будущего Granite KIE-sidecar. Код пока не подключен к пользовательскому API,
очередям или production-маршрутизации и не меняет существующие документы.

Исполнитель принимает только `finish_reason=stop`, строгий JSON без повторных
ключей и `NaN/Infinity`, отклоняет echo схемы, лишние/пропущенные поля и
несовпадение типов. Сетевые 408/429/5xx допускают один повтор. Невалидный ответ
может вызвать детерминированное разбиение схемы глубиной не более трех уровней;
общий бюджет ограничен 24 модельными попытками. Частичные ответы объединяются
только без конфликтующих значений, после чего весь результат повторно
проверяется против исходной схемы. Сырой ответ модели находится только в
диагностической структуре попытки и не предназначен для журналов/метрик.

`KIEArtifact` получил обратно совместимую версию 2: она хранит точный вложенный
`result` и SHA-256 нормализованной схемы. Старый плоский список `fields` остается
версией 1. Для вложенного результата действуют лимиты глубины, числа узлов,
размера строк, конечности чисел и запрет активного HTML/встроенных ресурсов.

Коммит основы: `7d5d82d`. Локально полный backend-набор дал **278 passed**;
профильные Ruff/mypy прошли. На A100 профильный набор дал **53 passed**.
Следующая работа до включения Granite: исполнитель табличного протокола,
миграция задания KIE с `schema_sha/model_revision/source_key`, claim/lease и
условной публикацией по `parse_revision`, sweeper зависших заданий, затем
закрытый RU/EN/ZH canary. До этого новый модуль остается неиспользуемой основой.

### 6.19. LightOnOCR-2 и изолированная проверка vLLM 0.25

Проверены Apache-2.0 модели `lightonai/LightOnOCR-2-1B` на ревизии
`c97bd377f04481830395218fa8951df9deaba756` и bbox-вариант на ревизии
`714ffe765cc4cb6f2d92530236f45d1c9465cd96`. Официальный результат основной
модели — 83,2 +/- 0,9 на OlmOCR-Bench; он не является доказательством качества
на текущих сложных таблицах, диаграммах и русскоязычной документации. Источники:
<https://huggingface.co/lightonai/LightOnOCR-2-1B>,
<https://huggingface.co/blog/lightonai/lightonocr-2>,
<https://huggingface.co/datasets/lightonai/LightOnOCR-bbox-bench>.

Стенд закрепляет SHA модели и входного PDF, рендерит 200 DPI с максимальной
стороной 1540 px, сохраняет сырой текст и атомарный summary, проверяет
`finish_reason`, число токенов, повторяющуюся генерацию и нормализованные bbox.
Direct Transformers использовал последнюю стабильную версию **5.13.1** и
явное `fix_mistral_regex=true`: первая сложная таблица заняла 92,124 с и была
оборвана на 4096 токенах. Этот backend непригоден по задержке.

Основной тест выполнен на последнем стабильном **vLLM 0.25.0** с Model Runner V2,
BF16 и контекстом 12288 на GPU5 рядом с MinerU. Production vLLM 0.22.1 не
обновлялся. Официальное семплирование `temperature=0.2, top_p=0.9` завершило
только страницу с графиком из пяти; первая таблица выродилась в повтор слова
`lock` до лимита. Детерминированный режим `temperature=0, seed=0` при 4096
токенах также завершил только **1/5** страниц:

| Тип страницы | Finish при 4096 | Задержка |
|---|---:|---:|
| сложная таблица 1 | length | 11,421 с |
| сложная таблица 2 | length | 11,663 с |
| график/текст | stop, 496 токенов | 1,659 с |
| научная восьмиколоночная страница | length | 11,870 с |
| плотный юридический текст | length | 11,856 с |

Повтор с лимитом 8192 для четырех отказов завершил только первую таблицу:
5279 токенов и 14,960 с. Вторая таблица, восьмиколоночная страница и юридический
текст снова дали `finish_reason=length` на 8192 токенах за 24,289-24,597 с.
Итоговый fail-closed результат на пяти самых сложных страницах — **2/5**
завершенных. Завершенная таблица содержит сбалансированный HTML (48 строк,
630 ячеек), но это структурный smoke, а не доказательство точности значений.

Bbox-вариант завершил три chart/layout страницы за 1,067-3,024 с. Во втором
прогоне найдено соответственно 1, 2 и 0 валидных рамок встроенных изображений;
в первом — 0, 2 и 0, а SHA выходов различаются даже при `temperature=0, seed=0`.
Эти bbox относятся только к встроенным изображениям, а не к тексту, полям или
полной структуре страницы. Они не заменяют bbox MinerU и schema-KIE Granite.

Решение: **не включать LightOnOCR-2 в production parser fallback** и не занимать
под него постоянную GPU. Возможный будущий режим — узкий shadow-sidecar для
умеренно плотных сканов/научных страниц с обязательным `finish_reason=stop`,
детектором повторов и сравнением с MinerU/Paddle на полном закрытом RU/EN/ZH
наборе. Bbox-ветку повторно рассматривать только после измерения recall/IoU на
собственных графиках и схемах.

Воспроизводимые результаты сохранены в
`/root/parser_trials/runs/lightonocr2_c97_vllm025_smoke_v3`,
`lightonocr2_c97_vllm025_greedy_v4`,
`lightonocr2_c97_vllm025_greedy8k_v5` и
`lightonocr2_bbox_714ffe_vllm025_greedy_v2`. Runner находится в
`scripts/benchmark_lightonocr2.py`. vLLM 0.25 подтвержден изолированно, но его
общий production upgrade требует отдельной матрицы совместимости Qwen3.5,
Hy-MT2, embeddings/reranker, Paddle и MinerU, а также сравнения VRAM, throughput,
TTFT/TPOT и rollback. Источник release notes:
<https://github.com/vllm-project/vllm/releases/tag/v0.25.0>.

### 6.20. Актуальный KIE-кандидат и долговечный lifecycle

12.07.2026 повторно проверены открытые Apache-2.0 кандидаты по первичным
leaderboard и model-card данным. Универсального parser-кандидата, который
одновременно доказанно превосходит MinerU 2.5 Pro и PaddleOCR-VL 1.6 и помещается
в доступный одиночный A100-40GB, не найдено. Infinity-Parser2-Pro имеет высокий
ParseBench, но 35,1B/~70GB требует двух GPU; Flash уже не прошел фактический SLA.

Для узкого извлечения полей и таблиц наиболее сильным кандидатом для парного A/B
был выбран `Qwen3-VL-8B-Instruct`, Apache-2.0, ревизия
`0c351dd01ed87e9c1b53cbc748cba10e6187ff3b`. На VAREX он сообщает 96,6 EM,
97,6 ANLS, 96,6 Flat, 97,3 Nested и 95,0 Table против 94,2/96,4/95,2/94,9/92,3
у Granite 4.1. Публичные числа использовались только для отбора. Одинаковый
локальный прогон на байтово проверенных 150 задачах выполнен в разделе 6.21:
Qwen выиграл
только часть метрик, а доверительные интервалы всех полевых разностей включают
nоль. Granite остается первым кандидатом для рабочего контура, Qwen — претендентом;
доказанного Apache-2.0 лидера для произвольных инженерных схем пока нет.

Исполнитель Granite дополнен ограниченным табличным протоколом с разбиением
value-полей, повторяемыми anchor-полями, строгим объединением без частичного
успеха и общим бюджетом попыток. Добавлена миграция `0024` и lifecycle
структурированных артефактов: идемпотентный get-or-create, claim/lease,
ограниченные повторы, условная публикация по revision, supersede и sweeper
зависших заданий. Изолированный локальный клиент vLLM принимает только localhost
и по умолчанию выключен. Bounded KIE job зарегистрирован только в
structured-sidecar: до claim проверяет feature flag, валидирует SHA изображения
и pinned contract, выбирает nested/table executor, пишет attempt-unique candidate
и публикует его только при совпадении claim token и текущей parse revision.
Stale/error candidate удаляется; сырой ответ модели в БД и журналы не попадает.
Локально 21 профильный тест, Ruff и mypy прошли. Producer заявок и
пользовательский API не подключены.

Для A/B использован безопасный профиль Qwen3-VL-8B: изолированное окружение
vLLM 0.25.0, BF16 без квантизации, TP=1, контекст 16384, `max_num_seqs=1`, одно
изображение 256-1280 visual tokens, `gpu_memory_utilization=0.85`, eager и
loopback-порт 18009. Веса занимают 16,33 GiB, BF16 KV на 16K — около 2,25 GiB.
Операционный аудит показал, что GPU2 занимали визуальный эмбеддер и реранкер, а
не бездействующий Alma worker. При пустых очередях они и Alma были временно
остановлены, после A/B полностью восстановлены. Между Alma и визуальными
сервисами нет арбитража GPU; до его реализации такое окно остается ручной
операцией. Следующий шаг — закрытая доменная проверка RU/EN/ZH, не пилотный трафик.

### 6.21. Перепроверка Qianfan и парный A/B Qwen3-VL/Granite

13.07.2026 завершена повторная проверка сильных открытых моделей. Большой Tavily
Research запускался с увеличенным тайм-аутом, но тарифный лимит Research был
исчерпан; актуализация продолжена по первичным карточкам моделей, точным ревизиям,
официальным рейтингам и независимому ParseBench. Это ограничение источника не
подменялось модельными выводами.

Qianfan-OCR (`baidu/Qianfan-OCR`, Apache-2.0, ревизия
`623bf5d20d446abdb36606aa4547cd0c18886fe5`) открыт и запускаем, но не является
сильным резервным кандидатом: текущий ParseBench дает 46,20 в целом, 72,30 по
таблицам, 0,90 по графикам и 20,60 по визуальной привязке. На предыдущем
локальном VAREX-прогоне он
дал 15,8 против 90,5 у раннего Granite. Поэтому Qianfan удален из активного
плана: не добавлять адаптер и маршрут запуска, не направлять к нему сложные
страницы. Канонический контур парсинга остается MinerU с выключенным по умолчанию
постраничным резервом в PaddleOCR-VL по существующей оценке качества.

Из более сильных кандидатов фактически запускаемы на одной A100-40GB Qwen3-VL
и Granite. KDL Frontier Parser nano лидирует в ParseBench с 76,36, но AGPL-3.0
не соответствует лицензионному правилу и ранее не дал валидного результата на
локальном runtime.
Infinity-Parser2-Flash получил 73,25, но не прошел локальный SLA; Pro требует
около 70 GB/две A100. Chandra OCR2 исключен из-за коммерческих ограничений
modified OpenRAIL. `oi-uae/oi-OCR` заявляет 77,43, но в публичном репозитории на
момент проверки отсутствуют веса и запускаемый код, поэтому результат не
считается открытым воспроизводимым кандидатом.

Парный `benchmark protocol v3` A/B выполнен на тех же 150 сложных
VAREX-страницах: 75 Nested и 75 Table, 2 936 полей, одинаковые байты PNG, схема,
запрос, `temperature=0`, `seed=0`, объект JSON и лимит 8192 выходных токенов.
Обе модели работали на GPU2 в
vLLM 0.25.0/Transformers 5.13.1/torch 2.11.0+cu130, BF16, eager, context 16384.
Закреплены точные ревизии Qwen
`0c351dd01ed87e9c1b53cbc748cba10e6187ff3b` и Granite
`82472ca3a4905fff5e4daa481c0b9cd530859c79`. Использованы штатные обработчики
изображений моделей; для Qwen явно ограничено 256-1280 визуальных токенов.
Это прямой прогон полной схемы без разбиения/объединения рабочего
`granite-varex-split-v1`; его результат сравнивает базовые модели, но не закрывает
проверку рабочего исполнителя.

| Метрика | Qwen3-VL-8B | Granite 4.1-4B | Qwen - Granite |
|---|---:|---:|---:|
| Exact match normalized, micro | 94,79% | 93,97% | +0,817 п.п.; 95% ДИ [-1,815; +2,798] |
| Exact match raw, micro | 93,12% | 93,53% | -0,409 п.п.; 95% ДИ [-3,133; +1,728] |
| ANLS normalized, micro | 96,08% | 96,45% | -0,377 п.п.; 95% ДИ [-2,952; +1,338] |
| Nested normalized EM | 96,7% | 94,9% | +1,8 п.п. |
| Table normalized EM | 93,3% | 93,3% | 0 |
| Полностью верные документы | 84/150 | 67/150 | +17; доля +11,33 п.п., 95% ДИ [+2,00; +20,67] |
| Валидный завершенный JSON | 149/150 | 150/150 | Qwen: один `length` на длинной таблице |
| Латентность, медиана / p95 | 8,485 / 13,241 с | 6,761 / 10,747 с | Granite быстрее |

Для полевых метрик применен парный кластерный бутстрэп: 20 000 выборок 150
документов с возвращением, документ переносится целиком со всеми полями,
фиксированное зерно `20260713`, процентильный 95% ДИ. Все три полевых интервала
включают ноль, поэтому
общее превосходство Qwen статистически не доказано. Единственный из рассчитанных
номинальных 95% ДИ, не включающий ноль, относится к доле полностью идеальных
документов; это исследовательский результат без поправки на множественные
сравнения. В этом прогоне Granite был быстрее, дал 150/150 завершенных ответов и
обработал патологически длинную таблицу, на которой Qwen достиг лимита после
190,421 с.

Решение:

1. Qianfan исключить из активных кандидатов для парсинга, KIE и графиков; хранить
   только как историческую модель сравнения.
2. Не менять канонический резерв парсинга: MinerU -> Paddle по постраничной
   оценке качества остается правильным контуром для структуры и RAG-чанков.
3. Granite оставить первым кандидатом отдельного контура schema-KIE; Qwen3-VL
   добавить в будущий общий контракт как претендента для вложенных форм, но не
   включать постановщик заданий/API до доменной проверки RU/EN/ZH.
4. На закрытом наборе минимум 60 нефтегазовых форм измерить те же метрики,
   сохранность чисел/единиц/идентификаторов и p95. Маршрутизацию Nested -> Qwen,
   Table/слишком большой ответ -> Granite рассматривать только при подтвержденном
   выигрыше.
5. Добавить арбитраж GPU2 между Alma и визуальными сервисами до следующего
   модельного окна; ручное совместное включение тяжелой Alma job небезопасно.

Воспроизводимые артефакты оставлены на A100:
`/root/parser_trials/varex_sidecar_qwen3vl_150` и
`/root/parser_trials/varex_sidecar_granite_v3_150`. Временные окружения и
Granite cache удалены; production API/worker, visual embedding/reranker и Alma
восстановлены, очереди пусты, внешний `/healthz` отвечает `ok`.

Источники:
<https://www.parsebench.ai/#leaderboard>,
<https://huggingface.co/baidu/Qianfan-OCR/tree/623bf5d20d446abdb36606aa4547cd0c18886fe5>,
<https://huggingface.co/Qwen/Qwen3-VL-8B-Instruct/tree/0c351dd01ed87e9c1b53cbc748cba10e6187ff3b>,
<https://huggingface.co/ibm-granite/granite-vision-4.1-4b/tree/82472ca3a4905fff5e4daa481c0b9cd530859c79>,
<https://github.com/udibarzi/varex-bench/blob/3cce9c18530748cfc843f38c02e2f43e0694d167/README.md>.

## 7. Этап P1: поиск и RAG

### 7.1. Настоящий BM25

Текущий лексический поиск использует PostgreSQL `ts_rank`, несмотря на название
BM25 в комментариях. Предлагается A/B с `pg_textsearch` 1.3.1: расширение имеет
PostgreSQL License, поддерживает PostgreSQL 17 и выполняет BM25 внутри базы.
Источники: <https://www.postgresql.org/about/news/pg_textsearch-v10-3264>,
<https://github.com/timescale/pg_textsearch>.

Работы:

1. Добавить расширение в отдельный тестовый образ PostgreSQL.
2. Создать языковые индексы и согласовать токенизацию RU/EN; для китайского
   проверить отдельную стратегию сегментации.
3. Сохранить dense-поиск и объединение RRF.
4. Подобрать `dense_top_k`, `sparse_top_k`, RRF `k` и порог реранкера по набору.
5. Проверить поиск артикулов, стандартов, точных фраз и редких терминов.
6. В production построить BM25-индекс параллельно с действующим GIN, переключить
   чтение флагом и не удалять GIN до завершения периода наблюдения.

**Финал 15.07.2026: NO-GO.** Кандидатный образ `pg_textsearch 1.3.1`, два
языковых индекса и парный protocol v4 были изолированно проверены на A100 и не
затронули production. Три предварительных прогона (direct BF16, vLLM с 9 и 21
повтором) fail-closed выявили нестабильность общего production-reranker; пороги
не ослаблялись. В `dc25476` scoring временно переведен на среднее трех
последовательных live-вызовов со строгой проверкой ответа. Финальный прогон также
остановлен до quality/load gate: production-shaped final set не совпал с frozen
scores (`attempt_id=7dc06f4c0fccfc3cea82d3be286378bc381f0b961f9eb6369dd5b0fcd30dba9a`,
`case_id=ragq-230802d83f01233877ae678e`); все 14 889 запросов vLLM получили
HTTP 200, но отчет, аттестация и A/B-метрики не создавались после fail-fast.
Поэтому улучшение BM25 не доказано,
миграция `0026` не выпущена, backend остается PostgreSQL FTS/GIN, а
не прошедшее qualification усреднение отменено в `575f864`.

### 7.2. Фильтрованный HNSW

Для установленного pgvector 0.8.2 проверить `hnsw.iterative_scan` в транзакции
поиска. Фильтры владельца, документа и папки могут уменьшать число найденных
кандидатов до применения реранкера.

Критерий приемки: рост полноты фильтрованного поиска при сохранении p95 и
отсутствии нарушения RLS.

### 7.3. Иерархический поиск

1. Первый уровень: документ/раздел/страница.
2. Второй уровень: точные чанки, таблицы и изображения внутри найденных узлов.
3. Расширение результата родителем, соседями и связанными продолжениями таблиц.
4. Дедупликация пересекающихся фрагментов до реранкера и перед контекстом.
5. Отдельные метрики для текстового, табличного и визуального поиска.

### 7.4. Контекст и проверка цитат

Работы:

1. Считать бюджет контекста настоящим токенизатором текущей модели, а не
   приближением через число символов.
2. Сжимать низкоприоритетные фрагменты экстрактивно, сохраняя числа и ссылки.
3. После ответа выделять проверяемые утверждения и сверять каждое с цитируемым
   фрагментом.
4. Неподтвержденные утверждения удалять, помечать или отправлять на один
   ограниченный повтор генерации.
5. Цитату считать корректной только при семантической поддержке утверждения,
   а не по наличию маркера `[n]`.

Критерий приемки: измеримый рост точности цитат и снижение доли неподтвержденных
утверждений без выхода p95 за согласованный SLA.

## 8. Этап P1: перевод

### 8.1. Память переводов

Ручные правки `SegmentVersion` должны стать источником повторного использования,
но не автоматически доверенным обучающим набором.

Работы:

1. Создать translation-memory с исходником, утвержденным переводом, языковой
   парой, доменом, проектом, редактором и статусом проверки.
2. Искать точные и близкие совпадения до вызова Hy-MT2.
3. Применять перевод автоматически только выше высокого порога; остальные
   совпадения передавать модели как контекст или показывать редактору.
4. Отслеживать происхождение результата и позволять отозвать ошибочную запись.
5. Изолировать проектную терминологию по владельцу/папке при необходимости.

### 8.2. Детерминированная защита технических сущностей

1. До перевода заменять числа, диапазоны, единицы, стандарты, формулы, артикулы
   и XML/Markdown-маркеры устойчивыми плейсхолдерами.
2. После перевода восстанавливать значения и проверять их количество и порядок.
3. Проверять обязательные термины глоссария в итоговом переводе, а не только
   передавать их в промпте.
4. Различать допустимую морфологию и фактическое нарушение терминологии.

### 8.3. Дообучение Hy-MT2

Текущих 9 ручных правок недостаточно. До накопления не менее 1000-3000
проверенных пар основной Hy-MT2-7B следует оставить без дообучения.

После накопления данных:

1. Провести очистку и экспертную выборочную проверку пар.
2. Разделить данные по документам, не допуская утечки между train/test.
3. Обучить LoRA Hy-MT2-7B отдельно от production-весов.
4. Проверить COMETKiwi, экспертную MQM-оценку, точность терминов и документовую
   согласованность.
5. Принимать адаптер только при выигрыше на отложенных документах.

## 9. Этап P2: эксплуатация

### 9.1. Разделить ARQ-очереди

Создать независимые воркеры и очереди:

- `parse`: MinerU/dots/Paddle и загрузка изображений;
- `translate`: основной и дополнительные языки;
- `export_index`: экспорт, рендер, текстовый и визуальный индекс;
- `memory`: извлечение и консолидация памяти.

Использовать стабильные идентификаторы заданий, идемпотентные контрольные точки,
ограниченные повторы и dead-letter очередь. Повтор задачи не должен удалять
успешный результат другого этапа или запускать дублирующий GPU-процесс.

Разделение выполнять без потери текущей очереди Redis: сначала добавить новые
воркеры и маршрутизацию только новых заданий, дождаться завершения старых задач,
затем остановить прежний общий воркер.

### 9.2. Перевести API и воркеры под systemd

Для каждого процесса определить:

- автоматический рестарт и предельное число рестартов;
- зависимости от Postgres, Redis, MinIO и модельных сервисов;
- NUMA/CPU/memory limits, отдельного пользователя и рабочую директорию;
- структурированные журналы и ротацию;
- readiness/liveness-проверки.

### 9.3. Наблюдаемость и SLO

Добавить в Prometheus/Grafana:

- vLLM TTFT, TPOT, throughput, KV-cache и prefix-cache hit rate;
- DCGM-метрики GPU: память, загрузка, температура, XID и OOM;
- возраст и глубину очередей ARQ, число повторов и ошибок по этапам;
- PostgreSQL, Redis и MinIO exporters;
- p50/p95/p99 API, чата, перевода одного сегмента и документа;
- качество production-выборки: ошибки чисел, пустые ответы, отсутствие цитат.

Определить SLO и оповещения до оптимизации производительности. Измерять следует
пользовательскую задержку и пропускную способность, а не только загрузку GPU.

### 9.4. Frontend

1. Устранить ошибки ESLint и добавить их в обязательный CI.
2. Ввести ленивую загрузку тяжелых маршрутов и бюджет размера чанков; отдельно
   контролировать pdf.js worker, viewer и основной bundle.
3. Добавить Playwright E2E для логина, загрузки, ожидания обработки, просмотра,
   редактирования, чата, цитат, экспорта и дополнительных языков.
4. Добавить проверки desktop/mobile, доступности и визуальных регрессий.

## 10. Модельные эксперименты

### 10.1. Оставить в production

- Hy-MT2-7B как основной переводчик;
- Qwen3.5-35B-A3B GPTQ-Int4 как RAG/agent/VL-модель;
- Qwen3-Embedding-8B + Qwen3-Reranker-4B;
- активный Qwen3-VL-Embedding-8B + Qwen3-VL-Reranker-2B.

### 10.2. Отложить до выполнения условий

**Qwen3.6-35B-A3B.** Apache-2.0, 35B/3B active, но BF16 не помещается на одну
A100-40GB, а официального GPTQ-Int4 на момент аудита нет. Вернуться после выхода
официального INT4 и повторить structured-output, agent, vision, длинный контекст,
качество ответа и нагрузочный тест. Источник:
<https://huggingface.co/Qwen/Qwen3.6-35B-A3B>.

**Hy-MT2-30B-A3B.** Apache-2.0, потенциальный пакетный режим повышенного качества,
но BF16 требует нескольких A100 или подходящей квантизации. Из-за отсутствия
NVLink постоянный TP-контур нежелателен. Проверять только если 7B показывает
измеримый потолок качества. Источник:
<https://huggingface.co/tencent/Hy-MT2-30B-A3B>.

**DeepSeek-OCR-2.** Не принимать как замену MinerU только по публичным рейтингам.
Допускать к тесту лишь после подтверждения коммерческой лицензии и выигрыша на
реальных документах.

**HunyuanOCR-1.5.** Не включать в кандидаты: Tencent Hunyuan Community License
не соответствует принятому правилу Apache-2.0 для коммерческого on-prem контура.

## 11. Проверка безопасности RAG

Развернуть локальный Promptfoo или эквивалентный собственный стенд для регулярных
проверок. Promptfoo имеет MIT-лицензию и сценарии RAG red-team:
<https://www.promptfoo.dev/docs/red-team/rag>.

Обязательные классы атак:

- инструкции внутри загруженного документа;
- отравление контекста и ложные ссылки;
- запросы на раскрытие других документов/пользователей;
- попытка заставить агента игнорировать область поиска;
- oversized input, бесконечный tool-loop и истощение токенов;
- извлечение системного промпта, памяти и внутренних идентификаторов;
- вредоносные имена файлов, Markdown/HTML и формулы.

Qwen3Guard-0.6B допустим только как дополнительная модель модерации jailbreak и
опасного контента. Он не заменяет RLS, allowlist инструментов, изоляцию данных и
тестирование косвенных prompt-injection.

## 12. Рекомендуемый порядок исполнения

1. Исправить тесты, Ruff, mypy и ESLint; включить self-hosted CI.
2. Сделать RLS fail-closed, убрать production-секреты по умолчанию.
3. Настроить резервное копирование и выполнить тест восстановления.
4. Создать первую версию эталонного набора и автоматический отчет метрик.
5. Провести MinerU 3.4 A/B.
6. Провести MinerU-Popo A/B и подготовить дерево документа.
7. Проверить `pg_textsearch` и iterative HNSW на эталонном наборе.
8. Реализовать иерархический поиск и проверку цитат.
9. Добавить translation-memory и строгую защиту технических сущностей.
10. Разделить ARQ-очереди, перевести процессы под systemd и расширить мониторинг.
11. Провести закрытую RU/EN/ZH KIE-проверку Qwen3-VL против Granite; Qianfan и
    Infinity-Parser2-Flash повторно не запускать без новой версии модели/рантайма,
    который устраняет зафиксированные провалы качества или SLA.
12. Вернуться к Qwen3.6/Hy-MT2-30B только при наличии аппаратно пригодных весов
    и доказуемой потребности по метрикам.

## 13. Формат отчетности

Каждый пункт вести в `docs/roadmap.md` по схеме:

1. **Plan**: проблема, гипотеза, исходные метрики, критерий приемки и отката.
2. **Act**: точные версии, конфигурация, коммит и измененные компоненты.
3. **Verify**: тестовый набор, команды, метрики до/после, p95 и безопасность.
4. **Report**: решение принять/отклонить/отложить, ограничения и инструкция отката.

Запрещено принимать технологию только по публичному рейтингу или заявлению
разработчика. Публичные результаты используются для отбора кандидатов; решение
принимается исключительно по воспроизводимым замерам DocRAGenslate.

## 14. Production-протокол выпуска

Для каждого выпуска использовать следующий порядок:

1. **Подготовка**: зеленый CI, резервная копия, проверенный rollback, перечень
   миграций, оценка влияния на 10 действующих учетных записей.
2. **Теневой прогон**: новая логика обрабатывает копии запросов или фиксированный
   приемочный набор, но ее результат не видят пользователи и он не изменяет данные.
3. **Пилот**: функция включается флагом для оператора и 1-2 выбранных аккаунтов.
4. **Наблюдение**: не менее одного рабочего цикла без критических ошибок; сравнение
   p95, ошибок, качества, очередей, GPU и пользовательской обратной связи.
5. **Расширение**: поэтапное включение для оставшихся аккаунтов.
6. **Фиксация**: запись результата в `docs/roadmap.md`, сохранение конфигурации и
   версии моделей, решение о сохранении или удалении старого контура.
7. **Откат**: при утечке данных, росте ошибок, потере документов, нарушении RLS
   или существенной деградации качества возврат выполняется немедленно, без
   ожидания окончания периода наблюдения.

Этот протокол выполняется внутри общего цикла WSL → локальная проверка/сборка →
A100 → серверная проверка → production → smoke → синхронизация. Успешная локальная
сборка сама по себе не является разрешением на обновление production, а успешный
серверный тест не заменяет проверку публичного адреса после переключения.

Минимальный автоматический smoke-набор после каждого выпуска:

- `GET https://doc-rag-translate.ds-mind-lab.ru/healthz` → HTTP 200 и `status=ok`;
- `GET /api/config` → production issuer и `auth_enabled=true`;
- главная SPA загружается без ошибок статических ресурсов;
- без токена защищенный API возвращает 401;
- пилотный пользователь видит только свои документы;
- загрузка тестового файла проходит полный путь до `done`;
- чат возвращает ответ с корректной кликабельной цитатой;
- экспорт открывается и содержит ожидаемый контрольный фрагмент.

Целевой технический ориентир: обычное приложение изменений без потери данных и
с откатом не более чем за 15 минут. Изменения БД и моделей, для которых это
невозможно, требуют отдельного согласованного окна и предварительной репетиции.
