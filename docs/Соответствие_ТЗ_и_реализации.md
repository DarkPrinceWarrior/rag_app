# Соответствие ТЗ заказчика и фактической реализации DocRAGenslate

> ## 🔄 Обновление 2026-06-26: доработки реализованы (ветка `feat/tz-doledki-2026-06-26`)
>
> После сверки доработаны все согласованные пункты «частично». Реализовано, развёрнуто
> на сервере и проверено (в т.ч. параллельно под двумя учётками admin/user):
>
> | Пункт | Было | Стало |
> |---|---|---|
> | **4.7.1** Изоляция по пользователю | дыра в поиске | ✅ owner-фильтр в `retrieve()` (поиск/чат/агент) + папки с владельцем + **активный RLS** (2-й контур, роль `rag_api`); проверено — чужой контент не виден нигде |
> | **4.7.2** История правок | отсутствовала | ✅ `segment_versions` (было→стало/кто/когда) + просмотр/откат во вьювере |
> | **4.7.2г** Метаданные | нет «объекта» | ✅ `source_type` + `project_object` (объект строительства) |
> | **4.7.3** Поиск по библиотеке | только по содержимому | ✅ фильтры по имени/типу/дате/объекту + триграммные индексы + панель фильтров |
> | **4.3.2** RU→EN / RU→ZH | только →RU | ✅ перевод документа на EN/ZH по запросу (`document_translations`), проверено: GOST/ISO и числа сохранены |
> | **4.3.5** Единицы/стандарты | только числа | ✅ `validate_standards` (ГОСТ/ISO/API…) + ре-перевод по фидбэку |
> | **4.4.3c** «Глава N» | без адресации | ✅ инструмент `get_chapter(номер)` — раздел целиком по заголовкам |
> | **4.5** Переполнение окна | риск падения | ✅ токен-гард `_fit_context_window` (режет историю/контекст под окно) |
>
> Итог после доработок: **50 реализовано / 5 частично / 0 не реализовано** (из 55).
> Остаточные «частично» — некритичные/допустимые по ТЗ: **4.1.1** (перевод произвольной
> веб-страницы — есть в расширении, нет в самой веб-версии), **4.2.10** (нет отдельного
> выбора режима «страница»/«раздел» — документ переводится целиком; доступны фрагмент и
> таблица), **4.4.3f** (перевод фрагмента командой в чате — есть во вьювере/расширении),
> **4.6.2** (краткое содержание — по диалогу, не отдельной функцией «документ→файл»),
> **5.4** (признаки виджета разнесены по попапу и плавающему ассистенту).
> Детали реализации — журнал `docs/roadmap.md`. Пораздельная сверка ниже частично
> отражает дореализационное состояние — приоритет имеет данный блок «Обновление».

> Документ-сверка: каждое требование ТЗ № 3086-СлЗап(333) («Интеллектуальный инструмент
> для перевода документов и веб-страниц с английского на русский язык») сопоставлено с
> кодом приложения. Статус по каждому пункту **подтверждён чтением исходного кода**
> (с указанием `файл:строка`), а затем перепроверен независимо.
> Источник ТЗ: `docs/ТЗ_инструмент_для_перевода_и_анализа_документации.docx`.

**Метод проверки.** Девять агентов прочитали фактический код по каждой секции ТЗ;
по каждой секции отдельный проверяющий заново открыл процитированный код и подтвердил
либо опроверг оценку. Это не «есть/нет по описанию», а сверка с тем, что реально
выполняется в коде.

---

## Итог

| Раздел ТЗ | Требований | Реализовано | Частично | Не реализовано |
|---|---|---|---|---|
| Цели разработки | 5 | 5 | — | — |
| 4.1 Способ доступа (веб + виджет) | 7 | 6 | 1 | — |
| 4.2 Загрузка и обработка | 10 | 9 | 1 | — |
| 4.3 Перевод текста | 8 | 6 | 2 | — |
| 4.4 Чат с документом | 10 | 8 | 2 | — |
| 4.5 История и контекст | 3 | 3 | — | — |
| 4.6 Экспорт результатов | 4 | 3 | 1 | — |
| 4.7 Библиотека и хранение | 4 | 1 | 3 | — |
| 5. Интерфейс | 4 | 3 | 1 | — |
| **Всего** | **55** | **44 (80 %)** | **11 (20 %)** | **0** |

**Главный вывод.** ТЗ реализовано полностью по сути: все ключевые сценарии работают,
ни одно требование не отсутствует целиком. 11 пунктов помечены «частично» — это либо
сужения, прямо допустимые самим ТЗ (доп. направления перевода — «опционально»), либо
содержательные доделки. **Один пункт требует внимания как риск, а не косметика** —
изоляция документов между пользователями (п. 4.7.1).

---

## 1. Цели разработки — 5/5 реализовано

| № | Требование | Статус | Как реализовано (подтверждено по коду) |
|---|---|---|---|
| G1 | Автоперевод техдокументации EN→RU | ✅ реализовано | Воркер `translate_document` (`workers/tasks.py:419-423`): язык-источник определяется автоматически `detect_lang` по преобладающему алфавиту (`llm/client.py:63`), цель жёстко «ru»; русский текст не переводится (тождество через `needs_translation`). Перевод — модель Hy-MT2 (`llm/fast.py:115`). |
| G2 | Загрузка разных форматов → перевод на RU | ✅ реализовано | `upload_document` (`api/routes/documents.py:48`) принимает 7 форматов, ставит в очередь цепочку разбор→перевод→экспорт (`tasks.py:294, 512`). |
| G3 | Перевод текста веб-страниц в браузере | ✅ реализовано | Расширение: `content.ts:123` `translatePage` обходит узлы и подменяет текст; сервер `api/routes/widget.py:86` `web_translate` переводит узлы батчами. На сервер уходит только текст, без разметки. |
| G4 | Поиск/выжимка/ответы/выгрузка/история | ✅ реализовано | Поиск `api/routes/library.py:124`; вопрос-ответ по документу `api/routes/chat.py:53` с цитатами; выгрузка `documents.py:236`; история в БД. *Оговорка:* «краткое содержание документа» получается запросом в чат, отдельной кнопки нет. |
| G5 | Форматы доступа: веб-версия + расширение | ✅ реализовано | SPA отдаётся из `api/main.py:116-132`; расширение MV3 (`extension/`). Десктоп-приложение снято осознанно — `docs/roadmap.md §0` ссылается на сам п. 4.1 ТЗ («способ доступа — веб-версия и виджет»). |

---

## 2. Раздел 4.1 — Способ доступа (веб-версия и виджет) — 6/7

| № | Требование | Статус | Подтверждение по коду |
|---|---|---|---|
| 4.1.1 | Полноценная веб-версия с полным функционалом | ⚠️ **частично** | Загрузка, библиотека, перевод (автоматом в пайплайне), экспорт, анализ таблиц, чат — всё есть (`routes/index.tsx`, `chat.tsx`). **Но в самой веб-версии нет перевода произвольной веб-страницы** — этой функции нет в маршрутах SPA (навигация только «Библиотека» и «Чат», `__root.tsx:51-54`). Перевод страниц есть только в виджете. |
| 4.1.2 | Компактный виджет через иконку браузера | ✅ реализовано | Собранный манифест: `manifest_version:3`, `action.default_popup=popup.html`, заголовок «DocRAGenslate». Попап на React (`popup/main.tsx`). |
| 4.1.3 | Виджет: быстрая загрузка файла | ✅ реализовано | `popup/App.tsx:76-99` — клик/перетаскивание → `POST /api/documents` с токеном; статус «Файл в очереди на перевод». |
| 4.1.4 | Виджет: перевод текущей страницы | ✅ реализовано | `App.tsx:49` → `content.ts:123` (батчи по 60, прогрессивная замена + кнопка «Оригинал») → `widget.py:86`. |
| 4.1.5 | Виджет: перевод выделенного фрагмента | ✅ реализовано | `content.ts:54-88` (кнопка у выделения → подсказка с переводом) → `widget.py:38` `selection_translate` (Hy-MT2 + глоссарий). |
| 4.1.6 | Виджет: история переводов | ✅ реализовано | `App.tsx:178-190` показывает историю из `storage.local`, клик копирует перевод. *Оговорка:* фиксируются только переводы выделения, не страниц/загрузок (`background.ts:29-34`). |
| 4.1.7 | Виджет: переход в полную версию (новая вкладка) | ✅ реализовано | `App.tsx:152-156` «Открыть веб-приложение» → `window.open(apiBase,'_blank')`. |

---

## 3. Раздел 4.2 — Загрузка и обработка документов — 9/10

| № | Требование | Статус | Подтверждение по коду |
|---|---|---|---|
| 4.2.1 | Форматы PDF/DOCX/XLSX/PPTX/TXT/JPG/PNG | ✅ реализовано | `documents.py:35` белый список из всех 7 форматов ТЗ + проверка байтовой сигнатуры (`_SIGNATURES`). *Оговорка:* список жёсткий, «и иных форматах» (ODT/RTF/HTML) не покрыты. |
| 4.2.2 | Извлечение текста, таблиц, структуры | ✅ реализовано | `segments.py:173-268` раскладывает на заголовки/абзацы/таблицы/рисунки/формулы с уровнями; `_TableHTMLParser` разворачивает объединённые ячейки; геометрия блоков из `parse.py:369`. |
| 4.2.3 | OCR для сканов | ✅ реализовано | Дефолтный разборщик MinerU2.5-Pro читает страницу как изображение (`config.py:202`); явный форс-OCR `tasks.py:155-168` + переразбор с выбором языка `documents.py:176`. |
| 4.2.4 | PDF-текстовые | ✅ реализовано | `kind=pdf_text` + `backfill_text_layer` (`parse.py:222`) добирает прореженные страницы из текстового слоя с дедупом и отбраковкой «зацикливания» модели. |
| 4.2.5 | PDF-сканированные | ✅ реализовано | `scan_pdf.py:57-114` рендерит скан фоном (печати/штампы видны), кладёт перевод плашками по координатам, отдаёт одиночный и двухслойный PDF. |
| 4.2.6 | Изображения/чертежи (печати, штампы, низкое разрешение) | ✅ реализовано | Картинка → 1-страничный PDF (`documents.py:70`) → сканово-OCR-ветка; описание чертежей/схем `vision.py:33`. *Оговорка:* спец-детектора печатей и предобработки низкого разрешения нет; визуальный индекс по умолчанию выключен (`config.py:88`). |
| 4.2.7 | Офисные файлы: разделы, таблицы, реквизиты | ✅ реализовано | `ooxml.py` извлекает и **вписывает перевод обратно в копию оригинала** по ключу позиции (round-trip для docx/xlsx/pptx) — вёрстка сохраняется; интерактивный просмотр листов/слайдов. |
| 4.2.8 | Обработка текста веб-страниц | ✅ реализовано | `content.ts:101-158` + `widget.py:86` — перевод уже открытой страницы. *Оговорка:* это перевод страницы, а не ингест по URL в библиотеку с индексацией (такой ветки нет). |
| 4.2.9 | Индикация распознавания + запуск перевода | ✅ реализовано | Полный цикл статусов (`models.py:41-49`), бейджи «разобран/переведён/готов» (`StatusBadge.tsx`), прогресс на карточке. Перевод стартует автоматически. *Оговорка:* отдельного сообщения «распознавание успешно» нет — информирование через бейдж. |
| 4.2.10 | Выбор режима перевода (документ/страница/раздел/таблица/фрагмент) | ⚠️ **частично** | Есть: полный документ (авто), выделенный фрагмент (`widget.py:60`), таблица (в чате → XLSX). **Нет режимов «страница» и «раздел» и единого селектора режима** в момент запуска — документ переводится целиком автоматически. |

---

## 4. Раздел 4.3 — Перевод текста — 6/8

| № | Требование | Статус | Подтверждение по коду |
|---|---|---|---|
| 4.3.1 | Основное направление EN→RU | ✅ реализовано | `tasks.py:419-423` цель «ru»; `fast.py:46` формирует «Translate … into Russian»; модель Hy-MT2 по умолчанию. |
| 4.3.2 | Доп. направления RU→EN, RU→ZH | ⚠️ **частично** (по ТЗ — опционально) | ZH→RU для документов работает полностью (`detect_lang` ловит китайский, отдельный китайский шаблон). RU→EN / RU→ZH возможны **только для разового выделения** в виджете (`widget.py:28,46`), для целого документа цель замкнута на «ru». ТЗ помечает эти направления как необязательные. |
| 4.3.3 | Структура таблиц/спецификаций, цифры не съезжают | ✅ реализовано | `segments.py:77-98` выравнивает строки по ширине (разворот объединённых ячеек); перевод поклеточно с валидацией чисел (`tasks.py:339-386`); чистые числа/коды не переводятся в текст (`client.py:44`). |
| 4.3.4 | Терминология (глоссарий) | ✅ реализовано | Глоссарий EN→RU с CRUD (`glossary.py`), подбор терминов по границам слов, прокидывание в перевод как якоря (`fast.py:50`). *Оговорка:* глоссарий действует только для EN→RU и не фильтруется по домену. |
| 4.3.5 | Единицы, аббревиатуры, стандарты, обозначения | ⚠️ **частично** | Автоматически сверяются **только числа**. Жёсткое правило про ГОСТ/ISO/API/ASTM и единицы заложено в системный промпт, но он используется только альтернативным движком (Qwen), а дефолтный Hy-MT2 его не применяет. Корректность единиц/стандартов опирается на саму модель перевода + опциональный глоссарий, формальной гарантии нет. |
| 4.3.6 | Сохранение структуры (заголовки, нумерация, таблицы, примечания, реквизиты) | ✅ реализовано | OOXML round-trip в копию оригинала; для PDF/скана `build_docx` собирает заголовки по уровням, таблицы, рисунки с подписями, формулы; `reflow` восстанавливает переносы списков. *Оговорка:* при заливке в DOCX сохраняется формат только первого фрагмента абзаца — смешанная разметка внутри абзаца схлопывается. |
| 4.3.7 | Числа и единицы не искажаются (валидация) | ✅ реализовано | `validate.py:18-52` нормализует и сверяет множества чисел; при потере — один автоматический ре-перевод с подсказкой; иначе сегмент помечается «требует проверки» (`tasks.py:313-334`). |
| 4.3.8 | Результат — редактируемый RU-документ | ✅ реализовано | `export` (`tasks.py:1074-1183`): полноценный `{имя}.ru.docx`, плюс PDF (оверлей для скана / reflow для текста) и копия оригинала с переводом для офисных форматов. |

---

## 5. Раздел 4.4 — Интерактивный режим (чат с документом) — 8/10

| № | Требование (пример из ТЗ) | Статус | Подтверждение по коду |
|---|---|---|---|
| 4.4.1 | Запросы на русском | ✅ реализовано | Весь контур чата на русском (системные промпты, классификатор, эвристики); гибридный поиск строит запрос и для русского, и для английского (`retrieve.py:81`). |
| 4.4.2 | Ответы по тексту документа (с цитатами) | ✅ реализовано | Гибридный поиск (плотный + разреженный) → слияние RRF → реранкер → top-k с фильтром области (`retrieve.py:126`); запрет выдумывать + обязательные ссылки `[n]` → кнопки-цитаты с переходом во вьювер (`chat.py:31, 110`). |
| 4.4.3a | «Выдели основные требования» | ✅ реализовано | Классификатор → агентный многошаговый сбор (`agent.py`, `AgentLoop.gather`). Спец-промпта под выделение требований нет — закрыто общим механизмом. |
| 4.4.3b | «Найди раздел про …» | ✅ реализовано | `tools.py:78` поиск фрагментов + `get_section` отдаёт полный текст фрагмента с путём заголовка. *Оговорка:* возвращается один фрагмент по ссылке, не весь диапазон раздела по оглавлению. |
| 4.4.3c | «Перескажи главу 3 коротко» | ⚠️ **частично** | Суммаризация найденных фрагментов работает, но **адресации «глава N» по номеру нет** — разбор номера есть только для рисунков/таблиц (`tools.py:131,163`), не для глав; «достать всю главу целиком» инструмента нет. |
| 4.4.3d | «Какие сроки поставки?» | ✅ реализовано | Одиночный фактологический вопрос → `single_hop`, один гибридный поиск; правило промпта переносить числа/единицы без изменений. |
| 4.4.3e | «Вытащи спецификации в таблицу» | ✅ реализовано | Два пути: спец-интент `extract_table` (структурированный JSON → XLSX с листом «Источники», `extract.py`, `routes/extract.py`) и инструмент агента `get_tables`. Кнопка «Таблица» в чате. |
| 4.4.3f | «Переведи только второй абзац» | ⚠️ **частично** (самый слабый пункт) | В наборе инструментов агента **переводчика нет** (`agent.py:144`, `tools.py:255`). Перевод произвольного фрагмента есть только в виджете/вьювере (`widget.py:60`), не на странице чата. Косвенно сценарий закрыт тем, что у фрагмента уже есть готовый перевод `text_ru`, который модель может вернуть. |
| 4.4.3g | «Что сказано про испытания?» | ✅ реализовано | Дословный пример из промпта классификатора (`agent.py:55`) → `single_hop` гибридный поиск. |
| 4.4.3h | «Сравни с тем, что обсуждали вчера» | ✅ реализовано | Две опоры: история текущей сессии (окно + сводка + инструмент `get_chat_history`) и кросс-сессионная память по документу (`memory/service.py`). *Уточнение:* слово «вчера» как дата не вычисляется — релевантность по смыслу. |

---

## 6. Раздел 4.5 — История и контекст (накопительный эффект) — 3/3

| № | Требование | Статус | Подтверждение по коду |
|---|---|---|---|
| 4.5.1 | Сохранение истории сессии по документу | ✅ реализовано | `ChatSession` привязан к `document_id`, `ChatMessage` хранит роль/текст/цитаты/время (`models.py:216, 241`); вопрос и ответ коммитятся в БД (`chat.py:91, 233`); чтение в хронологии (`chat.py:324`). |
| 4.5.2 | Контекст в рамках сессии | ✅ реализовано | Окно последних реплик (8, `config.py:143`) + инкрементальная сводка вытесненной истории + инструмент агента `get_chat_history` — всё в рамках одной сессии. |
| 4.5.3 | Накопительный эффект между сессиями по тому же документу | ✅ реализовано | Документная сводка `scope='document'` (одна на документ, обновляется); новый чат на том же документе поднимает тот же контекст (`adapter.py:75`, `chat.py:93`); долговременные факты извлекаются и хранятся (`memory_tasks.py`, `consolidate.py`). Документный поиск нового чата безусловно ограничен тем же документом. |

---

## 7. Раздел 4.6 — Экспорт результатов — 3/4

| № | Требование | Статус | Подтверждение по коду |
|---|---|---|---|
| 4.6.1 | Полный перевод → DOCX | ✅ реализовано | `build_docx` (`export_docx.py:160`) — заголовки, абзацы, формулы, рисунки с подписями, таблицы стилем «Table Grid»; ключ экспорта в БД, скачивание `documents.py:236`. |
| 4.6.2 | Краткое содержание → TXT/DOCX | ⚠️ **частично** | Реализована выжимка **диалога** (`digest.py:21`), а не самостоятельное краткое содержание самого документа; выжимка всегда вшита в общий файл вместе с транскриптом; текстовый вариант отдаётся как Markdown (`.md`), не строго `.txt`. |
| 4.6.3 | Таблицы/спецификации → XLSX | ✅ реализовано | `extract_xlsx` (`routes/extract.py:56`) — openpyxl, лист «Спецификации» + лист «Источники»; кнопка XLSX в чате. Плюс для исходных XLSX перевод вписывается в копию оригинала. |
| 4.6.4 | История диалога → PDF/DOCX | ✅ реализовано | `export_session` (`chat.py:354`) собирает весь диалог → DOCX (`render_docx`). Из «PDF или DOCX» реализован DOCX (дизъюнкция удовлетворена); генерации PDF из транскрипта нет. |

---

## 8. Раздел 4.7 — Хранение и библиотека документов — 1/4 (зона доделок)

| № | Требование | Статус | Подтверждение по коду |
|---|---|---|---|
| 4.7.1 | Личная библиотека с изоляцией по пользователю | ⚠️ **частично — РИСК** | Изоляция работает на уровне приложения (фильтр по владельцу в списке и доступе к документу). **Но есть дыры:** (1) документы с пустым владельцем (dev-режим) видны всем; (2) папки не привязаны к владельцу (`models.py:163` — у `Folder` нет `owner_sub`) → общие для всех; (3) **поиск по содержимому не фильтруется по владельцу** (`retrieve.py:126` не принимает владельца) — это касается `/api/search`, single-hop чата и инструмента агента `search_chunks`; (4) защита на уровне БД (RLS) есть только для таблиц памяти, для документов/чанков — нет. |
| 4.7.2 | Хранение: исходник, перевод, история правок/чата, метаданные | ⚠️ **частично** | Исходник (MinIO), перевод (сегменты + экспортные артефакты), история чата, метаданные (дата/тип/размер/владелец) — есть. **Истории правок/версий перевода нет:** правка сегмента перезаписывает текст без версии, аудит фиксирует только id документа без «было/стало» (`segments.py:105`); класса версий в схеме нет. |
| 4.7.3 | Поиск по библиотеке (название/дата/тип/объект/ключевые слова) | ⚠️ **частично** | Реализован только **поиск по содержимому** (`library.py:124`). Поиска по имени/дате/типу файла нет; «объект строительства» как поле отсутствует (моделируется только папками); список фильтруется по папке на клиенте. |
| 4.7.4 | Группировка по папкам | ✅ реализовано | Полноценные папки: создание с дедупом, перемещение, удаление (документы остаются), счётчики (`library.py:22-82`, `index.tsx`). Удаление папки не удаляет документы. *Оговорка:* папки глобальные (без владельца), вложенности нет. |

---

## 9. Раздел 5 — Требования к интерфейсу — 3/4

| № | Требование | Статус | Подтверждение по коду |
|---|---|---|---|
| 5.1 | Не перегруженный интерфейс (загрузка, список, чат) | ✅ реализовано | Навигация — ровно две вкладки «Библиотека» и «Чат» (`__root.tsx:51`); зона загрузки + виртуализированный список + окно чата. |
| 5.2 | Адаптивность под ноутбуки/ПК | ✅ реализовано | Tailwind-утилиты: контейнеры `max-w-5xl`, боковая панель истории скрывается на узких окнах (`md:flex`), перенос элементов; ассистент в относительных к экрану размерах. |
| 5.3 | Чат как мессенджер (пользователь справа, система слева) + копирование | ✅ реализовано | `chat.tsx`: реплика пользователя `ml-auto bg-primary` (справа), ответ — слева в карточке; текст выделяется и копируется. *Оговорка:* отдельной кнопки «Копировать» в веб-чате нет (есть в попапе расширения). |
| 5.4 | Виджет: компактный, сворачивание, переход в полную версию | ⚠️ **частично** | Все три признака есть, но **разнесены по двум поверхностям**: компактность — везде; сворачивание реализовано в плавающем ассистенте (`DocAssistant.tsx:137`); переход в полную версию — в попапе расширения (`App.tsx:153`). Единого виджета со всеми тремя признаками нет. |

---

## 10. Список доделок (11 пунктов «частично»)

**Приоритет 1 — риск/безопасность:**
1. **4.7.1 Изоляция по пользователю** — закрыть дыры: фильтр владельца в поиске по содержимому (`retrieve.py`), владелец у папок, политики RLS для документов/чанков, поведение для документов с пустым владельцем.

**Приоритет 2 — содержательные пробелы:**
2. **4.7.2** История правок перевода (версии сегментов, «было/стало» в аудите).
3. **4.7.3** Поиск по библиотеке по метаданным (имя/дата/тип) + поле «объект строительства».
4. **4.2.10** Выбор режима перевода (страница/раздел) и единый селектор режима.
5. **4.6.2** «Краткое содержание документа» как отдельная функция и отдельный файл-выжимка (сейчас — выжимка диалога).
6. **4.4.3f** Команда «переведи этот фрагмент» прямо в чате (сейчас — только во вьювере/виджете).
7. **4.4.3c** Адресация «глава N» по номеру и выборка раздела целиком.
8. **4.1.1** Перевод произвольной веб-страницы из самой веб-версии (сейчас — только в виджете).

**Приоритет 3 — мелочи/допустимо по ТЗ:**
9. **4.3.5** Валидация единиц/аббревиатур/стандартов (сейчас — только числа + глоссарий).
10. **4.3.2** RU→EN / RU→ZH для целого документа (ТЗ: «опционально, не приоритет»).
11. **5.4** Объединить компактность + сворачивание + переход в одном виджете; кнопка «Копировать» в веб-чате.

**Сверх ТЗ (реализовано дополнительно):** перевод ZH→RU (китайский) с отдельным шаблоном;
адаптивный агентный чат (классификатор одно-/многошаговых запросов с инструментами);
гибридный поиск + реранкер; долговременная память; экспорт диалога в Markdown;
интерактивный просмотр листов XLSX и слайдов PPTX с наложением перевода;
рендер «как в Microsoft» через LibreOffice; SSO/RBAC/аудит/наблюдаемость.
