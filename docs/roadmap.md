# rag_app — Roadmap

> Рабочий документ по схеме **Plan → Act → Verify → Report**.
> Прогресс по этапам — в «Журнале» в конце файла.

# Корпоративный инструмент перевода и анализа документации (EN→RU)

**Архитектурный план и техническое ТЗ для разработчика**
Версия 1.0 · 10 июня 2026 · на основе ТЗ № 3086-СлЗап(333) от 19.03.2026

---

## 0. Фиксация скоупа (что строим, что выкидываем)

Из ТЗ заказчика убираем десктопное приложение (противоречит же его собственному п. 4.1, где способ доступа — «веб-версия и виджет»). Итоговый продукт:

1. **Веб-приложение** — загрузка документов, перевод с сохранением структуры, side-by-side просмотр оригинал/перевод, редактирование, чат с документом (RAG), библиотека, экспорт.
2. **Браузерное расширение (MV3)** — перевод выделенного текста, перевод текущей страницы, быстрая загрузка файла, история, переход в веб-версию.
3. **Бэкенд-платформа** — пайплайн парсинг → OCR → перевод → реконструкция документа; LLM-инференс полностью on-premise (внешние сервисы перевода в компании запрещены — это жёсткое ограничение из ТЗ, оно диктует весь стек: **только open-weight модели, ничего не уходит наружу**).

Ключевые нефункциональные требования:
- Документ 100+ страниц → перевод за минуты, не часы (асинхронный пайплайн, постраничный параллелизм).
- Сканы с печатями/штампами/низким разрешением → OCR обязателен.
- Таблицы и спецификации: цифры и единицы измерения не искажаются (отдельный контур валидации).
- Результат — редактируемый DOCX/XLSX, а не картинка.

---

## 1. Сводная таблица решений («почему это, а не то»)

| Слой | Выбор | Главные альтернативы | Почему так |
|---|---|---|---|
| Парсинг PDF/сканов | **MinerU 2.5** (+ PaddleOCR-VL как OCR-движок сложных сканов) | Docling, Marker, olmOCR-2, DeepSeek-OCR-2 | MinerU — топ OmniDocBench среди open-source пайплайнов, лучший на сложных таблицах и «грязных» сканах; декомпозиция layout→content; работает на разном железе. PaddleOCR-VL-1.5 (0.9B) — SOTA-класс на OmniDocBench при крошечном размере, 109 языков, устойчив к перекосам/печатям |
| Перевод PDF с сохранением вёрстки | **BabelDOC** (ядро) через **PDFMathTranslate-next** | Самописный re-render, DeepL Document (запрещён) | Единственный зрелый open-source движок «PDF на входе → переведённый PDF с той же вёрсткой на выходе»: DocLayout-YOLO + IR-слой + реконструкция. Работает с любым OpenAI-совместимым LLM → втыкаем свой vLLM. ⚠️ AGPL-3.0 — см. раздел 9 |
| Перевод DOCX/XLSX/PPTX | **Собственный OOXML-транслятор** (обход run'ов) | Конвертация в PDF и обратно | Для офисных форматов вёрстку сохранять не нужно — она уже в XML. Меняем только текстовые узлы, стили/таблицы/нумерация остаются нетронутыми. python-docx / openpyxl / python-pptx |
| LLM перевода | **Qwen3-32B** (рабочая лошадь) + **Hunyuan-MT-7B** (быстрый контур) | Llama 4, Gemma 4, Tower+, NLLB | Qwen3: Apache 2.0 (юридически чистая для коммерции), 119 языков, сильный EN↔RU, та же модель тянет и RAG-чат — один деплой на всё. Hunyuan-MT-7B выиграла WMT2025 в 30 из 31 направлений при 7B параметрах — идеальна для виджета (низкая латентность) |
| Инференс | **vLLM** | SGLang, TensorRT-LLM, LMDeploy | vLLM — безопасный дефолт: OpenAI-совместимый API, самая широкая экосистема, гибкость моделей, prefix caching. SGLang быстрее на shared-prefix нагрузках — держим как drop-in замену на этапе оптимизации (оба отдают OpenAI API, миграция бесплатна) |
| Эмбеддинги | **BGE-M3** (старт) → **Qwen3-Embedding-4B/8B** (качество) | mE5, Jina v5 | BGE-M3: dense+sparse+multi-vector в одной модели → гибридный поиск из коробки, 100+ языков, MIT. Qwen3-Embedding-8B — №1 multilingual MTEB, Apache 2.0 |
| Reranker | **Qwen3-Reranker-0.6B/4B** | BGE-reranker-v2-m3 | Та же серия, что эмбеддер, заточен под top-100 → top-5 |
| Векторное хранилище | **PostgreSQL 17 + pgvector** | Qdrant, Milvus, Weaviate | Корпоративная библиотека — это максимум сотни тысяч–единицы миллионов чанков. До ~5M векторов pgvector — консенсусная рекомендация 2026: ноль лишней инфраструктуры, ACID, фильтры обычным SQL (права доступа, папки, объекты строительства). Qdrant — только если перерастём |
| Очередь задач | **ARQ** (Redis, asyncio-native) | Celery, Dramatiq, Temporal | Весь пайплайн I/O-bound (HTTP к vLLM, S3, Postgres). ARQ нативно async, без sync/async-моста Celery; ретраи, cron, приоритеты есть. Celery тащит инфраструктуру 2009 года; Temporal — overkill для одной команды |
| Backend API | **FastAPI** (Python 3.13) | Litestar, Go | Весь ML-стек — Python; FastAPI остаётся индустриальным стандартом AI-бэкендов 2026, SSE-стриминг из коробки, Pydantic v2 |
| Frontend | **React 19 + Vite 8 + TypeScript** | Next.js, Svelte | SSR не нужен (внутренний инструмент за SSO). Vite 8 (март 2026) с Rolldown — сборка в 10–30× быстрее. TanStack Router + TanStack Query, Tailwind CSS 4 + shadcn/ui |
| Расширение | **WXT** | Plasmo, CRXJS | Консенсус 2026: WXT — лидер (Vite-based, file-based entrypoints, бандл ~в 2 раза меньше Plasmo, кросс-браузерность Chrome/Edge/Firefox из одной кодовой базы, активная поддержка). CRXJS стагнирует, Plasmo — React-only и тяжелее |
| Хранилище файлов | **MinIO** (S3 API) | Файловая система | Версионирование, пресайн-ссылки, бэкап, S3-совместимость для будущего переезда |
| Наблюдаемость LLM | **Langfuse** (self-hosted) | Phoenix/Arize, LangSmith | Open-source, on-prem, трейсы каждого перевода/RAG-запроса, оценка качества, стоимость токенов |
| Авторизация | **Keycloak** (OIDC) ↔ корпоративный AD/LDAP | Самописная | SSO обязателен в корпоративной среде; Keycloak — стандарт on-prem |

---

## 2. Общая архитектура

```
┌────────────┐   ┌─────────────────┐
│  Web SPA   │   │ Browser Ext(WXT)│
│ React 19   │   │ popup + content │
└─────┬──────┘   └────────┬────────┘
      │  HTTPS / SSE      │
      ▼                   ▼
┌─────────────────────────────────────┐
│         API Gateway (FastAPI)       │
│  auth(OIDC) · REST · SSE-стримы     │
└──┬──────────┬──────────┬────────────┘
   │          │          │
   ▼          ▼          ▼
┌───────┐ ┌────────┐ ┌─────────────┐
│Postgres│ │ MinIO  │ │ Redis + ARQ │
│17 +    │ │ (S3)   │ │ task queue  │
│pgvector│ └────────┘ └──────┬──────┘
└───────┘                    │
              ┌──────────────┼───────────────┐
              ▼              ▼               ▼
        ┌──────────┐  ┌───────────┐  ┌────────────┐
        │ Parsing  │  │ Translate │  │ RAG worker │
        │ worker   │  │ worker    │  │ (chat)     │
        │ MinerU + │  │ BabelDOC/ │  │ hybrid     │
        │ PaddleOCR│  │ OOXML     │  │ retrieve + │
        └────┬─────┘  └─────┬─────┘  │ rerank     │
             │              │        └─────┬──────┘
             └──────┬───────┴──────────────┘
                    ▼  OpenAI-compatible API
            ┌──────────────────┐
            │   vLLM cluster   │
            │ Qwen3-32B (AWQ)  │
            │ Hunyuan-MT-7B    │
            │ BGE-M3 / Qwen3-  │
            │ Embedding (TEI)  │
            └──────────────────┘
```

Всё в Docker; старт — docker-compose на одном GPU-сервере, целевое состояние — K8s с выделенной GPU-нодой.

---

## 3. Пайплайн обработки документа (ядро системы)

### 3.1. Ingestion и маршрутизация

```
upload → virus scan (ClamAV) → определение типа →
  PDF:   pdffonts → есть текстовый слой? 
         ├─ да  → text-PDF ветка
         └─ нет → scan-PDF ветка (OCR)
  DOCX/XLSX/PPTX → OOXML ветка
  JPG/PNG        → OCR ветка
  TXT/HTML(web)  → plain ветка
```

Детект текстового слоя — `pdffonts`/pypdfium2 за миллисекунды, без запуска тяжёлых моделей. Смешанные PDF (часть страниц текст, часть скан) маршрутизируются постранично.

### 3.2. Парсинг → структурное представление

Любой вход приводится к **единому промежуточному формату** (JSON, идейно как DoclingDocument / MinerU content_list): дерево блоков `{type: heading|paragraph|table|figure|formula|stamp, bbox, page, text, children}`.

- **MinerU 2.5** — основной пайплайн: layout-анализ отдельно от распознавания контента, чистка колонтитулов, таблицы в HTML, формулы в LaTeX. Бенчмаркается в топе OmniDocBench (CVPR 2025 benchmark, лидерборд обновляется до сих пор — туда же входят PaddleOCR-VL-1.5, GLM-OCR, MinerU2.5).
- **PaddleOCR-VL (0.9B)** — OCR-движок для худших сканов: печати, штампы, перекос, низкое разрешение. Ультракомпактная VLM (NaViT-энкодер + ERNIE-0.3B), 109 языков, в обзорах 2026 её главное достоинство — именно устойчивость к «грязным» реальным документам, а не лабораторным примерам.
- Эскалация: если средний confidence страницы ниже порога — страница рендерится в изображение и прогоняется через **Qwen3-VL** (уже стоит в vLLM) как fallback «умного OCR».

Этот же структурный JSON — источник чанков для RAG (раздел 5), т.е. парсим **один раз**.

### 3.3. Перевод: три ветки по типу источника

**A. PDF (текст и скан) → переведённый PDF + DOCX**
**BabelDOC** (funstory-ai) через обвязку **PDFMathTranslate-next**: layout-детекция (DocLayout-YOLO), извлечение текста с координатами, перевод через наш vLLM (OpenAI-совместимый endpoint — поддерживается нативно), реконструкция PDF с исходной вёрсткой, включая многострочные формулы и таблицы; есть режим bilingual (оригинал и перевод чередующимися страницами) — это закрывает требование «сравнение оригинала и перевода» прямо в экспорте. Параллельно из переведённого структурного JSON собираем редактируемый DOCX (python-docx) — это требование экспорта из ТЗ.

**B. DOCX / XLSX / PPTX → тот же формат, та же вёрстка**
Никакой конвертации в PDF. Идём по OOXML:
- DOCX: обход `paragraph.runs`, склейка run'ов одного абзаца в сегмент (иначе LLM получит рваные куски из-за дробления Word'ом), перевод сегмента, обратное распределение по run'ам с сохранением форматирования первого run'а как доминирующего.
- XLSX: openpyxl, переводим **только** ячейки со строковым типом; числа, формулы, форматы не трогаем по построению — это автоматически гарантирует требование «цифры не съезжают».
- PPTX: python-pptx, текстовые фреймы + заметки; авто-уменьшение кегля при перерастании текстбокса (русский текст в среднем на 15–20% длиннее английского).

**C. Веб-страницы**
Расширение собирает текстовые узлы DOM (TreeWalker), батчирует по ~2–4K токенов с сохранением идентификаторов узлов, шлёт на API, заменяет текст in-place (паттерн Immersive Translate). Никакой выгрузки HTML на сервер целиком — переводим только текст, разметка остаётся в браузере.

### 3.4. Качество перевода: терминология и валидация

1. **Глоссарий** — таблица в Postgres (EN-термин → утверждённый RU-термин, домен: строительство/договоры/нефтегаз). Перед переводом сегмента релевантные термины инжектятся в промпт (constrained terminology); BabelDOC сам умеет document-level terminology extraction — используем.
2. **Контекстное окно**: переводим не сегмент в вакууме, а с 1–2 соседними абзацами и заголовком раздела — резко снижает рассогласование терминов между страницами.
3. **Числовая валидация** (post-check, дёшево и обязательно): регэксп-экстракция всех чисел/единиц из source и target сегмента; расхождение → автоматический ре-перевод с указанием ошибки, после второго фейла → флаг «требует проверки» в UI. Это прямой ответ на требование ТЗ про спецификации.
4. **Оценка качества**: офлайн-харнесс на **COMET / MetricX-24** + выборочная разметка инженерами; A/B Qwen3-32B vs Hunyuan-MT-7B на корпусе реальных документов компании до фиксации дефолтной модели.

### 3.5. Производительность (цель «часы → минуты»)

- Перевод страниц — параллельно: документ 100 стр ≈ 300–500 сегментов → батчи по N конкурентных запросов в vLLM (continuous batching там нативный).
- Прикидка: Qwen3-32B-AWQ на A100-40 даёт ~1–2K tok/s в батче на реплику → две реплики ~2–4K tok/s; 100-страничный документ ≈ 60–80K токенов выхода → **2–5 минут** перевода + 1–2 минуты парсинга. Влезаем в «минуты».
- Прогресс — SSE-стрим в UI постранично (пользователь видит, как документ переводится, может начать читать первые страницы).

---

## 4. LLM-инфраструктура

### 4.1. Модели (все open-weight, всё офлайн)

| Роль | Модель | Лицензия | VRAM (квант) |
|---|---|---|---|
| Перевод (качество) + RAG-чат | Qwen3-32B, AWQ INT4 (FP8 на A100/Ampere недоступен — compute 8.0) | Apache 2.0 | ~20 GB весов → 1×A100-40 |
| Перевод (скорость, виджет) | Hunyuan-MT-7B | Tencent license (проверить юристами) | ~16 GB FP16 / 8 GB INT4 |
| OCR-fallback / чертежи | Qwen3-VL (8B/32B) | Apache 2.0 | 16–40 GB |
| Эмбеддинги | BGE-M3 → Qwen3-Embedding-4B | MIT / Apache 2.0 | 2–10 GB |
| Reranker | Qwen3-Reranker-0.6B | Apache 2.0 | ~2 GB |

Почему Qwen3, а не Llama 4 / Gemma 4: (а) Apache 2.0 без ограничений Llama-лицензии; (б) в обзорах 2026 Qwen3 — консенсусный «безопасный энтерпрайз-выбор» для multilingual; (в) одна модель закрывает и перевод, и чат, и structured output → один деплой, один прогрев, общий KV-кэш.

Почему Hunyuan-MT-7B как второй контур: победа в WMT2025 (30/31 направлений) при 7B — лучшее качество/латентность для интерактивного перевода выделенного текста; направление en→ru входит в её 33 языка.

### 4.2. Сервинг

- **vLLM** ≥ v0.10: OpenAI-совместимый API, prefix caching, continuous batching, AWQ-Marlin. Все воркеры ходят в него по `OPENAI_BASE_URL` — это даёт бесплатную замену движка (SGLang при росте RAG-нагрузки: его RadixAttention даёт документированные 30–40% экономии GPU на shared-prefix трафике, а чат с документом — это именно он) и бесплатную замену модели.
- Эмбеддинги/reranker — **Text Embeddings Inference** (HF TEI) отдельным контейнером.

### 4.3. Раскладка под фактическое железо (Proxmox, хост `zeta`, LXC CT 135: 6×A100-SXM4-40GB)

Финальная диагностика (2026-06-11, проверено на хосте): **NVLink физически отсутствует** — SXM4-модули на PCIe-карьере, без HGX/NVSwitch (ноль линков в `nvlink -c`, пустой `dmesg | grep nvlink`, ни одного NV# в `topo -m`, NVSwitch в `lspci` нет). Связь — только PCIe Gen4 ×16 (~31.5 GB/s/напр. против 600 GB/s у NVLink3), P2P недоступен → `NCCL_P2P_DISABLE=1` — **постоянная корректная настройка**, NCCL-коллективы идут через host-RAM. `nvidia-fabricmanager` не ставить — управлять нечем. Два NUMA-острова: GPU 0–2 (node0) и GPU 3–5 (node1, пара 4↔5 = PIX); A↔B = SYS (UPI).

**Следствие: tensor parallelism исключён архитектурно и навсегда** (не лечится софтом). Вместо TP — **data parallelism**: Qwen3-32B-AWQ (~20 GB весов) целиком влезает на одну A100-40, межкартный трафик отсутствует в принципе — отсутствие NVLink для этой схемы безразлично.

Раскладка (whisperx на GPU0 остановлен 2026-06-11 — проверить, что сервис disabled, а не stopped, иначе вернётся после ребута):

| GPU | NUMA | Сервис | Заметки |
|---|---|---|---|
| 0 | node0 | Qwen3-32B-AWQ — реплика A | перевод + чат |
| 1 | node0 | Qwen3-32B-AWQ — реплика B | за общим роутером (LiteLLM proxy / nginx least_conn) |
| 2 | node0 | Qwen3-VL-8B | OCR-fallback, чертежи |
| 3 | node1 | Hunyuan-MT-7B | быстрый контур виджета, изолирован от batch-нагрузки |
| 4 | node1 | TEI (BGE-M3 + Qwen3-Reranker) + PaddleOCR-VL | мелкие burst-модели |
| 5 | node1 | Резерв: LoRA-дообучение, A/B, стейджинг | при росте → третья реплика 32B; обучение — по рецептам Tier 1/2 runbook'а |

Правила эксплуатации:
- KV-кэш: `--kv-cache-dtype fp8_e5m2` (формат хранения, на Ampere работает) → ~100K+ токенов кэша на реплику при ~16 GB свободной VRAM.
- NUMA-пиннинг обязателен (H2D/D2H-копии не должны ходить через UPI): процессы GPU0–2 → `numactl --cpunodebind=0 --membind=0`, GPU3–5 → node1. `NCCL_P2P_DISABLE=1 NCCL_IB_DISABLE=1` — в окружении постоянно.
- Если в будущем понадобится модель >40 GB: **PP, а не TP**, через границу островов (PP передаёт только активации на границе стейджа — нечувствителен к host-staging); схема TP=2 внутри острова (пара 4↔5 = PIX) × PP=2 между островами — и только для compute-bound задач.
- **ECC оставить Enabled.** Рекомендация выключить ради +1–2 GB/карту годится для research; для прод-сервиса, переводящего договоры и спецификации, тихий bit flip = неотлавливаемое искажение цифр — ровно то, против чего в ТЗ отдельное требование.
- Диск 492 GB мал: веса моделей (~100 GB) + MinIO + Postgres → запросить отдельный том 1–2 TB под `/models` и данные MinIO.
- CPU: контейнер видит 64 из 128 потоков хоста — попросить `pct set 135 -cores 96`, CPU-стадии MinerU и ARQ-воркеры утилизируют.

### 4.4. Специфика деплоя в LXC (не KVM!)

CT 135 — LXC-контейнер: драйвер NVIDIA живёт на хосте `zeta` (580.95.05), внутрь прокинуты `/dev/nvidia*`, userspace-библиотеки в контейнере обязаны **точно совпадать с версией драйвера хоста** (обновление драйвера на хосте = синхронное обновление либ в CT).

- **Основной путь**: GPU-сервисы (vLLM ×3, TEI, OCR) — напрямую systemd-юниты + uv внутри CT, без Docker-прослойки. Меньше слоёв — меньше сюрпризов с драйвером.
- Stateless-инфраструктура (Postgres, Redis, MinIO, FastAPI, фронт) — docker-compose внутри CT: требует `features: nesting=1` на контейнере и nvidia-container-toolkit с `no-cgroups = true`, если какой-то GPU-сервис всё же пойдёт в Docker (cgroup-делегирование устройств в nested-контейнерах не работает).
- Перепроверка железа после любых изменений платформы — по чек-листу Приложения B runbook'а (`nvidia-smi nvlink -c`, `topo -m`, `lspci | grep -i nvswitch`).

---

## 5. RAG: «чат с документом»

Тренд 2026 — **не** тащить LangChain: тонкая собственная оркестрация (или Pydantic AI как лёгкая прослойка), полный контроль промптов, трейсинг в Langfuse.

1. **Чанкинг по структуре**, не по символам: чанк = раздел/подраздел/таблица из структурного JSON парсера (метаданные: страница, bbox, заголовок-путь «4 → 4.3 → таблица 2»). Таблицы — отдельные чанки с markdown-представлением + заголовок таблицы.
2. **Гибридный поиск**: dense (BGE-M3) + sparse (BM25 — `tsvector` в том же Postgres или sparse-векторы BGE-M3) → RRF-слияние → **Qwen3-Reranker** top-20 → top-5. Гибрид критичен именно для технички: артикулы, номера ГОСТов, аббревиатуры dense-поиском ловятся плохо.
3. **Двуязычный индекс**: эмбеддим и оригинал (EN), и перевод (RU) с перекрёстной ссылкой — вопрос на русском находит и английский исходник (BGE-M3/Qwen3-Embedding кросс-язычны по построению).
4. **Цитаты обязательны**: ответ содержит ссылки на чанки → UI подсвечивает место в документе (bbox есть из парсера). Это главная защита от галлюцинаций в договорной тематике.
5. **История и контекст** (п. 4.5 ТЗ): сессии чата в Postgres; при новом вопросе — последние N реплик + суммаризация старых в системный промпт.
6. Спец-интенты из ТЗ («вытащи спецификации в таблицу») — structured output (JSON-режим vLLM) → рендер таблицы → экспорт XLSX.
7. **Адаптивный agentic-уровень** (тренд 2026 — не «agentic везде», а роутинг по сложности). Быстрый классификатор (промпт к Qwen3 со structured output) делит запросы:
   - *single-hop* («найди раздел про ответственность», «что сказано про испытания») → обычный hybrid+rerank, ответ за 2–3 с;
   - *multi-hop / экстракция* («вытащи все спецификации в таблицу», «сравни этот пункт с тем, что обсуждали вчера») → агентный цикл: retrieval как tool call, инструменты `search_chunks`, `get_section(id)`, `get_tables(doc)`, `get_chat_history`; модель итеративно ищет, оценивает достаточность и дособирает контекст.
   - **Стоп-условия — обязательная часть дизайна** (главный провал agentic RAG — незавершающийся цикл): ≤5 итераций, ≤30K токенов на запрос, ≤60 с wall-clock. Реализация — тонкий tool-calling цикл руками (~150 строк, function calling в vLLM), без LangChain; LangGraph — только если понадобятся чекпоинты/human-in-the-loop. Все шаги цикла — в трейсы Langfuse.

---

## 6. Backend

- **FastAPI** (Python 3.13, uv как пакетный менеджер, ruff + mypy), Pydantic v2.
- **ARQ** воркеры тремя пулами: `parse` (CPU/GPU-bound, низкий параллелизм), `translate` (высокий параллелизм, I/O-bound), `index+chat`. Прогресс задач — Redis pub/sub → SSE.
- **PostgreSQL 17**: users, documents, versions, glossary, chat_sessions, chunks(+vector), audit_log. Партиционирование chunks по document_id не нужно до миллионов строк.
- **MinIO**: bucket'ы `originals/`, `translated/`, `exports/`, версионирование включено.
- API-контур: `POST /documents`, `GET /documents/{id}/status` (SSE), `POST /documents/{id}/translate`, `PATCH /segments/{id}` (правки), `POST /chat`, `POST /export/{format}`, `POST /web/translate` (батч узлов для расширения), `POST /selection/translate` (быстрый контур → Hunyuan-MT).
- Экспорт: DOCX — python-docx; XLSX — openpyxl; PDF истории чата — typst или weasyprint.

---

## 7. Frontend (веб)

- **React 19 + TypeScript + Vite 8** (Rolldown-бандлер — Rust, прод-сборка в разы быстрее; вышел в марте 2026).
- **TanStack Router** (file-based, type-safe) + **TanStack Query** (серверное состояние, SSE-интеграция) + **Zustand** для локального.
- **Tailwind CSS 4 + shadcn/ui** — требование ТЗ «интерфейс не перегружен» закрывается дефолтной строгостью shadcn.
- Side-by-side просмотр: слева оригинал PDF (**pdf.js** / react-pdf c канвас-рендером и подсветкой bbox текущего сегмента), справа перевод в **TipTap** (ProseMirror) — инлайн-правки сегментов с автосохранением (`PATCH /segments`). Синхронный скролл по якорям сегментов.
- Виртуализация списков (TanStack Virtual) — документы 100+ страниц не должны убивать DOM.
- Чат — стриминг токенов по SSE, цитаты-чипы → клик скроллит оригинал к bbox.

---

## 8. Браузерное расширение

- **WXT** (Vite-based; file-based entrypoints; единая кодовая база Chrome/Edge/Firefox; бандл заметно меньше Plasmo — в публичных сравнениях ~400KB против ~800KB). React внутри.
- **MV3** (обязателен — MV2 похоронен в 2025): background = service worker, никакого remote code, строгий CSP.
- Компоненты:
  - `popup` — мини-история, кнопка «перевести страницу», drag-n-drop файла, переход в веб-версию;
  - `content script` — UI в **Shadow DOM** (изоляция стилей от страницы): floating-кнопка у выделения → перевод выделенного (быстрый контур Hunyuan-MT, ответ < 2 c); полностраничный перевод через TreeWalker-батчи с прогрессивной заменой узлов;
  - `background SW` — очередь батчей, auth-токен (OIDC PKCE через chrome.identity → Keycloak), ретраи.
- Раздача — корпоративная политика Chrome/Edge (ExtensionInstallForcelist), не публичный стор.

---

## 9. Безопасность, лицензии, соответствие

- **Контур**: всё on-prem, egress c GPU-нод закрыт, модели заливаются офлайн-артефактами. Ни один байт документа не покидает периметр — главный аргумент проекта против запрещённых публичных переводчиков.
- **AGPL-3.0 у BabelDOC/PDFMathTranslate** — единственная юридическая мина стека. Для внутреннего использования без распространения ПО AGPL обычно приемлема (сеть-копилефт триггерится предоставлением сервиса третьим лицам; внутри одного юрлица — серая зона, у многих корпораций бан на AGPL политикой). Действия: (а) согласовать с юристами до начала; (б) изолировать BabelDOC в отдельный микросервис-контейнер с узким API — если придётся выпиливать, заменим только его (запасной путь: свой re-render на pypdfium2 + DocLayout-YOLO, дороже на ~3–4 недели); (в) не модифицировать исходники, конфигурировать снаружи.
- Остальной стек юридически чистый: Apache 2.0 (Qwen3, vLLM, MinerU-движки), MIT (BGE-M3, FastAPI, WXT, shadcn), PostgreSQL/BSD.
- RBAC: документ принадлежит пользователю; папки/проекты с шарингом; pgvector-поиск всегда фильтруется по правам в том же SQL-запросе (нет отдельной vector-БД → нет рассинхрона прав).
- Аудит: кто загрузил/перевёл/экспортировал/спросил — append-only audit_log (требование для договорных документов).
- ClamAV на загрузке; лимиты размера; PDF-санитайзер (pdfcpu) против JS-вложений.

---

## 10. Наблюдаемость

- **Langfuse** (self-hosted): трейс каждого перевода и RAG-запроса (промпт, retrieved chunks, ответ, латентность, токены), датасеты для регрессионных прогонов качества.
- Prometheus + Grafana: vLLM-метрики (TTFT, throughput, queue depth), ARQ-очереди, Postgres.
- Sentry (self-hosted) — ошибки фронта и бэка.

---

## 11. План работ (MVP → прод)

**Этап 1 — скелет (3–4 нед).** Docker-compose: Postgres+pgvector, MinIO, Redis, FastAPI, vLLM c Qwen3-32B-AWQ. Загрузка PDF → MinerU-парсинг → структурный JSON → сегментный перевод → DOCX-экспорт. Примитивный UI списка документов. *Критерий: текстовый 50-страничный PDF переводится в DOCX < 10 мин.*

**Этап 2 — вёрстка и сканы (3–4 нед).** BabelDOC-микросервис (PDF→PDF), OCR-ветка (PaddleOCR-VL), OOXML-транслятор DOCX/XLSX/PPTX, числовая валидация, глоссарий. Side-by-side вьювер с правками. *Критерий: скан договора с печатями → переведённый PDF с сохранённой вёрсткой + редактируемый DOCX.*

**Этап 3 — RAG и библиотека (3 нед).** Гибридный индекс, reranker, чат с цитатами и подсветкой, история сессий, папки/поиск по библиотеке, экспорт выжимки и истории чата.

**Этап 4 — расширение (2–3 нед).** WXT: перевод выделения, перевод страницы, popup, OIDC, корпоративная раздача.

**Этап 5 — прод (2–3 нед).** Keycloak/AD SSO, RBAC, аудит, Langfuse, нагрузочное (20 одновременных документов), бенчмарк качества COMET на корпусе компании, при необходимости — переезд translate-контура на SGLang.

Итого ~3,5–4,5 месяца одним сильным фуллстеком + поддержка DevOps на GPU-инфраструктуру.

---

## 12. Ключевые репозитории

| Что | Где |
|---|---|
| MinerU | github.com/opendatalab/MinerU |
| PaddleOCR / PaddleOCR-VL | github.com/PaddlePaddle/PaddleOCR |
| BabelDOC | github.com/funstory-ai/BabelDOC |
| PDFMathTranslate-next | github.com/PDFMathTranslate/PDFMathTranslate-next |
| OmniDocBench (выбор OCR по фактам) | github.com/opendatalab/OmniDocBench |
| Qwen3 / Qwen3-Embedding | github.com/QwenLM/Qwen3, github.com/QwenLM/Qwen3-Embedding |
| Hunyuan-MT | github.com/Tencent-Hunyuan/Hunyuan-MT |
| vLLM | github.com/vllm-project/vllm |
| SGLang (план Б инференса) | github.com/sgl-project/sglang |
| TEI (эмбеддинги) | github.com/huggingface/text-embeddings-inference |
| pgvector | github.com/pgvector/pgvector |
| ARQ | github.com/python-arq/arq |
| WXT | github.com/wxt-dev/wxt |
| Langfuse | github.com/langfuse/langfuse |
| DocLayout-YOLO | github.com/opendatalab/DocLayout-YOLO |

---

## 12.1. План обновления моделей (зафиксирован 2026-06-11, исполнение после этапа 5)

Архитектура держит замену любого движка сменой `base_url`/имени модели в конфиге.
Порядок по соотношению эффект/риск:

1. ✅ *(выполнено 2026-06-12, см. журнал)* **Эмбеддер: BGE-M3 → Qwen3-Embedding-0.6B** (немедленно, дёшево). Легче (~1,2 vs 2,3 ГБ),
   выше на multilingual-MTEB, dim 1024 → схема pgvector не меняется, только переиндексация
   (`scripts/reindex_all.py`). Добавить instruction-prefix для запросов (серия instruction-aware,
   без него −1–5%). **Reranker: BGE-reranker-v2-m3 → Qwen3-Reranker-4B** — та же точка
   подключения (`/v1/rerank`, vLLM `--runner pooling` + hf_overrides yes/no-классификатора),
   ~8 ГБ рядом с эмбеддером на GPU4. 8B-reranker не брать: скорит top-20 в интерактиве,
   латентность важнее последних процентов.
2. ❌ *(A/B проведён 2026-06-12 — НЕ переключаться, см. журнал)* **LLM: Qwen3-32B → Qwen3.6-27B
   только через A/B** (вышел 22.04.2026, Apache 2.0, dense 27B, мультимодальный, BF16 55,6 ГБ).
   Итог A/B на 300 парах (COMETKiwi-22, тот же пайплайн с глоссарием): прод 0.8411 vs кандидат
   0.7116; даже по чистым переводам медиана хуже (0.808 vs 0.846), скорость ~10× хуже, data-free
   AWQ-квант QuantTrio разваливается на длинных прод-промптах с глоссарием/контекстом (111/300
   ответов — мусор «!!!»; диагностика: 17/20 мусора даже при concurrency=1 — батчинг ни при чём;
   короткие одиночные фразы при greedy проходят. Официального AWQ у Qwen нет, FP8 на Ampere
   невозможен). Вернуться к вопросу при выходе
   официального AWQ-INT4 или калиброванного кванта + зрелой поддержки гибридной DeltaNet-архитектуры
   в vLLM; кандидатом остаётся и Qwen3.5/3.6-35B-A3B MoE.
3. ⏸ *(замер 2026-06-13 — на текущем корпусе НЕ переключаться, см. журнал)* **Эмбеддер-максимум:
   Qwen3-Embedding-4B/8B** — решение по цифрам retrieval. Бенч-харнесс `scripts/eval_retrieval.py`
   готов; на тест-корпусе 4B не дал прироста (хуже 0.6B при ×3 стоимости индексации) — корпус из
   дублей плохо различает эмбеддеры. **Перемерить на реальной библиотеке заказчика** (одна команда).
   Подводный камень при переходе: dim 2560/4096, HNSW-лимит 2000 → MRL-обрезка (текстовая серия
   MRL-обученная, у неё усечение валидно) или `halfvec`.
4. ✅ *(выполнено 2026-06-13, см. журнал)* **Визуальный контур: Qwen3-VL-Embedding-8B** (GPU5
   `:8007`) — **отдельным контуром, не вместо текстовых**: по тексту MMTEB VL-эмбеддер уступает
   текстовому того же размера (подтверждено релизным блогом Qwen). Реализовано: `page_embeddings`
   (dim 4096 без HNSW — страниц на порядки меньше чанков), индексация страниц сканов в цепочке,
   `GET /api/search/visual` (RU-запросы прозрачно переводятся быстрым контуром). VL-Reranker-8B —
   добавить при росте визуальной библиотеки.

## 13. Главные риски

1. **AGPL BabelDOC** — решается изоляцией + юристами (раздел 9); запасной самописный re-render оценён.
2. **Качество EN→RU на узкой терминологии** (нефтегаз/стройка) — глоссарий с первого дня + быстрый цикл оценки на реальных документах заказчика; при провале — LoRA-дообучение Qwen3 на парах из накопленной библиотеки (данные будут копиться сами).
3. **GPU-дефицит** — стек квантуется: Qwen3-14B-AWQ + Hunyuan-MT-7B живут на одной 48GB-карте с приемлемой деградацией.
4. **Сканы катастрофического качества** — честный confidence-флаг «не распознано надёжно» вместо тихой галлюцинации; страница помечается в UI.
5. **Доп. направления перевода ru→en, ru→zh** — в ТЗ § 4.3 это явно **опциональный, не приоритетный** функционал (не «ползучесть скоупа»). Архитектурно направление перевода — конфиг промпта (`lang_in/lang_out`); в MVP фиксируем EN→RU, доп. направления — отдельным этапом по согласованию с заказчиком (ru→en умеренно; ru→zh дороже: CJK-шрифты, отсутствие глоссария, замер качества).


## Статус этапов (план работ § 11)

- [x] **Этап 1 — скелет** *(2026-06-11; критерий «50-стр. текстовый PDF → DOCX < 10 мин» выполнен: 126 с)*
- [x] **Этап 2 — вёрстка и сканы** *(2026-06-11; критерий «скан с печатями → PDF с вёрсткой + DOCX» выполнен: оверлей-рендер, печати сохранены; OOXML и глоссарий проверены e2e)*
- [x] **Этап 3 — RAG и библиотека** *(2026-06-11; гибридный поиск + reranker + чат с цитатами и историей + папки/поиск; agentic-уровень § 5 п.7 и экспорт выжимки — следующая итерация)*
- [x] **Этап 4 — браузерное расширение** *(2026-06-11; WXT/MV3: выделение <2 c через Hunyuan-MT, перевод страницы, popup; OIDC и корпоративная раздача — этап 5)*
- [x] **Этап 5 — прод** *(2026-06-11; SSO/RBAC/аудит/Langfuse, нагрузочное: 20 документов за 9,5 мин без ошибок; COMET-бейзлайн: COMETKiwi-22 = 0.8426 на 500 парах)*

## Журнал

| Дата | Что сделано | Проверка | Статус |
|---|---|---|---|
| 2026-06-11 | Инициализирован скелет репозитория: `uv` (Python 3.13), Serena, `CLAUDE.md`/`AGENTS.md` с MCP-инструкциями, `docs/`. Залит на GitHub (public) и на a100-сервер `/root/projects/rag_app`. | `gh repo view`, клон на сервере | ✅ |
| 2026-06-11 | **Этап 1, описание проекта**: заполнен Project Purpose в `CLAUDE.md`/`AGENTS.md` (назначение, компоненты, точки входа, статус этапов); roadmap с ТЗ синхронизирован сервер→локаль и закоммичен. | diff CLAUDE.md/AGENTS.md (byte-identical) | ✅ |
| 2026-06-11 | **Этап 1, backend-скелет**: FastAPI (`POST/GET /api/documents`, download original/docx, retry, healthz, примитивный UI на `/`); ARQ-воркеры `parse_document → translate_document → export_document`; БД `documents`+`segments` (SQLAlchemy 2.0 + asyncpg, create_all на старте — alembic отложен на этап 2); MinIO-хранилище (bucket'ы originals/artifacts/translated/exports); зависимости в `pyproject.toml`, MinerU вынесен в extra `parse` (на сервере: `uv sync --extra parse`). | `ruff` чисто, `pytest` 3/3, смоук импорта | ✅ |
| 2026-06-11 | **Этап 1, инфраструктура a100**: docker-compose — Postgres 17+pgvector (`127.0.0.1:5433`), Redis 7 (`:6379`), MinIO (`:9000/:9001`); порты смещены — 5432/8000/3000/8010 заняты соседними проектами. API на `:8100`, vLLM на `:8001`. Всё слушает только 127.0.0.1; доступ к UI — `ssh -L 8100:127.0.0.1:8100 a100`. | `docker compose ps` — все healthy | ✅ |
| 2026-06-11 | **Этап 1, vLLM**: Qwen3-32B-AWQ (веса 19 ГБ → `/root/models/Qwen3-32B-AWQ`), systemd-юнит `vllm-qwen32b` — GPU0, `--kv-cache-dtype fp8_e5m2`, `--max-model-len 16384`, numactl node0, NCCL_P2P_DISABLE=1; отдельный venv `/root/services/vllm-qwen32b` (py3.12, vLLM 0.22.1). Грабля: FlashInfer JIT-компилирует сэмплер при первом старте → нужны `ninja` в venv и `CUDA_HOME`/`PATH` с nvcc (поправлено в юните и setup-скрипте). Перевод в non-thinking режиме (`chat_template_kwargs.enable_thinking=false`). | `systemctl is-active`, `/v1/models`, смоук-перевод (числа/единицы сохранены) | ✅ |
| 2026-06-11 | **Этап 1, парсинг**: MinerU **3.3.0** (новее 2.5 из ТЗ; формат `content_list` совместим). Грабли: в 3.x нет флага `-d` — девайс через env `MINERU_DEVICE_MODE` (GPU2 по раскладке § 4.3); `mineru` берётся из venv воркера (`sys.executable`), не из PATH. В сегменты пишем bbox (для подсветки цитат RAG на этапе 3). Сканы без текстового слоя честно отклоняются с понятной ошибкой (OCR — этап 2). | прогрев на 3-стр. PDF, `content_list.json` разобран в 20 сегментов | ✅ |
| 2026-06-11 | **Этап 1, e2e-критерий**: 50-стр. текстовый PDF (312 сегментов, 10 таблиц) → переведённый DOCX за **126 с** (критерий < 10 мин, запас ~5×). Перевод сегментов конкурентно (semaphore 12), контекст = заголовок раздела + предыдущий абзац (заголовки — без контекста, иначе модель утаскивала контекст в ответ; текст обёрнут в `<doc>`-маркеры). UI проверен Playwright-смоуком (список, статусы, прогресс, ссылки скачивания). | время e2e, DOCX: 312 абзацев/10 таблиц, числа в таблицах не искажены, маркеров в тексте нет | ✅ |
| 2026-06-11 | **Известные ограничения этапа 1** (план на этап 2): только текстовые PDF; терминология местами кривая («Pressure Vessels» → «Давление сосудов») — лечится глоссарием § 3.4; в одной таблице потерялась ячейка «1» (rowspan у MinerU) — поймает числовая валидация § 3.4; единицы в таблицах остаются латиницей (MPa, ºC). GitHub-push из сессии заблокирован (политика) — код доставлялся `git push ssh://a100/...` (на сервере `receive.denyCurrentBranch=updateInstead`); push в origin — за Русланом. | — | ⚠️ |
| 2026-06-11 | **Этап 2, схема БД**: переход на alembic (async-шаблон): `0001` — baseline этапа 1, `0002` — glossary, `documents.kind`/ключи экспортов, `segments.needs_review`/`validation`; `create_all` удалён. На сервере: `alembic stamp 0001` → `upgrade head`. | миграция применена на a100 без потери данных | ✅ |
| 2026-06-11 | **Этап 2, OCR-ветка**: сканы идут через `mineru -m auto` (text/OCR постранично), маршрутизация по типу: `pdf_text`/`pdf_scan`/`docx`/`xlsx`/`pptx`. Скан 4 стр. с печатями (150 DPI, генератор `scripts/make_scan_pdf.py`) распознан в 27 сегментов и переведён. | e2e: скан → 27 сегментов, перевод 27/27 | ✅ |
| 2026-06-11 | **Этап 2, числовая валидация** (§ 3.4 п.3): мультимножество чисел источника обязано сохраниться в переводе (нормализация: десятичная запятая, разделители тысяч); расхождение → 1 ре-перевод с фидбеком → флаг `needs_review` + детали в `segments.validation`; подсветка в UI. Лишние числа в переводе не валят проверку (числительное → цифра). | 6 юнит-тестов; e2e: review=0 на всех документах | ✅ |
| 2026-06-11 | **Этап 2, глоссарий** (§ 3.4 п.1): таблица `glossary`, CRUD `/api/glossary` (upsert по en_term), сид 28 терминов нефтегаз/сварка/договоры; подбор терминов по вхождению (границы слов, длинные фразы первыми, ≤10) → блок «обязательная терминология» в промпте. | e2e DOCX: «scope of work»→«Объём работ», «pressure vessel»→«сосуд под давлением», «hydrostatic testing»→«гидравлическое испытание» | ✅ |
| 2026-06-11 | **Этап 2, OOXML-транслятор** (§ 3.3.B): DOCX (склейка runs, перевод в первый run, body+таблицы), XLSX (только строковые ячейки; числа и формулы не трогаются по построению), PPTX (text frames + заметки); адрес блока — `meta.location`, обратная запись в копию оригинала → экспорт `source`. | 3 round-trip юнит-теста; e2e: docx 7 сегм. (стиль Heading 1 сохранён), xlsx 4 сегм. (числа 120/48 и формула `=B3*2` нетронуты) | ✅ |
| 2026-06-11 | **Этап 2, PDF с вёрсткой**: два контура. (а) Текстовые PDF — **BabelDOC 0.6.3** в изолированном venv `/root/services/babeldoc` (AGPL § 9: только CLI, конфиг снаружи), mono+dual PDF. (б) **Сканы** — BabelDOC их не переводит («Scanned PDF detected») → собственный оверлей-рендер (`pipeline/scan_pdf.py`, «запасной путь» из § 9): фон — скан с печатями, текстовые блоки закрыты плашками с переводом по bbox. Грабля: bbox из `content_list` — во внутреннем масштабе MinerU, координаты в пунктах — в `middle.json` `para_blocks` (проверено сверкой с pdfium); геометрия пишется в `meta.bbox_pt`/`page_size_pt` при парсинге (текст — по нормализованному тексту, таблицы/формулы/картинки — по порядку). | e2e скан: PDF (RU) + PDF (EN+RU) + DOCX; печати и штамп «СОГЛАСОВАНО» сохранены, визуальная проверка рендера | ✅ |
| 2026-06-11 | **Этап 2, side-by-side вьювер**: `/view?id=…` — EN|RU по страницам, contenteditable-правки (PATCH `/api/segments/{id}`, снимает `needs_review`), «Пересобрать экспорт» (`POST /documents/{id}/reexport`); подсветка сегментов с проваленной валидацией. Playwright-смоук: рендер, правка через PATCH — ок. | Playwright + curl PATCH | ✅ |
| 2026-06-11 | **Этап 2, BabelDOC — три грабли и решения.** (1) Qwen3 отвечал BabelDOC в thinking-режиме (он не шлёт `chat_template_kwargs`) — экспорт висел 30+ мин, `<think>`-мусор в PDF → в юните vLLM `--default-chat-template-kwargs '{"enable_thinking": false}'`; наш клиент шлёт флаг явно, поведение не изменилось. (2) Кэш переводов BabelDOC (`~/.cache/babeldoc/cache.v1.db`) был отравлен thinking-прогоном — вычищен; с чистым кэшем 50 стр → PDF за **253 с**. (3) Вотермарк-страница в выводе → `--watermark-output-mode no_watermark`. Плюс по находке Руслана: `--auto-enable-ocr-workaround` теперь передаётся всегда на ветке `pdf_text` — закрывает searchable-сканы (растр+текстовый слой); для image-only сканов флаг не работает («no paragraphs», проверено) — там наш оверлей. | финальный e2e: 50 стр → PDF (вёрстка сохранена, кириллический текстовый слой, числа на месте) + DOCX + dual-PDF | ✅ |
| 2026-06-11 | **Известные ограничения этапа 2** (план на этап 3+): PDF-вёрстка для текстовых PDF переводится самим BabelDOC — наш глоссарий/валидация/правки в этот контур не попадают (только в DOCX-экспорт; вариант — кормить BabelDOC `--glossary-files` из нашей таблицы); оверлей сканов — растровый PDF (текст не выделяется); правки сегментов скана применяются к оверлею при ре-экспорте, но не к DOCX-вёрстке BabelDOC; PPTX без авто-уменьшения кегля при переполнении текстбокса; XLSX переводит только строковые ячейки (без учёта стилей с числовым форматом текста). | — | ⚠️ |
| 2026-06-11 | **Этап 3, сервисы GPU4** (§ 4.3): vLLM-юниты `vllm-bge-m3` (эмбеддинги dense 1024, `:8002`, `--runner pooling`) и `vllm-reranker` (BGE-reranker-v2-m3, `:8003`, `/v1/rerank`), NUMA node1, суммарно ~12 ГБ VRAM на GPU4. Веса — `/root/models/`. Qwen3-Reranker-0.6B — план апгрейда (та же точка подключения). | `/v1/models` обоих, кросс-язычный rerank смоук | ✅ |
| 2026-06-11 | **Этап 3, индекс**: alembic 0003 — `CREATE EXTENSION vector`, `chunks` (text_en/text_ru, два эмбеддинга, generated `tsvector` russian+english, HNSW ×2 + GIN), `chat_sessions`/`chat_messages`, `folders` + поля документа (chunk_count, indexed_at, index_error, folder_id). Чанкинг по структуре (§ 5 п.1): чанк = раздел до следующего заголовка (лимит 4000 симв.), таблицы отдельными чанками без фильтра длины; heading_path, страницы, bbox сегментов в meta. `index_document` в цепочке после export; `scripts/reindex_all.py` для до-индексации. | 6 документов → 134 чанка; юнит-тесты чанкинга | ✅ |
| 2026-06-11 | **Этап 3, гибридный поиск** (§ 5 п.2–3): dense (`LEAST` cosine по emb_en/emb_ru — двуязычность) + BM25 (`websearch_to_tsquery` russian+english) → RRF (k=60) → reranker top-20 → top-5; фильтры document_id/folder_id обычным SQL в тех же запросах; деградация до RRF-порядка при недоступном reranker'е. `GET /api/search` — поиск по библиотеке со сниппетами. | «испытательное давление» → таблицы с 23.6 MPa, score 0.92/0.91/0.86 | ✅ |
| 2026-06-11 | **Этап 3, чат** (§ 5 п.4–5): `POST /api/chat` — SSE-стрим; промпт с обязательными цитатами `[n]` и честным отказом; история сессии (последние 8 реплик); цитаты парсятся из ответа → `chat_messages.citations` (chunk, документ, heading_path, страницы, bbox — задел подсветки). UI `chat.html`: выбор документа/вся библиотека, стрим, цитаты-чипы → `/view`. Грабли: default `uuid4` колонок срабатывает на flush (не при конструировании) + порядок INSERT по FK → явный id и `flush()`. | e2e: «испытательное давление?» → «1,43× расчётного, ≥60 мин [1]» (стр. 43); follow-up «А в МПа?» → «23,6 МПа» (история работает); скан: «248 HV10, NACE MR0175 [1]» (стр. 3); Playwright-смоук UI | ✅ |
| 2026-06-11 | **Этап 3, библиотека**: папки (`/api/folders`, перенос документа, счётчики), фильтр и гибридный поиск в `index.html`. | curl: создание папки, перенос, счётчик documents=1 | ✅ |
| 2026-06-11 | **Известные ограничения этапа 3** (план на этап 4+): agentic-уровень § 5 п.7 (multi-hop, tool-цикл со стоп-условиями) не реализован — только single-hop hybrid+rerank; спец-интенты «вытащи спецификации в таблицу» (§ 5 п.6) и экспорт выжимки/истории чата — не реализованы; цитаты-чипы ведут на `/view` без прокрутки к bbox (подсветка в PDF — вместе с pdf.js-вьювером); суммаризация старой истории чата не делается (только последние 8 реплик); BabelDOC-глоссарий (`--glossary-files`) всё ещё не подключён. | — | ⚠️ |
| 2026-06-11 | **Этап 4, быстрый контур** (§ 4.1/4.3): Hunyuan-MT-7B (веса 15 ГБ) на GPU3, юнит `vllm-hunyuan` (`:8004`, NUMA node1); официальный промпт «Translate the following segment into Russian…» с рекомендованными параметрами (t=0.7, top_p=0.6, top_k=20, rp=1.05). `llm/fast.py` — фолбэк на Qwen3 non-thinking при недоступности. API виджета (§ 6): `POST /api/selection/translate`, `POST /api/web/translate` (батч узлов [{id,text}], конкурентность 16, упавшие узлы остаются как есть); CORS middleware. | латентность выделения **754–1389 мс** (критерий < 2 с); батч 12 узлов — 518 мс; числа и русский текст не искажаются | ✅ |
| 2026-06-11 | **Этап 4, расширение** (§ 8): `extension/` — WXT 0.20 + React 19, MV3 (SW, без remote code). Content script: кнопка у выделения и тултип в **closed Shadow DOM**, полностраничный перевод TreeWalker-батчами по 60 узлов с прогрессивной заменой и откатом «Оригинал» (script/style/code/pre и невидимые узлы пропускаются, разметка на сервер не уходит — § 3.3.C). Background SW: вся сеть, ретраи ×3, история в storage.local (20 записей). Popup: перевести страницу/оригинал, загрузка файла в библиотеку, история с копированием, настройка адреса API (storage.sync). Сборка: 208 КБ, zip 68 КБ. | сборка `wxt build` + `tsc --noEmit` чисто | ✅ |
| 2026-06-11 | **Этап 4, e2e в браузере**: playwright-смоук с собранным расширением (`extension/e2e/smoke.mjs`): выделение → кнопка → тултип «…16,5 МПа при проектной температуре 120 градусов» (hunyuan-mt-7b, 717 мс) → полностраничный перевод in-place (бейдж «переведено узлов — 3»). Грабли: content script не работает на `data:`/`file:` URL (смоук поднимает локальный http) и расширения не грузятся в старом headless (нужен `--headless=new`). | скриншоты e2e, перевод страницы и тултип визуально проверены | ✅ |
| 2026-06-11 | **Известные ограничения этапа 4** (план на этап 5): OIDC PKCE (chrome.identity → Keycloak) не подключён — API без аутентификации, CORS `*`; корпоративная раздача (ExtensionInstallForcelist) и прод-`host_permissions` — при развёртывании; терминология Hunyuan без глоссария («кислая среда» вместо «сероводородная среда» из glossary) — приемлемо для быстрого контура, документный перевод идёт через Qwen3+глоссарий; PDF-вкладки браузера content script'ом не переводятся (нет DOM) — пользователь загружает файл в библиотеку через popup. | — | ⚠️ |
| 2026-06-11 | **Этап 5, SSO**: Keycloak 26.4 в compose (`:8180`, база — отдельная БД в нашем Postgres), realm `rag-app` импортируется из `deploy/keycloak/` (клиенты rag-web/rag-extension public+PKCE, роли user/admin, тестовые пользователи). `api/auth.py`: bearer-токен по JWKS (кэш 1 ч), флаг `RAG_AUTH_ENABLED` (по умолчанию false — включение одной переменной). UI: `static/auth.js` — PKCE-вход без внешних библиотек (без CDN, § 9), перехват fetch `/api/*`. Грабли: (1) в JWKS Keycloak есть enc-ключ RSA-OAEP — PyJWK на нём падает, фильтр `use=sig`; (2) issuer зависит от хоста обращения — пиним `KC_HOSTNAME`; (3) Keycloak 24+ требует firstName/lastName, иначе «Account is not fully set up»; (4) H2-volume падает по правам — база в Postgres. | без токена 401; с токеном 200; PKCE-вход в браузере (Playwright): редирект → логин → список документов | ✅ |
| 2026-06-11 | **Этап 5, RBAC и аудит**: `documents.owner_sub` (sub из токена), список и доступ по id фильтруются: admin — всё, user — свои + dev-документы (owner NULL); чужой документ отвечает 404 (существование не раскрывается). Append-only `audit_log` (миграция 0004, `REVOKE UPDATE/DELETE`): upload/retry/download/chat_query/segment_edit/glossary_upsert; ошибка аудита не роняет операцию. | curl-смоук: `chat_query` записан с username=ruslan | ✅ |
| 2026-06-11 | **Этап 5, Langfuse** (§ 10): v3 self-hosted отдельным compose (`deploy/langfuse/`, web `:8200`, свои postgres/clickhouse/redis/minio во внутренней сети, headless-инициализация ключей через `LANGFUSE_INIT_*`). `observability.py`: трейс `rag-chat` (span retrieve со score'ами + generation) и `translate-document`; no-op без ключей, ошибки трейсинга не роняют операции. Грабля: `LANGFUSE_*` из `.env` не попадают в `os.environ` (pydantic-settings не экспортирует) → `load_dotenv()`. | трейс виден через public API Langfuse (`/api/public/traces`) | ✅ |
| 2026-06-11 | **Этап 5, нагрузочное — критерий выполнен**: 20 одновременных документов × 10 стр. (200 страниц, 1220 сегментов) → все done за **574 с (~9,5 мин)** полного цикла (парсинг → перевод → экспорт → RAG-индекс, 200 чанков), ноль ошибок. **Главная находка теста**: pypdfium2 не потокобезопасен — 4 конкурентных `pdf_info` из `asyncio.to_thread` убивали воркер segfault'ом без трейсбека; фикс — глобальный `PDFIUM_LOCK` на все обращения к pdfium (parse + оверлей сканов). Замер — по таймстемпам БД (created_at → indexed_at). | SQL-тайминги; воркер жив весь прогон | ✅ |
| 2026-06-11 | **Этап 5, COMET-бейзлайн снят** (после принятия лицензии cometkiwi на HF): `scripts/eval_comet.py`, метрика **COMETKiwi-22** (reference-free QE), 500 пар EN→RU из БД (Qwen3-32B-AWQ + глоссарий + числовая валидация), GPU5. Результат: **средний 0.8426**, медиана 0.8473, p10 0.8121, **доля < 0.5 — 0.0%** (ни одного подозрительного сегмента). Это базовая линия для A/B с Qwen3.6-27B (§ 12.1): переключение только при выигрыше по этой же метрике на этом же корпусе. Технически: отдельный venv `/root/services/comet-eval` (unbabel-comet ↔ mineru конфликт по transformers), HF-токен на сервере. | прогон 500 пар, отчёт в /tmp/comet_eval.log | ✅ |
| 2026-06-11 *(обновлено 2026-06-13)* | **Этап 5 / проект — что осталось** (за рамками MVP). **Блок A (прод-готовность) выполнен 2026-06-13 — см. строку ниже**: OIDC в расширении + `RAG_AUTH_ENABLED=true`, CORS без wildcard, Prometheus/Grafana+Sentry, prod-scaffold Keycloak и раздачи расширения. **Остаётся**: фактическая пусконаладка у заказчика (реальный TLS-сертификат Keycloak, координаты AD/LDAP, MDM/GPO-раскладка расширения) — scaffold готов, но не проверяем без инфраструктуры заказчика; SGLang-эксперимент (§ 4.2); ru→en/ru→zh (§ 13 п.5 — согласовать скоуп с заказчиком); перемер эмбеддера на документах заказчика (на публичных аналогах сделан 2026-06-14 — 0.6B силён, r@5=0.900). | — | 📋 |
| 2026-06-12 | **§ 12.1 п.2: A/B Qwen3.6-27B — НЕ переключаться.** Стенд: QuantTrio/Qwen3.6-27B-AWQ (data-free, официального AWQ нет) на GPU5 `:8005`, прод-vLLM 0.22.1 загрузил гибридную DeltaNet-архитектуру без правок. Харнесс `scripts/ab_translate.py`: те же 300 сегментов, тот же пайплайн (контекст+глоссарий), COMETKiwi-22 по обоим наборам одним прогоном. Итог: прод Qwen3-32B **0.8411** (0% мусора) vs кандидат **0.7116** (10% < 0.5); медиана чистых переводов кандидата тоже хуже (0.808 vs 0.846); скорость ~10× ниже (102 c vs 10 c на той же подвыборке — Triton JIT DeltaNet-ядер, без CUDA-графов). Главный дефект: квант стабилен только на коротких одиночных greedy-запросах, на реальных промптах — мусор «!!!» (111/300; диагностика: 17/20 при concurrency=1, 13/20 при 12 — батчинг ни при чём; сэмплинг t=0.3/0.7 ломает даже короткие). Кандидат на будущее: официальный AWQ-INT4 (в discussions Qwen просят), калиброванные кванты, Qwen3.6-35B-A3B MoE. | A/B 300 пар, COMET оба набора, диагностика serial/concurrent, скорость на одинаковой подвыборке | ❌ |
| 2026-06-13 | **§ 12.1 шаг 3: 4B-эмбеддер — на текущем корпусе не переключаться.** Бенч `scripts/eval_retrieval.py`: 60 синтетических вопросов из чанков (генерация Qwen3-32B, фикс. seed — один набор для всех), dense recall/MRR в памяти, max по EN/RU-векторам как в проде. Итог: 0.6B r@5=0.250/MRR=0.182 vs 4B full r@5=0.133/MRR=0.108, 4B+MRL-1024 r@5=0.200/MRR=0.122; индексация ×3 дороже. Оговорка: тест-корпус — 50 секций-дублей, эталон один из близнецов → метрика шумная (n=60), но прироста, оправдывающего миграцию, нет. Перемер на реальной библиотеке — одна команда. | три прогона на одном наборе вопросов | ⏸ |
| 2026-06-13 | **§ 12.1 шаг 4 выполнен: визуальный контур сканов** — Qwen3-VL-Embedding-8B (юнит `vllm-visual-embedding`, GPU5 `:8007`, отдельный venv), `page_embeddings` (0005+0006), задача `index_pages_visual` в цепочке после экспорта (только pdf_scan), `GET /api/search/visual`. **Три грабли подряд**: (1) bf16 → NaN даже на мини-картинках; (2) fp16 → NaN на реальных страницах в зависимости от содержимого пикселей (2B и 8B одинаково; fp32 не влезает в A100-40) — корень: **скомпилированные ядра vision-башни vLLM 0.22.1 на Ampere; `--enforce-eager` лечит полностью** (4/4 страницы на 144 DPI); вероятно, тот же баг внёс вклад в «!!!»-мусор A/B Qwen3.6 — при реванше пробовать eager первым делом; (3) **MRL-усечение 4096→1024 разрушает ранжирование** (VL-серия не Matryoshka-обученная; текстовая Qwen3-Embedding — обученная, там усечение валидно) → полный dim 4096 без HNSW (последовательный скан, страниц мало). Кросс-язычность VL-эмбеддера слабая (RU 0.33 vs EN 0.56 на контроле) → RU-запросы прозрачно переводятся Hunyuan'ом. E2e: «таблица с проектными параметрами» → стр. 2 и 4 (верно, 0.66); печати на тест-скане есть на всех страницах — штамп стр. 1 находится только специфичным запросом (свойство тест-данных). | NaN-диагностика по dtype/размеру/eager, e2e визуальный поиск | ✅ |
| 2026-06-15 | **Сверка с оригинальным ТЗ** (`docs/ТЗ_инструмент_…docx`). roadmap покрывает ТЗ ~90%. Разрывы: (1) **форматы загрузки JPG/PNG/TXT** (§ 4.2) — в плане § 3.1 есть, в коде не было (`ALLOWED_EXTENSIONS` = pdf/docx/xlsx/pptx) → закрыто строкой ниже; (2) **выбор режима перевода** (полный/страница/раздел/таблица/фрагмент, § 4.2) — полный + виджет-фрагмент были, on-demand перевод фрагмента в вебе добавлен; (3) ru→en/ru→zh — формулировка § 13 п.5 поправлена (это опциональный пункт ТЗ § 4.3, не «ползучесть»); (4) межсессионная память по документу (§ 4.5) — есть внутрисессионная + суммаризация (остаётся как доработка); (5) фасет «объект строительства» (§ 4.7) — закрыт папками + полнотекстом. Экспорт § 4.6 — закрыт полностью (DOCX/MD/XLSX/DOCX-история). | чтение ТЗ docx, сверка по разделам | 📋 |
| 2026-06-15 | **Закрыты разрывы с ТЗ §4.2: форматы JPG/PNG/TXT + перевод фрагмента.** (1) **JPG/PNG** — изображение документа на загрузке оборачивается в 1-страничный PDF (Pillow) → существующий `pdf_scan`-пайплайн (MinerU OCR → перевод → оверлей/DOCX); ноль изменений в parse/export. (2) **TXT** — новый `kind=text`: parse разбивает на абзацы-сегменты (без OCR), export — только DOCX; колонка `kind` строковая, миграция не нужна. (3) **Перевод фрагмента** (режим «выделенный фрагмент») — `POST /api/translate/fragment` (Qwen3 + глоссарий, документ-качество, `app.state.translator`) + страница `/translate` в SPA (EN→RU, копирование). `ALLOWED_EXTENSIONS` и `accept` = pdf/docx/xlsx/pptx/jpg/png/txt. | e2e на реальных данных: PNG (рендер стр. NORSOK M-501, 1191×1684) → `pdf_scan/done`, 5 сегм., OCR-перевод, 1 чанк; TXT → `text/done`, 4 сегм.; фрагмент в браузере «corrosion allowance… sour service» → «припуск на коррозию… сероводородной среде» (глоссарий применён) | ✅ |
| 2026-06-15 | **Форс-OCR для PDF с битым ToUnicode-cmap (таблицы-«каша»).** Диагноз (по «Борец», сам распарсил рендер стр.10 в 200 DPI и сверил): тело документа — корректный cmap (чистый русский), но шрифт таблицы винтовых насосов имеет **битый ToUnicode** → MinerU брал текст из битого слоя («Hacoc», «no,aya», «3OBHE»); рендер страницы при этом идеально читаем (глифы целы, ломается только Unicode-извлечение). Эксперимент `mineru -m ocr -l east_slavic` (Cyrillic-модель PaddleOCR) подтвердил: таблица распозналась верно («Диапазон подач при 120–750 об/мин», «потребляемая/рекомендуемая мощность насоса/двигателя, кВт», «500 об/мин»), тело не деградировало. Оформлено как функция: поля `documents.parse_force_ocr`/`ocr_lang` (миграция 0008), `run_mineru(method=, lang=)`, `parse_document` чтит флаг (форс-OCR → `kind=pdf_scan`, экспорт оверлеем, а не babeldoc по битому тексту), `POST /api/documents/{id}/reparse-ocr {lang}` (выбор сохраняется, переживает retry), кнопка «OCR-распознавание» во вьювере PDF. Применено к «Борец» (east_slavic) → таблица читаема. Остаток: единичные глифы («Hacoc», единица «M/CyT») — предел OCR; язык по умолчанию east_slavic (англ. сканы — en, дефолт пайплайна `-m auto -l en` не тронут). | 25/25 pytest, ruff/tsc чисто; standalone OCR-прогон (341 элемент, тело чистое) + e2e: «Борец» reparse-ocr → `kind=pdf_scan`, шапка таблицы по-русски, структура (colspan=4/rowspan=2) цела | ✅ |
| 2026-06-15 | **Постраничный вьювер (оригинал↔перевод синхронно) + merged-ячейки таблиц.** По замечаниям Руслана. (1) **Пагинация**: правая панель перевода — не сплошная прокрутка, а одна страница за раз со стрелками ←/→ и счётчиком «стр. N / M», синхронно с левым `PdfPane` (общий контролируемый `page`-state; клик любой стрелки листает обе панели; цитата/seg-переход ставит страницу). (2) **Таблицы с объединёнными ячейками**: `_TableHTMLParser.cells()` отдаёт сырые строки со спанами → `meta.table_cells` → `SegmentOut` → UI рендерит настоящую `<table>` с `colSpan/rowSpan`; перевод берётся построчно из `translated_text` (превью строится по сырым ячейкам — одна строка на строку таблицы, 1:1 с переводом), фолбэк на исходный текст ячейки при сбое разметки; шапка (по rowspan первой строки) выделяется; merged-таблица read-only (старый редактируемый рендер — фолбэк для документов без `table_cells`). Бонус: построчный вход переводчику дал часть ячеек по-русски («Насос»/«Масса, кг» вместо «Hacoc»/«Macca,Kr»). Оговорка: OCR-искажения кириллицы остаются (битый cmap исходника). | 25/25 pytest (+parse_table спаны, +table_cells в сегменте); e2e: «Борец» стр.10 — обе панели «стр.10/30», правая = 4 сегм. (только страница), таблица colSpan=4 «500 об/мин» над 4 под-колонками + rowSpan=2; клик «→» листает обе панели 10→11 | ✅ |
| 2026-06-15 | **Синхронизация страниц оригинал↔перевод + ровные таблицы (colspan/rowspan).** По замечаниям Руслана. (1) **Sync страниц**: правая панель — единая прокрутка всех страниц перевода; scroll-spy по разделителям `data-page` переключает левый `PdfPane` на текущую читаемую страницу (проп `pageHint`, bbox-подсветку не трогает). (2) **Таблицы**: `_TableHTMLParser` разворачивает `colspan`/`rowspan` в ровную сетку (текст — в левую-верхнюю ячейку спана, продолжения пустые); раньше объединённые шапки MinerU давали рваные строки. На таблице винтовых насосов «Борец» (стр.10): было `[5,4,8,8,…]`, стало все 34 строки = 8 колонок, шапка `colspan=4` «500 об/мин» над 4 под-колонками собрана верно. Оговорка: OCR-текст этой таблицы искажён (кириллица→латиница-двойники: «Hacoc»=Насос) — уровень OCR/битого cmap исходного PDF, не структура (кандидат на форс-OCR таблиц). | 24/24 pytest (+colspan/rowspan grid); e2e: test_3p — scroll перевода вниз → оригинал слева стр.1→2, вверх → 1; реальный HTML таблицы «Борец» → distinct_widths=[8] | ✅ |
| 2026-06-15 | **Форматирование вьювера: иерархия/списки + единый document-flow для всех форматов + bbox у старых документов.** Расследование (по замечаниям Руслана): списки `•\t` MinerU отдаёт отдельными paragraph → вложенность восстановима на рендере; уровни заголовков MinerU в этом корпусе почти все =2 (глубже не различает); bbox у старых pdf_text = 0 — парсились до появления геометрии. SOTA-обзор (июнь-2026, MinerU/Docling/Marker/Unstructured/LlamaParse): парсер адекватен, проблема в недоиспользовании его вывода + рендере, менять не нужно. Сделано: (1) **списки** — подряд идущие буллет-пункты группируются в `<ul>` с отступом и висячей строкой (детект по глифу `•·‣▪►–…`; нумерацию НЕ трогаем — ложные срабатывания на измерениях «16.5 MPa»); (2) **иерархия заголовков** — размер+отступ по `heading_level`; (3) **единый `DocFlow`** вместо карточек-секций: PDF — справа сплошной документ, txt/docx/xlsx/pptx — две колонки «оригинал \| перевод», обе документом (раньше грид-карточек); (4) **bbox** — `highlightOf` мягко деградирует (нет геометрии → клик всё равно переходит на страницу сегмента); старые pdf_text (test_50p#2, test_3p) переразобраны → bbox 312/312 и 20/20, подсветка восстановлена. Известный предел: MinerU плющит под-заголовки до уровня 2 (глубже — потребует инференса уровня по кеглю из middle.json). | tsc+vite чисто; e2e: «Борец» стр.6 — 6 буллетов в одном `<ul>` с отступом; docx — 2 колонки flow без карточек; test_3p после переразбора — клик стр.2 → bbox-оверлей виден, панель на стр.2/3 | ✅ |
| 2026-06-15 | **Доводки по находкам прогона картинок: (1) OCR-шапка больше не теряется, (2) перевод во вьювере — структурой документа.** (1) **Титульная шапка**: MinerU помечает её `type:"header"` и кладёт в конец content_list, а `content_list_to_segments` обрабатывал только text/table/image/equation → титул молча выпадал. Теперь: бегущий колонтитул (повтор текста на ≥2 страницах) отбрасывается как шум, **уникальный** header промотируется в `heading` (level 1) и вставляется в начало своей страницы; footer по-прежнему отбрасывается. (2) **Вьювер**: правая панель PDF-вьювера переписана из плоского списка карточек-сегментов в сплошной документ — заголовки по уровню, абзацы текстом, таблицы настоящими `<table>` (правка ячеек реасемблируется в `\|`-формат); инлайн-правка, bbox-подсветка по клику и маркер «проверить числа» сохранены, добавлены разделители страниц. | юнит-тесты (промоция уникального header / отброс бегущего+footer, порядок idx) 7/7 segments; e2e на сервере: retry демо-PNG → **12 сегм.** (было 11), idx 0 = heading «TECHNICAL SPECIFICATION…» (RU); вьювер в браузере — титул заголовком, разделы, реальная таблица, клик→bbox слева | ✅ |
| 2026-06-14 | **Чат: суммаризация старой истории (§ 5 п.5) + экспорт выжимки.** (1) **Суммаризация**: миграция 0007 (`chat_sessions.summary` + `summary_msg_count`); вытесненные за окно `rag_history_messages`=8 старые реплики инкрементально сворачиваются в русскую сводку (`ChatEngine.summarize_history`), которая инжектится в системный промпт ответа (`stream_answer(..., summary=)`); пересчёт только новых вытесненных (не каждый ход весь диалог). (2) **Экспорт**: `rag/digest.py` (LLM executive-выжимка + транскрипт + дедуп-источники) → `GET /api/chat/sessions/{id}/export?format=md|docx` (StreamingResponse, python-docx); в SPA-чате кнопки «Выжимка: MD / DOCX». | 21/21 pytest; e2e: 6-ходовый диалог → `summary_msg_count=2`, сводка в БД заполнена; экспорт md 13 КБ (заголовок+выжимка+пункты) и docx 40 КБ (magic PK) | ✅ |
| 2026-06-14 | **§ 7 — веб-фронт перенесён на React-SPA** (было: примитивный server-rendered vanilla-UI; стало — стек по ТЗ). `web/`: Vite 8 + React 19 + TS, TanStack Router (file-based) + Query + Virtual, Zustand, Tailwind 4 + shadcn-style UI-кит (вендорен вручную, без Radix — оффлайн/контроль), PKCE-auth портирован из `static/auth.js` в React-провайдер. Страницы: **библиотека** (поллинг статусов, папки, drag-n-drop, гибридный поиск, виртуализация), **чат** (SSE + трасса агента + цитаты-чипы), **вьювер pdf.js** (канвас оригинала + bbox-оверлей по сегментам + правки перевода с автосохранением; worker бандлится `?worker` — без CDN), **экстракция** (таблица + XLSX). Бэкенд: `bbox`/`page_size` в `SegmentOut` (для подсветки на PDF); FastAPI отдаёт SPA из `web/dist` (catch-all → index.html, `/api`·`/healthz`·`/metrics` целы); старые `api/static/*.html`+`auth.js` удалены, `/static`-mount убран. Сборка локальная (`deploy/build_web.sh` — на сервере Node нет), `web/dist` в `.gitignore`. **Развилки (на своё усмотрение):** обычный Vite (не rolldown — стабильность), `contenteditable` (не TipTap — сегменты plain-text), `dist` не коммитим. Версии сверены через tavily (Tailwind v4 `@tailwindcss/vite`, shadcn+React 19, TanStack). | 21/21 pytest; tsc+vite чисто; e2e в браузере (Playwright + токен Keycloak): PKCE-логин → библиотека (реальные доки + папки) → вьювер (pdf.js рендерит NORSOK M-501, bbox по `?seg` на стр. 3) → чат («&lt;0.7 Маха [1]» с цитатой) | ✅ |
| 2026-06-14 | **Функциональный блок: spec-интенты (§ 5 п.6) + подсветка цитат + BabelDOC-глоссарий.** (1) **Spec-интенты**: `rag/extract.py` (structured output `response_format=json_schema` → {title, columns, rows, sources}) + `POST /api/extract/table` и `POST /api/extract/xlsx` (openpyxl, stateless StreamingResponse) + страница `/extract` (выбор документа, рендер таблицы, скачивание XLSX, источники со ссылками на `/view`). E2e: «системы покрытий в таблицу» → 9 строк (номер/температура/подготовка), XLSX валиден (magic PK). (2) **Подсветка цитат**: в цитату добавлены `segment_ids` (из `chunk.meta`), чипы чата и источники экстракции → `/view?id=…&seg=…&page=…`; `view.html` скроллит к фрагменту и подсвечивает (flash-анимация, якоря `page-N` + `data-id` сегмента). Без тяжёлого pdf.js-вендоринга — улучшение существующего вьювера. E2e: цитата несёт seg_ids + page. (3) **BabelDOC-глоссарий**: `write_glossary_csv` (формат BabelDOC `source,target,tgt_lng`, фильтр tgt_lng=ru) из таблицы glossary → `--glossary-files` в `_export_pdf_layout`; утверждённая терминология теперь доходит и до PDF-контура вёрстки (раньше только DOCX — ⚠️ этапа 2 закрыта). E2e: реэкспорт → babeldoc с `--glossary-files`, CSV 28 терминов («pressure vessel→сосуд под давлением»). | 21/21 pytest (+тест формата глоссария); e2e extract table+xlsx, citation seg_ids, babeldoc glossary args | ✅ |
| 2026-06-14 | **§ 12.1 п.2 — валидация на реальных документах домена** (публичные аналоги). Загружены 3 англоязычных PDF (полный путь parse→translate→export→index, все pdf_text): NORSOK P-001 Process design (446 сегм./86 чанков), NORSOK M-501 Protective coatings (275/67), Sunpower EPC Contract (1107/170) — нефтегаз + коррозия + договоры, 323 чанка. **Перевод (COMET wmt22-cometkiwi-da, 1722 пары)**: средний **0.8188**, медиана 0.8402, p10 0.7325, доля<0.5 — **0.4%** (≈7 сегм.); против синтетического бейзлайна 0.8426 — −2,4 п. (реальный юридический/стандартный текст плотнее), мусора практически нет. **Retrieval (Qwen3-Embedding-0.6B, 30 вопросов из чанков, корпус 457)**: **recall@1=0.800, recall@5=0.900, MRR@10=0.843** — на порядок лучше синтетики из дублей (там r@5=0.250); подтверждает § 12.1 п.3: проблема была в шумном тест-корпусе, не в эмбеддере — на 4B мигрировать не нужно. **RAG e2e (через API с токеном Keycloak)**: Q1 «число Маха факел/PSV» → 0,6 / <0,7 Маха верно (2 цитаты); Q2 «покрытия в таблицу» → markdown-таблица систем по диапазонам температур (Sa 2½, ISO 8503; 6 цитат); Q3 «EPC: приёмка+гарантии в таблицу» → таблица (гарантия 5 лет и т.д.; 5 цитат). Классификатор все три → multi_hop, агент завершал за 1 итерацию (8 чанков хватало, стоп=finish, ~2K токенов) — стоп-условия держат, runaway нет; экстракция в таблицу — готовый фундамент под spec-интенты § 5 п.6. Документы оставлены в библиотеке как постоянный реальный тест-набор; `scripts/_realdocs_qa.py` — генератор doc-scoped QA. | COMET 1722 пары; retrieval 30Q/457 чанков; 3 RAG-запроса e2e | ✅ |
| 2026-06-13 | **§ 5 п.7 — adaptive agentic-RAG.** Классификатор сложности (`response_format=json_schema` на Qwen3-32B — прод-vLLM поднят без `--enable-auto-tool-choice`, function calling эмулируется structured output; `guided_json` верхним уровнем игнорируется, работает именно `json_schema`) делит запросы single_hop/multi_hop + эвристический fallback. **multi-hop** — hand-rolled ReAct-цикл (`rag/agent.py` + `rag/tools.py`): инструменты `search_chunks`/`get_section`/`get_tables`/`get_chat_history` копят evidence-пул (ref = 8 симв. uuid) → union → reranker → top-12 → существующий `stream_answer` (контур цитат `[n]` не тронут). **Стоп-условия** (главный провал agentic RAG — незавершающийся цикл): ≤5 итераций / ≤30K токенов (сумма usage) / ≤60 c wall-clock + finish-действие модели; при пустом evidence — подстраховочный single-hop. SSE-события `mode`/`step`/`agent_summary` (chat.html рендерит трассу шагов; неизвестные типы старый UI игнорирует). Шаги цикла — в Langfuse (`log_agent_trace`). Настройки `agent_*` в config. | 19/19 pytest, ruff чисто; e2e через SSE с токеном Keycloak: single «испытательное давление» → single_hop, 5 цитат; multi «сравни… и сведи в таблицу» → multi_hop, 3 шага (2 поиска + get_section), стоп=finish, 12 фрагм./7065 ток., ответ — markdown-таблица с 12 цитатами | ✅ |
| 2026-06-13 | **Блок A прод-готовности (§ «что осталось», п.1–4).** (1) **OIDC в расширении**: `extension/utils/auth.ts` — `chrome.identity.launchWebAuthFlow` PKCE S256 → Keycloak, authority/client_id берутся с `/api/config` (не зашиты), токены в `storage.local` + refresh; вход/выход в popup (через SW, чтобы окно пережило закрытие попапа), Bearer во всех запросах SW и при загрузке файла; `permission identity`, `RAG_EXT_HOST`/`RAG_EXT_KEY` в `wxt.config.ts`. Сборка `tsc --noEmit` + `wxt build` чисто (213 КБ); прод-вариант добавляет корп-хост в `host_permissions` и фикс. `manifest.key`. (2) **CORS без wildcard** → `settings.cors_origins` + регулярка `chrome-extension://.*` (фоновый SW ходит по host_permissions в обход CORS). (3) **Мониторинг (§ 10)**: `prometheus-fastapi-instrumentator` → публичный `/metrics`; `deploy/monitoring/` (Prometheus :9090 + Grafana :3001). **Грабля**: API слушает только `127.0.0.1:8100` — из bridge-сети контейнера loopback недоступен (`host.docker.internal` не помогает) → `network_mode: host` + оба сервиса привязаны к 127.0.0.1; datasource+дашборд провижинятся. **Sentry** в `observability.py` (модульный init, no-op без `SENTRY_DSN`, ловит API и воркер). (4) **`RAG_AUTH_ENABLED=true`** в `.env` сервера + рестарт API. **Грабля**: `/metrics` обязан остаться публичным (вне `require_user`) — иначе после включения auth Prometheus-таргет падает (проверено: target up). **Прод-scaffold** (инфра заказчика, локально непроверяемо): `deploy/keycloak/{PROD.md,docker-compose.prod.yml,ldap-federation.example.json}` (TLS, `start --optimized`, федерация AD); `deploy/extension-policy/` (ExtensionSettings/ExtensionInstallForcelist, `update_manifest.xml`, стабильный ID). Зависимости: `prometheus-fastapi-instrumentator`, `sentry-sdk[fastapi]` (`uv sync --extra parse` — mineru сохранён). | без токена 401 (`/api/documents`, `/api/search`), с токеном Keycloak (ROPC ruslan, azp=rag-web, roles admin/user) 200; `/healthz`+`/metrics` 200 публично; Prometheus target up + PromQL отдаёт данные; Grafana datasource+дашборд; Sentry init с DSN — клиент active; OIDC e2e интерактивно — ручной загрузкой в браузер (realm-redirect готов) | ✅ |
| 2026-06-12 | **§ 12.1 шаг 1 выполнен: Qwen3-Embedding-0.6B + Qwen3-Reranker-4B** вместо BGE-M3/BGE-reranker-v2-m3. Эмбеддер: dim 1024 сохранён — схема pgvector не тронута, только переиндексация (134 чанка); запросы теперь с инструкцией (`embed_query`), документы — без. Reranker: юнит с официальным vLLM-рецептом (`hf_overrides`: Qwen3ForSequenceClassification + classifier_from_token [no,yes]). **Грабля №1** — vLLM не оборачивает вход шаблоном сам: без него скоры слипаются (0.34/0.27 на контрольной паре), с полным chat-шаблоном — 0.97/0.00. **Грабля №2** — SO_REUSEPORT: старый vllm-bge-m3 не был выведен, оба процесса слушали :8002, запросы раскидывались между ними → 404 «через раз» (setup-скрипт теперь глушит старые юниты безусловно). Результат на контрольных запросах: «испытательное давление» — в топе таблицы с прямым ответом (раньше — общие секции); «припуск на коррозию» — точное попадание 0.997 при фоне 0.95 (у BGE всё слипалось в 0.96±0.003). GPU4: 0.15+0.35 VRAM, место под PaddleOCR осталось. | контрольная пара rerank, 3 запроса до/после, e2e чат с цитатой, 6/6 переиндексированы | ✅ |
