global:
  scrape_interval: 15s
  scrape_timeout: 10s

scrape_configs:
  - job_name: rag-api
    metrics_path: /metrics
    static_configs:
      # host-режим контейнера: API на 127.0.0.1:8100 хоста
      - targets: ["127.0.0.1:8100"]
        labels:
          service: rag-api
